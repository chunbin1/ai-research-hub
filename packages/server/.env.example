ZHIPU_API_KEY=
ZHIPU_MODEL=glm-4.7
ZHIPU_EMBEDDING_MODEL=embedding-3
PORT=3001
CHROMA_URL=http://localhost:8000
# 研报章节长、问题宽,单篇检索返回块数上调(默认 5 偏紧,关键节常排到 6~9 位)
RAG_MAX_K=10
# cosine 距离阈值(默认 0.7);# LOG_RETRIEVAL=1 打印每次检索的候选距离
# RAG_DISTANCE_THRESHOLD=0.7
# LOG_RETRIEVAL=1
