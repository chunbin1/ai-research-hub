# 手动添加自选股(管理员)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 管理员在 `/signals` 上手动添加标的(添加前探测并确认公司身份),并把「禁用」换成「删除」——软删除墓碑使已删标的不会被下一次「重新抽取」带回来。

**Architecture:** 新增一个纯编排的 `probeSymbol`(复用既有的 `normalizeSymbol` + `fetchDailyQuotes`);`watchlist.enabled` 语义改为「未删除」,不改表结构;删除同时清掉该标的两个周期的日志与事件。前端把「启用」开关列换成「删除」操作列,并加一个两步式(查询 → 确认)的添加弹窗。

**Tech Stack:** Fastify 5 + better-sqlite3 + node:test(服务端);React 19 + antd 6 + vitest + @testing-library/react(前端)。无新增依赖。

**Spec:** `docs/superpowers/specs/2026-08-22-手动添加自选股-design.md`

## Global Constraints

- **不改表结构。** `watchlist.enabled` 的语义从「启用/禁用」改为「**未删除 / 已删除**」,
  在建表语句与类型定义处加注释说明。列名不动。
- **「禁用」功能整体移除。** 操作栏只有「删除」。
- **删除是软删除**(`enabled = 0`),墓碑行保留 —— 它使 `addWatchlistEntry` 的
  `ON CONFLICT DO NOTHING` 天然阻止「重新抽取」复活已删标的。**不要改抽取逻辑。**
- **删除同时清掉该标的两个周期的 `daily_states` 与 `signal_events`。**
- **添加前必须探测并由管理员确认。** 查询成功前「确认添加」按钮禁用。
- `enough === false`(历史不足 120 根)**不阻止添加**,只在界面提示。
- 三个新路由**全部仅管理员**(`requireAdmin`)。
- **不放宽抽取正则。** 本功能是绕过它的手段,不是修它。
- 注释与用户可见文案一律中文。
- 服务端测试用 `node:test` + `assert/strict`;测试文件 import 被测模块用 `.ts` 后缀;
  源码 import 带 `.js` 后缀(ESM)。前端测试用 `vitest` + `@testing-library/react`,
  前端 import **不带**扩展名。
- **测试不得依赖真实网络访问** —— `probeSymbol` 的 `fetchDailyQuotes` 经参数注入。
- antd `Alert` 用 `title` 属性(antd 6),不是 `message`。
- 两个汉字的 `Button` 加 `autoInsertSpace={false}`;装饰性图标加 `aria-hidden`。

## File Structure

**服务端**

| 文件 | 职责 | 动作 |
|---|---|---|
| `packages/server/src/services/signals/probeSymbol.ts` | 归一化 + 拉一次行情,返回可供确认的身份信息 | 新增 |
| `packages/server/src/services/watchlistStore.ts` | `listWatchlist` 排除墓碑;`setWatchlistEnabled` → `softDeleteWatchlistEntry`;新增 `reviveWatchlistEntry` | 修改 |
| `packages/server/src/routes/signals.ts` | 加 probe / add / delete 三个路由,移除 PATCH | 修改 |

**前端**

| 文件 | 职责 | 动作 |
|---|---|---|
| `packages/client/src/components/AddSymbolModal.tsx` | 两步式添加弹窗 | 新增 |
| `packages/client/src/types.ts` | `ProbeResult` | 修改 |
| `packages/client/src/api.ts` | `probeSymbol` / `addSymbol` / `deleteSymbol`;移除 `setWatchlistEnabled` | 修改 |
| `packages/client/src/hooks/useSignals.ts` | `setEnabled` → `remove`;新增 `add` | 修改 |
| `packages/client/src/pages/SignalsPage.tsx` | 「启用」列 → 「操作」列;加「添加标的」按钮 | 修改 |

任务顺序按依赖排:先数据层(Task 1),再探测器(Task 2),再路由(Task 3),
最后前端(Task 4 类型与 api、Task 5 弹窗、Task 6 看板接线),文档收尾(Task 7)。

---

### Task 1: `watchlistStore` 改为软删除语义

**Files:**
- Modify: `packages/server/src/services/watchlistStore.ts`
- Test: `packages/server/src/services/watchlistStore.test.ts`

**Interfaces:**
- Consumes: 无新增
- Produces:
  - `softDeleteWatchlistEntry(symbol: string): void` —— 只做 `enabled = 0`
  - `reviveWatchlistEntry(symbol: string): void` —— `enabled = 1`,并清空 `status` / `last_error`
  - `listWatchlist()` 行为变更:**只返回 `enabled = 1` 的行**
  - `setWatchlistEnabled` **移除**

- [ ] **Step 1: 写失败的测试**

替换 `packages/server/src/services/watchlistStore.test.ts` 里现有的
`listScannable 排除禁用与 invalid` 那条测试(它用了将被移除的 `setWatchlistEnabled`),
并把 import 里的 `setWatchlistEnabled` 换成两个新函数:

```ts
import {
  initWatchlistTable, addWatchlistEntry, listWatchlist, listScannable,
  updateScanResult, softDeleteWatchlistEntry, reviveWatchlistEntry,
} from './watchlistStore.ts'
```

新的测试(替换掉那条旧的):

```ts
test('listScannable 排除已删除与 invalid', () => {
  addWatchlistEntry({ symbol: 'ALB', market: 'US', sourceDoc: 'd', sourceText: 't' })
  addWatchlistEntry({ symbol: 'SQM', market: 'US', sourceDoc: 'd', sourceText: 't' })
  addWatchlistEntry({ symbol: '9696.HK', market: 'HK', sourceDoc: 'd', sourceText: 't' })
  softDeleteWatchlistEntry('SQM')
  updateScanResult('9696.HK', { status: 'invalid', lastError: '代码不存在', lastScanAt: 'a' })
  assert.deepEqual(listScannable().map(e => e.symbol), ['ALB'])
})

test('listWatchlist 不返回已删除的行 —— 墓碑不该出现在界面上', () => {
  addWatchlistEntry({ symbol: 'ALB', market: 'US', sourceDoc: 'd', sourceText: 't' })
  addWatchlistEntry({ symbol: 'SQM', market: 'US', sourceDoc: 'd', sourceText: 't' })
  assert.equal(listWatchlist().length, 2)
  softDeleteWatchlistEntry('SQM')
  assert.deepEqual(listWatchlist().map(e => e.symbol), ['ALB'])
})

test('重新抽取不会把已删除的标的带回来', () => {
  // 这是「删了不会自己回来」的回归护栏。墓碑行占着主键,addWatchlistEntry 的
  // ON CONFLICT DO NOTHING 直接冲突 —— 不需要黑名单表,也不需要改抽取逻辑。
  addWatchlistEntry({ symbol: 'ALB', market: 'US', sourceDoc: 'doc_1', sourceText: 't' })
  softDeleteWatchlistEntry('ALB')
  addWatchlistEntry({ symbol: 'ALB', market: 'US', sourceDoc: 'doc_1', sourceText: 't' })
  assert.deepEqual(listWatchlist(), [])
  assert.deepEqual(listScannable(), [])
})

test('复活已删除的标的,并清掉上一次的失败状态', () => {
  addWatchlistEntry({ symbol: 'ALB', market: 'US', sourceDoc: 'd', sourceText: 't' })
  updateScanResult('ALB', { status: 'invalid', lastError: '代码不存在', lastScanAt: 'a' })
  softDeleteWatchlistEntry('ALB')
  assert.deepEqual(listWatchlist(), [])

  reviveWatchlistEntry('ALB')
  const e = listWatchlist()[0]
  assert.equal(e.symbol, 'ALB')
  assert.equal(e.enabled, 1)
  assert.equal(e.status, 'ok')        // 复活时状态归零,否则它永远进不了 listScannable
  assert.equal(e.last_error, null)
  assert.deepEqual(listScannable().map(x => x.symbol), ['ALB'])
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && pnpm test 2>&1 | tail -20`
Expected: FAIL —— `softDeleteWatchlistEntry is not a function` 之类的导出缺失错误

- [ ] **Step 3: 写实现**

在 `packages/server/src/services/watchlistStore.ts` 中:

**① 建表语句的 `enabled` 那行加注释:**

```ts
      -- enabled 现在的语义是「未删除」而不是「启用/禁用」:0 表示已被管理员删除。
      -- 保留墓碑行是刻意的 —— addWatchlistEntry 的 ON CONFLICT DO NOTHING 会因此
      -- 直接冲突,使「重新抽取」不会把已删除的标的带回来。
      enabled      INTEGER NOT NULL DEFAULT 1,
```

**② 类型定义的 `enabled` 字段加注释:**

```ts
  /** 1 = 未删除,0 = 已被管理员删除(墓碑)。不是「启用/禁用」——禁用功能已移除。 */
  enabled: 0 | 1
```

**③ `listWatchlist` 加过滤:**

```ts
export function listWatchlist(): WatchlistEntry[] {
  // 墓碑(enabled = 0)不返回 —— 界面上不该看到已删除的标的
  return db().prepare('SELECT * FROM watchlist WHERE enabled = 1 ORDER BY symbol').all() as WatchlistEntry[]
}
```

**④ 把 `setWatchlistEnabled` 整个替换成这两个函数:**

```ts
/**
 * 软删除。只打墓碑,不删行 —— 墓碑占着主键,使 addWatchlistEntry 的
 * ON CONFLICT DO NOTHING 直接冲突,「重新抽取」因此不会把它带回来。
 * 该标的的日志与事件由调用方(routes/signals.ts)另行清除。
 */
export function softDeleteWatchlistEntry(symbol: string): void {
  db().prepare('UPDATE watchlist SET enabled = 0 WHERE symbol = ?').run(symbol)
}

/**
 * 复活一个已删除的标的。状态一并归零 —— 若上次是因 invalid 被标记,
 * 不清掉的话它复活后仍会被 listScannable 排除在外,等于活不过来。
 */
export function reviveWatchlistEntry(symbol: string): void {
  db().prepare(
    "UPDATE watchlist SET enabled = 1, status = 'ok', last_error = NULL WHERE symbol = ?",
  ).run(symbol)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && pnpm test 2>&1 | tail -20`
Expected: 除 `routes/signals.ts` 相关的编译/测试失败外,`watchlistStore` 的测试全绿。
`signals.ts` 此刻仍 import 着已被移除的 `setWatchlistEnabled`,**这是预期的** ——
Task 3 会修好它。若 `pnpm test` 因此整体报错,先只跑本文件:

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && npx tsx --test src/services/watchlistStore.test.ts 2>&1 | tail -20`
Expected: PASS,4 条相关测试全绿

- [ ] **Step 5: 提交**

```bash
cd /Users/lee/myCode/ai-research-hub
git add packages/server/src/services/watchlistStore.ts packages/server/src/services/watchlistStore.test.ts
git commit -m "feat(server): watchlist 改为软删除语义,移除禁用

enabled 的语义从「启用/禁用」改为「未删除/已删除」,列名与表结构不动。
墓碑行使 addWatchlistEntry 的 ON CONFLICT DO NOTHING 天然阻止「重新抽取」
把已删标的带回来 —— 不需要黑名单表,也不需要改抽取逻辑。"
```

---

### Task 2: `probeSymbol` —— 添加前的身份探测

**Files:**
- Create: `packages/server/src/services/signals/probeSymbol.ts`
- Test: `packages/server/src/services/signals/probeSymbol.test.ts`
- Modify: `packages/server/src/services/market/yahooClient.ts`(给 `QuoteSeries` 加 `exchange`)
- Test: `packages/server/src/services/market/yahooClient.test.ts`
- Test: `packages/server/src/services/signals/scanner.test.ts`(**连带修复**,见 Step 6)

**Interfaces:**
- Consumes:
  - `normalizeSymbol(raw): { symbol, market } | null` from `../market/symbol.js`
  - `fetchDailyQuotes(symbol, deps?): Promise<QuoteSeries>` 与 `YahooError` from `../market/yahooClient.js`
  - `MIN_BARS` from `./scanner.js`
  - `listWatchlist()` from `../watchlistStore.js`
  - 注:`listWatchlist()` 现在只返回未删除的行(Task 1),所以「已删除」要另外查
- Produces:
  - `class ProbeError extends Error { kind: 'unsupported_market' | 'not_found' | 'upstream' }`
  - `interface ProbeResult { symbol; market: 'US'|'HK'; name: string|null; currency: string|null; exchange: string|null; bars: number; enough: boolean; alreadyListed: boolean; deleted: boolean }`
  - `probeSymbol(raw: string, deps?: { fetchQuotes?; findEntry? }): Promise<ProbeResult>`

**为什么要人工确认而不只是探测:** `HBM` 在 Yahoo 上是加拿大铜矿 Hudbay Minerals,
探测会成功、扫描也会成功 —— 代码合法、公司不对。404 兜底抓不住这类错,
只有把公司全名摆到管理员面前才能。

- [ ] **Step 1: 写失败的测试**

创建 `packages/server/src/services/signals/probeSymbol.test.ts`:

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { probeSymbol, ProbeError } from './probeSymbol.ts'
import { YahooError, type QuoteSeries } from '../market/yahooClient.ts'
import type { WatchlistEntry } from '../watchlistStore.ts'

function series(symbol: string, n: number, over: Partial<QuoteSeries> = {}): QuoteSeries {
  const bars = Array.from({ length: n }, (_, i) => ({
    date: `2026-01-${String((i % 28) + 1).padStart(2, '0')}`, high: 101, low: 99, close: 100,
  }))
  return { symbol, name: 'Rocket Lab Corporation', currency: 'USD', bars, rawClose: {}, ...over }
}

const noEntry = () => null

test('正常探测:归一化 + 公司身份 + 历史根数', async () => {
  const r = await probeSymbol(' nasdaq: rklb ', {
    fetchQuotes: async () => series('RKLB', 1218),
    findEntry: noEntry,
  })
  assert.equal(r.symbol, 'RKLB')
  assert.equal(r.market, 'US')
  assert.equal(r.name, 'Rocket Lab Corporation')
  assert.equal(r.currency, 'USD')
  assert.equal(r.bars, 1218)
  assert.equal(r.enough, true)
  assert.equal(r.alreadyListed, false)
  assert.equal(r.deleted, false)
})

test('港股代码归一化到 4 位', async () => {
  const r = await probeSymbol('700.HK', {
    fetchQuotes: async (s) => series(s, 1218, { name: 'Tencent Holdings Limited', currency: 'HKD' }),
    findEntry: noEntry,
  })
  assert.equal(r.symbol, '0700.HK')
  assert.equal(r.market, 'HK')
})

test('历史不足 120 根时 enough 为 false,但不算错误', async () => {
  // SPCX(SpaceX)2026-06-12 才上市,当前仅 49 根。应该允许添加,只是暂时不出信号。
  const r = await probeSymbol('NASDAQ: SPCX', {
    fetchQuotes: async () => series('SPCX', 49, { name: 'Space Exploration Technologies Corp.' }),
    findEntry: noEntry,
  })
  assert.equal(r.bars, 49)
  assert.equal(r.enough, false)
})

test('不支持的市场直接拒绝,不打网络', async () => {
  let called = false
  await assert.rejects(
    () => probeSymbol('002466', { fetchQuotes: async () => { called = true; return series('x', 1) }, findEntry: noEntry }),
    (e: unknown) => e instanceof ProbeError && e.kind === 'unsupported_market',
  )
  assert.equal(called, false, '归一化不通过时不该再去打 Yahoo')
})

test('Yahoo 查不到时抛 not_found', async () => {
  await assert.rejects(
    () => probeSymbol('NASDAQ: NOPE', {
      fetchQuotes: async () => { throw new YahooError('NOPE: 代码不存在', 'not_found') },
      findEntry: noEntry,
    }),
    (e: unknown) => e instanceof ProbeError && e.kind === 'not_found',
  )
})

test('上游其他故障抛 upstream,与「代码不存在」区分开', async () => {
  await assert.rejects(
    () => probeSymbol('NASDAQ: RKLB', {
      fetchQuotes: async () => { throw new YahooError('被限流', 'rate_limited') },
      findEntry: noEntry,
    }),
    (e: unknown) => e instanceof ProbeError && e.kind === 'upstream',
  )
})

test('已在自选股中', async () => {
  const entry = { symbol: 'RKLB', enabled: 1 } as WatchlistEntry
  const r = await probeSymbol('NASDAQ: RKLB', {
    fetchQuotes: async () => series('RKLB', 1218),
    findEntry: () => entry,
  })
  assert.equal(r.alreadyListed, true)
  assert.equal(r.deleted, false)
})

test('曾被删除:alreadyListed 为 false,deleted 为 true', async () => {
  const entry = { symbol: 'RKLB', enabled: 0 } as WatchlistEntry
  const r = await probeSymbol('NASDAQ: RKLB', {
    fetchQuotes: async () => series('RKLB', 1218),
    findEntry: () => entry,
  })
  assert.equal(r.alreadyListed, false, '墓碑不算「已在自选股中」——它应该能被添加(复活)')
  assert.equal(r.deleted, true)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && npx tsx --test src/services/signals/probeSymbol.test.ts 2>&1 | tail -12`
Expected: FAIL —— `Cannot find module './probeSymbol.ts'`

- [ ] **Step 3: 写实现**

创建 `packages/server/src/services/signals/probeSymbol.ts`:

```ts
// packages/server/src/services/signals/probeSymbol.ts
//
// 手动添加标的前的身份探测。归一化 → 拉一次行情 → 把公司全名和历史根数交回去,
// 由管理员确认后才入库。
//
// 为什么探测完还要人工确认:`HBM` 在 Yahoo 上是加拿大铜矿 Hudbay Minerals,
// 探测会成功、扫描也会成功 —— 代码合法、公司不对。404 兜底抓不住这类错误,
// 只有把公司全名摆到管理员面前才能。
import { normalizeSymbol, type Market } from '../market/symbol.js'
import { fetchDailyQuotes, YahooError, type QuoteSeries } from '../market/yahooClient.js'
import { listWatchlist, type WatchlistEntry } from '../watchlistStore.js'
import { MIN_BARS } from './scanner.js'
import { getDb } from '../db.js'

export type ProbeErrorKind = 'unsupported_market' | 'not_found' | 'upstream'

export class ProbeError extends Error {
  constructor(message: string, readonly kind: ProbeErrorKind) {
    super(message)
    this.name = 'ProbeError'
  }
}

export interface ProbeResult {
  symbol: string
  market: Market
  name: string | null
  currency: string | null
  exchange: string | null
  /** 可用的已收盘日线根数 */
  bars: number
  /** 是否达到预热门槛。false 不阻止添加,只在界面上提示 */
  enough: boolean
  alreadyListed: boolean
  /** 曾被删除。添加即复活 */
  deleted: boolean
}

export interface ProbeDeps {
  fetchQuotes?: (symbol: string) => Promise<QuoteSeries>
  /** 按 symbol 查表(含墓碑)。listWatchlist 只返回未删除的行,所以这里要直接查 */
  findEntry?: (symbol: string) => WatchlistEntry | null
}

function defaultFindEntry(symbol: string): WatchlistEntry | null {
  return (getDb().prepare('SELECT * FROM watchlist WHERE symbol = ?').get(symbol) as WatchlistEntry) ?? null
}

export async function probeSymbol(raw: string, deps: ProbeDeps = {}): Promise<ProbeResult> {
  const fetchQuotes = deps.fetchQuotes ?? ((s: string) => fetchDailyQuotes(s))
  const findEntry = deps.findEntry ?? defaultFindEntry

  const norm = normalizeSymbol(raw)
  if (!norm) {
    throw new ProbeError('只支持美股与港股。港股请写成 0700.HK,美股请写成 NASDAQ: RKLB 或 RKLB', 'unsupported_market')
  }

  let series: QuoteSeries
  try {
    series = await fetchQuotes(norm.symbol)
  } catch (err) {
    if (err instanceof YahooError && err.kind === 'not_found') {
      throw new ProbeError(`Yahoo 上查不到 ${norm.symbol}`, 'not_found')
    }
    throw new ProbeError(err instanceof Error ? err.message : String(err), 'upstream')
  }

  const entry = findEntry(norm.symbol)
  return {
    symbol: norm.symbol,
    market: norm.market,
    name: series.name,
    currency: series.currency,
    exchange: series.exchange ?? null,
    bars: series.bars.length,
    enough: series.bars.length >= MIN_BARS,
    // 墓碑不算「已在自选股中」—— 它应该能被添加(走复活路径)
    alreadyListed: entry?.enabled === 1,
    deleted: entry?.enabled === 0,
  }
}
```

**注意:** `QuoteSeries` 当前没有 `exchange` 字段。在
`packages/server/src/services/market/yahooClient.ts` 里给它加上,并在解析处回填:

接口加一行:

```ts
export interface QuoteSeries {
  symbol: string
  name: string | null
  currency: string | null
  /** 交易所全名,如 NasdaqGS / HKSE。确认弹窗要显示它 */
  exchange: string | null
  bars: Bar[]
  rawClose: Record<string, number>
}
```

返回处加一行:

```ts
  return {
    symbol,
    name: typeof meta.longName === 'string' ? meta.longName : null,
    currency: typeof meta.currency === 'string' ? meta.currency : null,
    exchange: typeof meta.fullExchangeName === 'string' ? meta.fullExchangeName : null,
    bars,
    rawClose,
  }
```

`yahooClient.test.ts` 里的 `fakePayload` 的 `meta` 也补一行 `fullExchangeName: 'NYSE',`,
并在「解析出 UTC 日期的 OHLC,并带上名称与币种」那条测试里加一行断言:

```ts
  assert.equal(s.exchange, 'NYSE')
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && npx tsx --test src/services/signals/probeSymbol.test.ts src/services/market/yahooClient.test.ts 2>&1 | tail -12`
Expected: PASS,8 条 probe 测试 + 原有 yahooClient 测试全绿

- [ ] **Step 6: 连带修复 `scanner.test.ts`**

这个文件受前两个任务波及,有两处要改:

**① 它也 import 了 Task 1 移除的 `setWatchlistEnabled`**(第 4 行)。改成:

```ts
import { initWatchlistTable, addWatchlistEntry, listWatchlist, softDeleteWatchlistEntry } from '../watchlistStore.ts'
```

并把「禁用的票不扫描」那条测试改名改实现 —— **禁用这个概念已经不存在了**:

```ts
test('已删除的票不扫描', async () => {
  addWatchlistEntry({ symbol: 'ALB', market: 'US', sourceDoc: 'd', sourceText: 't' })
  softDeleteWatchlistEntry('ALB')
  const summary = await scanAll({ fetchQuotes: async () => { throw new Error('不该被调用') } })
  assert.deepEqual(summary, { total: 0, ok: 0, failed: 0, insufficient: 0 })
})
```

**② `fakeSeries()` 构造的 `QuoteSeries` 字面量缺 `exchange`**(本任务把它设成了必填)。
在 `return` 的对象里补一行:

```ts
  return { symbol, name: `${symbol} Inc`, currency: 'USD', exchange: 'NasdaqGS', bars, rawClose }
```

- [ ] **Step 7: 类型检查并提交**

```bash
cd /Users/lee/myCode/ai-research-hub/packages/server && pnpm typecheck
```

Expected: 只剩 `src/routes/signals.ts` 那一条 `setWatchlistEnabled` 报错(Task 3 的活),
`scanner.test.ts` 的两条错误应已消失。

```bash
cd /Users/lee/myCode/ai-research-hub
git add packages/server/src/services/signals/probeSymbol.ts packages/server/src/services/signals/probeSymbol.test.ts packages/server/src/services/market/yahooClient.ts packages/server/src/services/market/yahooClient.test.ts packages/server/src/services/signals/scanner.test.ts
git commit -m "feat(server): 添加标的前的身份探测

归一化 + 拉一次行情,把公司全名与历史根数交给管理员确认。
HBM 在 Yahoo 上是加拿大铜矿 Hudbay —— 代码合法、公司不对,
这类错 404 兜底抓不住,只有人工确认能挡。"
```

---

### Task 3: 三个路由 —— probe / add / delete

**Files:**
- Modify: `packages/server/src/routes/signals.ts`
- Test: `packages/server/src/routes/signals.test.ts`
- Test: `packages/server/src/routes/signals.auth.test.ts`
- Test: `packages/server/src/routes/signals.disabled.test.ts`

**Interfaces:**
- Consumes: `probeSymbol` / `ProbeError` from `../services/signals/probeSymbol.js`;
  `addWatchlistEntry` / `softDeleteWatchlistEntry` / `reviveWatchlistEntry` / `listWatchlist`
  from `../services/watchlistStore.js`;`replaceStates` / `replaceEvents` from `../services/signalStore.js`
- Produces:
  - `POST /signals/watchlist/probe` — body `{ code }` → `ProbeResult`
  - `POST /signals/watchlist` — body `{ symbol, market }` → `{ ok: true }`
  - `DELETE /signals/watchlist/:symbol` → `{ ok: true }`
  - `SignalsRoutesOptions` 新增可选 `probe?: typeof probeSymbol`(测试注入,避免打 Yahoo)
  - `PATCH /signals/watchlist/:symbol` **移除**

- [ ] **Step 1: 写失败的测试**

在 `packages/server/src/routes/signals.test.ts` 中,把 import 里的 `setWatchlistEnabled`
换掉,并加上新用到的:

```ts
const { initWatchlistTable, addWatchlistEntry, updateScanResult, listWatchlist, softDeleteWatchlistEntry } =
  await import('../services/watchlistStore.ts')
const { initSignalTables, replaceStates, replaceEvents, getLatestState, getLatestEvent } =
  await import('../services/signalStore.ts')
```

`freshApp()` 注册路由时补一个假 probe(默认成功,单条测试可覆盖):

```ts
async function freshApp(probe?: unknown) {
  const db = new Database(':memory:')
  initWatchlistTable(db); initSignalTables(db); initSiteSettingsTable(db); initDocumentTable(db)
  const app = fastify()
  await app.register(signalRoutes, {
    prefix: '/api',
    scan: async () => ({ total: 2, ok: 2, failed: 0, insufficient: 0 }),
    probe: (probe ?? (async () => ({
      symbol: 'RKLB', market: 'US', name: 'Rocket Lab Corporation', currency: 'USD',
      exchange: 'NasdaqGS', bars: 1218, enough: true, alreadyListed: false, deleted: false,
    }))) as never,
  })
  await app.ready()
  return app
}
```

删掉现有的 `PATCH 禁用与启用` 那条测试,换成下面这批:

```ts
test('POST /signals/watchlist/probe 返回公司身份', async () => {
  const app = await freshApp()
  const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist/probe', payload: { code: 'RKLB' } })
  assert.equal(res.statusCode, 200)
  const b = res.json()
  assert.equal(b.symbol, 'RKLB')
  assert.equal(b.name, 'Rocket Lab Corporation')
  assert.equal(b.exchange, 'NasdaqGS')
  assert.equal(b.enough, true)
})

test('probe 的 code 必须是非空字符串', async () => {
  const app = await freshApp()
  for (const payload of [{}, { code: '' }, { code: '   ' }, { code: 123 }]) {
    const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist/probe', payload })
    assert.equal(res.statusCode, 400, `payload ${JSON.stringify(payload)} 应该 400`)
  }
})

test('probe 失败时把 ProbeError 的 kind 与文案透传出去', async () => {
  const { ProbeError } = await import('../services/signals/probeSymbol.ts')
  const app = await freshApp(async () => { throw new ProbeError('只支持美股与港股', 'unsupported_market') })
  const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist/probe', payload: { code: '002466' } })
  assert.equal(res.statusCode, 400)
  assert.equal(res.json().error, 'unsupported_market')
  assert.match(res.json().message, /只支持美股与港股/)
})

test('POST /signals/watchlist 新增标的,来源为空表示手动添加', async () => {
  const app = await freshApp()
  const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist', payload: { symbol: 'RKLB', market: 'US' } })
  assert.equal(res.statusCode, 200)
  const e = listWatchlist()[0]
  assert.equal(e.symbol, 'RKLB')
  assert.equal(e.market, 'US')
  assert.equal(e.source_doc, null)
  assert.equal(e.source_text, null)
})

test('POST /signals/watchlist 复活已删除的标的', async () => {
  const app = await freshApp()
  addWatchlistEntry({ symbol: 'RKLB', market: 'US', sourceDoc: 'doc_1', sourceText: '来自研报' })
  updateScanResult('RKLB', { status: 'invalid', lastError: '旧的失败', lastScanAt: 'a' })
  softDeleteWatchlistEntry('RKLB')
  assert.deepEqual(listWatchlist(), [])

  const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist', payload: { symbol: 'RKLB', market: 'US' } })
  assert.equal(res.statusCode, 200)
  const e = listWatchlist()[0]
  assert.equal(e.symbol, 'RKLB')
  assert.equal(e.status, 'ok')
  assert.equal(e.last_error, null)
  assert.equal(e.source_doc, 'doc_1', '复活保留原来的来源,不该被抹成手动添加')
})

test('POST /signals/watchlist 重复添加返回 409', async () => {
  const app = await freshApp()
  addWatchlistEntry({ symbol: 'RKLB', market: 'US', sourceDoc: null, sourceText: null })
  const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist', payload: { symbol: 'RKLB', market: 'US' } })
  assert.equal(res.statusCode, 409)
})

test('POST /signals/watchlist 校验入参', async () => {
  const app = await freshApp()
  for (const payload of [{}, { symbol: 'RKLB' }, { symbol: '', market: 'US' }, { symbol: 'RKLB', market: 'CN' }]) {
    const res = await app.inject({ method: 'POST', url: '/api/signals/watchlist', payload })
    assert.equal(res.statusCode, 400, `payload ${JSON.stringify(payload)} 应该 400`)
  }
})

test('DELETE 软删除并清掉两个周期的日志与事件', async () => {
  const app = await freshApp(); seedALB(app.db)
  assert.ok(getLatestState('ALB', '1d'))
  assert.ok(getLatestState('ALB', '1wk'))
  assert.ok(getLatestEvent('ALB', '1d'))

  const res = await app.inject({ method: 'DELETE', url: '/api/signals/watchlist/ALB' })
  assert.equal(res.statusCode, 200)
  assert.deepEqual(listWatchlist(), [], '删除后不再出现在列表里')
  assert.equal(getLatestState('ALB', '1d'), null)
  assert.equal(getLatestState('ALB', '1wk'), null)
  assert.equal(getLatestEvent('ALB', '1d'), null)
  assert.equal(getLatestEvent('ALB', '1wk'), null)
})

test('DELETE 不存在的标的返回 404', async () => {
  const app = await freshApp()
  const res = await app.inject({ method: 'DELETE', url: '/api/signals/watchlist/NOPE' })
  assert.equal(res.statusCode, 404)
})
```

`packages/server/src/routes/signals.auth.test.ts` 里把 PATCH 换成三个新路由:

```ts
  for (const [method, url, payload] of [
    ['POST', '/api/signals/scan', undefined],
    ['POST', '/api/signals/extract', undefined],
    ['POST', '/api/signals/watchlist/probe', { code: 'RKLB' }],
    ['POST', '/api/signals/watchlist', { symbol: 'RKLB', market: 'US' }],
    ['DELETE', '/api/signals/watchlist/ALB', undefined],
  ] as const) {
    const res = await app.inject({ method, url, payload })
    assert.equal(res.statusCode, 401, `${method} ${url} 应该 401`)
  }
```

`packages/server/src/routes/signals.disabled.test.ts` 同样把 PATCH 那行换成:

```ts
    ['POST', '/api/signals/watchlist/probe', { code: 'RKLB' }],
    ['POST', '/api/signals/watchlist', { symbol: 'RKLB', market: 'US' }],
    ['DELETE', '/api/signals/watchlist/ALB', undefined],
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && pnpm test 2>&1 | tail -20`
Expected: FAIL —— `signals.ts` 仍 import 着已移除的 `setWatchlistEnabled`(Task 1 留下的),
且三个新路由不存在

- [ ] **Step 3: 写实现**

修改 `packages/server/src/routes/signals.ts`:

**① import 区:**

```ts
import {
  listWatchlist, addWatchlistEntry, softDeleteWatchlistEntry, reviveWatchlistEntry,
  type WatchlistEntry,
} from '../services/watchlistStore.js'
import {
  getLatestState, getStates, getLatestEvent, countEventsSince, getRecentEvents,
  replaceStates, replaceEvents, type Timeframe,
} from '../services/signalStore.js'
import { probeSymbol, ProbeError } from '../services/signals/probeSymbol.js'
```

**② options 加一个注入点:**

```ts
export interface SignalsRoutesOptions {
  /** 测试注入假实现,避免真的打 Yahoo */
  scan?: typeof scanAll
  /** 同上 */
  probe?: typeof probeSymbol
}
```

以及在插件体顶部:

```ts
  const probe = opts.probe ?? probeSymbol
```

**③ 把整个 `app.patch(...)` 块删掉,换成下面三个路由:**

```ts
  app.post<{ Body: { code?: unknown } }>('/signals/watchlist/probe', async (request, reply) => {
    if (!requireAdmin(request, reply)) return
    const code = (request.body ?? {}).code
    if (typeof code !== 'string' || !code.trim()) {
      return reply.status(400).send({ error: 'invalid_input', message: '代码不能为空' })
    }
    try {
      return await probe(code)
    } catch (err) {
      // ProbeError 的 kind 原样透传 —— 前端要靠它区分「市场不支持」和「查不到」
      if (err instanceof ProbeError) {
        return reply.status(400).send({ error: err.kind, message: err.message })
      }
      throw err
    }
  })

  app.post<{ Body: { symbol?: unknown; market?: unknown } }>('/signals/watchlist', async (request, reply) => {
    if (!requireAdmin(request, reply)) return
    const { symbol, market } = request.body ?? {}
    if (typeof symbol !== 'string' || !symbol.trim()) {
      return reply.status(400).send({ error: 'invalid_input', message: 'symbol 不能为空' })
    }
    if (market !== 'US' && market !== 'HK') {
      return reply.status(400).send({ error: 'invalid_input', message: 'market 只能是 US 或 HK' })
    }
    // 直接查表(含墓碑)—— listWatchlist 只返回未删除的行,判不出「曾被删除」
    const existing = getWatchlistRow(symbol)
    if (existing?.enabled === 1) {
      return reply.status(409).send({ error: 'already_listed', message: `${symbol} 已在自选股中` })
    }
    if (existing) {
      // 复活。保留原来的 source_doc / source_text —— 它当初确实来自那篇研报
      reviveWatchlistEntry(symbol)
    } else {
      // 手动添加没有来源报告,source_doc / source_text 留空
      addWatchlistEntry({ symbol, market, sourceDoc: null, sourceText: null })
    }
    return { ok: true }
  })

  app.delete<{ Params: { symbol: string } }>('/signals/watchlist/:symbol', async (request, reply) => {
    if (!requireAdmin(request, reply)) return
    const { symbol } = request.params
    if (!listWatchlist().some(e => e.symbol === symbol)) {
      return reply.status(404).send({ error: 'not_found' })
    }
    softDeleteWatchlistEntry(symbol)
    // 连日志一起清 —— 一只票约 1200 行,留着是再也不会更新、也没人看的死数据
    for (const tf of TIMEFRAMES) {
      replaceStates(symbol, tf, [])
      replaceEvents(symbol, tf, [])
    }
    return { ok: true }
  })
```

**④ 在文件里的 helper 区(`rowOf` 附近)加一个直查函数:**

```ts
/** 按 symbol 直查一行,**含墓碑** —— listWatchlist 只返回未删除的行 */
function getWatchlistRow(symbol: string): WatchlistEntry | null {
  return (getDb().prepare('SELECT * FROM watchlist WHERE symbol = ?').get(symbol) as WatchlistEntry) ?? null
}
```

并在 import 区补 `import { getDb } from '../services/db.js'`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/server && pnpm test 2>&1 | tail -20`
Expected: PASS,全套服务端测试全绿

- [ ] **Step 5: 类型检查并提交**

```bash
cd /Users/lee/myCode/ai-research-hub/packages/server && pnpm typecheck
cd /Users/lee/myCode/ai-research-hub
git add packages/server/src/routes/signals.ts packages/server/src/routes/signals.test.ts packages/server/src/routes/signals.auth.test.ts packages/server/src/routes/signals.disabled.test.ts
git commit -m "feat(server): 手动添加标的的三个路由,移除禁用

probe 探测身份、POST 添加(已删除的走复活)、DELETE 软删除并清日志。
复活时保留原有 source_doc —— 它当初确实来自那篇研报,不该被抹成手动添加。"
```

---

### Task 4: 前端类型、api 与 hook

**Files:**
- Modify: `packages/client/src/types.ts`
- Modify: `packages/client/src/api.ts`
- Modify: `packages/client/src/hooks/useSignals.ts`
- Test: `packages/client/src/hooks/useSignals.test.ts`

**Interfaces:**
- Consumes: 上面三个路由
- Produces:
  - `interface ProbeResult { symbol; market: 'US'|'HK'; name: string|null; currency: string|null; exchange: string|null; bars: number; enough: boolean; alreadyListed: boolean; deleted: boolean }`
  - `api.probeSymbol(code: string): Promise<ProbeResult>`
  - `api.addSymbol(symbol: string, market: 'US'|'HK'): Promise<void>`
  - `api.deleteSymbol(symbol: string): Promise<void>`
  - `api.setWatchlistEnabled` **移除**
  - `useSignals()` 的 `setEnabled` **改名** `remove(symbol)`;新增 `add(symbol, market)`

- [ ] **Step 1: 加类型**

在 `packages/client/src/types.ts` 末尾追加:

```ts
export interface ProbeResult {
  symbol: string
  market: 'US' | 'HK'
  name: string | null
  currency: string | null
  exchange: string | null
  /** 可用的已收盘日线根数 */
  bars: number
  /** 是否达到预热门槛。false 不阻止添加,只提示 */
  enough: boolean
  alreadyListed: boolean
  /** 曾被删除。添加即复活 */
  deleted: boolean
}
```

- [ ] **Step 2: 写失败的测试**

在 `packages/client/src/hooks/useSignals.test.ts` 末尾追加:

```ts
test('add 成功后刷新并自增 version', async () => {
  let added = false
  stubFetch((url, init) => {
    if (url === '/api/signals/watchlist' && init?.method === 'POST') { added = true; return { body: { ok: true } } }
    return { body: { rows: added ? [ALB, { ...ALB, symbol: 'RKLB' }] : [ALB] } }
  })
  const { result } = renderHook(() => useSignals())
  await waitFor(() => expect(result.current.loading).toBe(false))
  const v0 = result.current.version

  await act(async () => { await result.current.add('RKLB', 'US') })
  await waitFor(() => expect(result.current.rows).toHaveLength(2))
  expect(result.current.version).toBe(v0 + 1)
})

test('remove 成功后刷新并自增 version', async () => {
  let removed = false
  stubFetch((url, init) => {
    if (init?.method === 'DELETE') { removed = true; return { body: { ok: true } } }
    return { body: { rows: removed ? [] : [ALB] } }
  })
  const { result } = renderHook(() => useSignals())
  await waitFor(() => expect(result.current.loading).toBe(false))
  const v0 = result.current.version

  await act(async () => { await result.current.remove('ALB') })
  await waitFor(() => expect(result.current.rows).toHaveLength(0))
  expect(result.current.version).toBe(v0 + 1)
})

test('add 失败时把服务端文案放进 error,且不动 rows', async () => {
  stubFetch((url, init) => {
    if (url === '/api/signals/watchlist' && init?.method === 'POST') {
      return { ok: false, body: { error: 'already_listed', message: 'RKLB 已在自选股中' } }
    }
    return { body: { rows: [ALB] } }
  })
  const { result } = renderHook(() => useSignals())
  await waitFor(() => expect(result.current.loading).toBe(false))

  await act(async () => { await result.current.add('RKLB', 'US') })
  expect(result.current.error).toBe('RKLB 已在自选股中')
  expect(result.current.rows).toHaveLength(1)
})
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm test 2>&1 | tail -12`
Expected: FAIL —— `result.current.add is not a function`

- [ ] **Step 4: 改 api**

在 `packages/client/src/api.ts` 中,**删掉** `setWatchlistEnabled`,追加三个方法:

```ts
  async probeSymbol(code: string): Promise<import('./types').ProbeResult> {
    const r = await fetch('/api/signals/watchlist/probe', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ code }),
    })
    if (!r.ok) throw new Error((await r.json().catch(() => ({}))).message ?? '查询失败')
    return r.json()
  },
  async addSymbol(symbol: string, market: 'US' | 'HK'): Promise<void> {
    const r = await fetch('/api/signals/watchlist', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ symbol, market }),
    })
    if (!r.ok) throw new Error((await r.json().catch(() => ({}))).message ?? '添加失败')
  },
  async deleteSymbol(symbol: string): Promise<void> {
    const r = await fetch(`/api/signals/watchlist/${encodeURIComponent(symbol)}`, { method: 'DELETE' })
    if (!r.ok) throw new Error((await r.json().catch(() => ({}))).message ?? '删除失败')
  },
```

- [ ] **Step 5: 改 hook**

在 `packages/client/src/hooks/useSignals.ts` 中,把 `setEnabled` 整个替换成:

```ts
  const add = useCallback(async (symbol: string, market: 'US' | 'HK') => {
    setError(null)
    try {
      await api.addSymbol(symbol, market)
      await refresh()
      setVersion(v => v + 1)
      return true
    } catch (err) {
      setError(err instanceof Error ? err.message : '添加失败')
      return false
    }
  }, [refresh])

  const remove = useCallback(async (symbol: string) => {
    setError(null)
    try {
      await api.deleteSymbol(symbol)
      await refresh()
      setVersion(v => v + 1)
      return true
    } catch (err) {
      setError(err instanceof Error ? err.message : '删除失败')
      return false
    }
  }, [refresh])
```

返回值那行相应改成:

```ts
  return { rows, loading, scanning, error, scan, extract, refresh, version, lastScan, add, remove }
```

顶部那段解释 `version` 的注释里,把 `setEnabled` 改成 `add/remove`。

- [ ] **Step 6: 运行测试确认通过**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm test 2>&1 | tail -12`
Expected: FAIL —— `SignalsPage.tsx` 还在用 `setEnabled`。**这是预期的**,Task 6 会修好。
先只跑 hook 的测试:

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm exec vitest run src/hooks/useSignals.test.ts 2>&1 | tail -12`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
cd /Users/lee/myCode/ai-research-hub
git add packages/client/src/types.ts packages/client/src/api.ts packages/client/src/hooks/useSignals.ts packages/client/src/hooks/useSignals.test.ts
git commit -m "feat(client): 手动添加/删除标的的 api 与 hook

setEnabled 改名 remove,新增 add。两者与 scan/extract 一样成功后自增 version ——
横幅与展开行日志靠它重新拉取,增删标的必须触发,否则横幅上会留着已删标的的事件。"
```

---

### Task 5: `AddSymbolModal` —— 两步式添加弹窗

**Files:**
- Create: `packages/client/src/components/AddSymbolModal.tsx`
- Test: `packages/client/src/components/AddSymbolModal.test.tsx`

**Interfaces:**
- Consumes: `api.probeSymbol`;`ProbeResult` from `../types`
- Produces:
  - `function AddSymbolModal(props: { open: boolean; onCancel: () => void; onConfirm: (symbol: string, market: 'US' | 'HK') => Promise<boolean> }): JSX.Element`

**交互:** 输入代码 → 点「查询」→ 展示公司全名 / 交易所 / 币种 / 历史根数 → 「确认添加」。
**查询成功前「确认添加」禁用** —— 这是整个功能的要害,不能让人跳过确认直接添加。

- [ ] **Step 1: 写失败的测试**

创建 `packages/client/src/components/AddSymbolModal.test.tsx`:

```tsx
import { test, expect, vi, afterEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { AddSymbolModal } from './AddSymbolModal'

const RKLB = {
  symbol: 'RKLB', market: 'US', name: 'Rocket Lab Corporation', currency: 'USD',
  exchange: 'NasdaqGS', bars: 1218, enough: true, alreadyListed: false, deleted: false,
}

function stubProbe(impl: (code: string) => { ok?: boolean; body: unknown }) {
  vi.stubGlobal('fetch', vi.fn(async (_url: string, init?: RequestInit) => {
    const { ok = true, body } = impl(JSON.parse(String(init?.body ?? '{}')).code)
    return { ok, json: async () => body } as Response
  }))
}

afterEach(() => { vi.unstubAllGlobals() })

const noop = async () => true

// 仓库没装 @testing-library/jest-dom,没有 toBeDisabled 这类 matcher ——
// antd 禁用时渲染的就是原生 <button disabled>,直接读属性即可。
const confirmBtn = () => screen.getByRole('button', { name: /确认添加/ }) as HTMLButtonElement

test('查询成功前「确认添加」是禁用的', async () => {
  stubProbe(() => ({ body: RKLB }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)
  expect(confirmBtn().disabled).toBe(true)
})

test('查询后展示公司身份,确认按钮解禁', async () => {
  stubProbe(() => ({ body: RKLB }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)

  await userEvent.type(screen.getByLabelText('代码'), 'RKLB')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))

  await waitFor(() => expect(screen.getByText('Rocket Lab Corporation')).toBeTruthy())
  expect(screen.getByText(/NasdaqGS/)).toBeTruthy()
  expect(screen.getByText(/1218/)).toBeTruthy()
  expect(confirmBtn().disabled).toBe(false)
})

test('历史不足时提示但仍可添加', async () => {
  // SPCX 2026-06-12 才上市,只有 49 根。应该能加,只是暂时不出信号。
  stubProbe(() => ({ body: { ...RKLB, symbol: 'SPCX', name: 'Space Exploration Technologies Corp.', bars: 49, enough: false } }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)

  await userEvent.type(screen.getByLabelText('代码'), 'SPCX')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))

  await waitFor(() => expect(screen.getByText(/历史仅 49 根/)).toBeTruthy())
  expect(confirmBtn().disabled).toBe(false)
})

test('已在自选股中时不能添加', async () => {
  stubProbe(() => ({ body: { ...RKLB, alreadyListed: true } }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)

  await userEvent.type(screen.getByLabelText('代码'), 'RKLB')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))

  await waitFor(() => expect(screen.getByText(/已在自选股中/)).toBeTruthy())
  expect(confirmBtn().disabled).toBe(true)
})

test('曾被删除的提示将恢复', async () => {
  stubProbe(() => ({ body: { ...RKLB, deleted: true } }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)

  await userEvent.type(screen.getByLabelText('代码'), 'RKLB')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))

  await waitFor(() => expect(screen.getByText(/将恢复该标的/)).toBeTruthy())
  expect(confirmBtn().disabled).toBe(false)
})

test('查询失败显示服务端文案,确认仍禁用', async () => {
  stubProbe(() => ({ ok: false, body: { error: 'unsupported_market', message: '只支持美股与港股' } }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)

  await userEvent.type(screen.getByLabelText('代码'), '002466')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))

  await waitFor(() => expect(screen.getByText(/只支持美股与港股/)).toBeTruthy())
  expect(confirmBtn().disabled).toBe(true)
})

test('改动代码后要重新查询 —— 旧的探测结果作废', async () => {
  // 否则会出现「查的是 RKLB、加进去的是别的代码」这种最坏情况
  stubProbe(() => ({ body: RKLB }))
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={noop} />)

  const input = screen.getByLabelText('代码')
  await userEvent.type(input, 'RKLB')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))
  await waitFor(() => expect(confirmBtn().disabled).toBe(false))

  await userEvent.type(input, 'X')
  expect(confirmBtn().disabled).toBe(true)
  expect(screen.queryByText('Rocket Lab Corporation')).toBeNull()
})

test('确认添加把探测到的 symbol 与 market 交回去', async () => {
  stubProbe(() => ({ body: RKLB }))
  const onConfirm = vi.fn(async () => true)
  render(<AddSymbolModal open onCancel={() => {}} onConfirm={onConfirm} />)

  await userEvent.type(screen.getByLabelText('代码'), ' nasdaq: rklb ')
  await userEvent.click(screen.getByRole('button', { name: '查询' }))
  await waitFor(() => expect(confirmBtn().disabled).toBe(false))
  await userEvent.click(screen.getByRole('button', { name: /确认添加/ }))

  // 交回去的是**探测结果里归一化后的** symbol,不是用户输入的原始串
  expect(onConfirm).toHaveBeenCalledWith('RKLB', 'US')
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm exec vitest run src/components/AddSymbolModal.test.tsx 2>&1 | tail -12`
Expected: FAIL —— 找不到 `./AddSymbolModal`

- [ ] **Step 3: 写组件**

创建 `packages/client/src/components/AddSymbolModal.tsx`:

```tsx
import { useState } from 'react'
import { Alert, Button, Descriptions, Input, Modal, Space, Typography } from 'antd'
import { api } from '../api'
import type { ProbeResult } from '../types'

interface Props {
  open: boolean
  onCancel: () => void
  /** 返回 true 表示添加成功,弹窗自行关闭并复位 */
  onConfirm: (symbol: string, market: 'US' | 'HK') => Promise<boolean>
}

/**
 * 两步式:先查询确认公司身份,再添加。
 *
 * 为什么不能一步到位:`HBM` 在 Yahoo 上是加拿大铜矿 Hudbay Minerals ——
 * 代码合法、探测成功、扫描也正常,只是公司不对。这类错只有把公司全名
 * 摆到人眼前才挡得住,所以查询成功前「确认添加」必须是禁用的。
 */
export function AddSymbolModal({ open, onCancel, onConfirm }: Props) {
  const [code, setCode] = useState('')
  const [probing, setProbing] = useState(false)
  const [adding, setAdding] = useState(false)
  const [result, setResult] = useState<ProbeResult | null>(null)
  const [error, setError] = useState<string | null>(null)

  const reset = () => { setCode(''); setResult(null); setError(null) }

  // 代码一改,上一次的探测结果立刻作废 —— 否则会出现
  // 「查的是 RKLB、加进去的是别的代码」这种最坏情况
  const onCodeChange = (v: string) => { setCode(v); setResult(null); setError(null) }

  const doProbe = async () => {
    setProbing(true); setError(null); setResult(null)
    try {
      setResult(await api.probeSymbol(code))
    } catch (err) {
      setError(err instanceof Error ? err.message : '查询失败')
    } finally { setProbing(false) }
  }

  const doConfirm = async () => {
    if (!result) return
    setAdding(true)
    try {
      if (await onConfirm(result.symbol, result.market)) { reset(); onCancel() }
    } finally { setAdding(false) }
  }

  const canAdd = !!result && !result.alreadyListed

  return (
    <Modal
      open={open}
      title="添加标的"
      onCancel={() => { reset(); onCancel() }}
      footer={[
        <Button key="cancel" onClick={() => { reset(); onCancel() }} autoInsertSpace={false}>取消</Button>,
        <Button key="ok" type="primary" disabled={!canAdd} loading={adding} onClick={() => void doConfirm()}>
          确认添加
        </Button>,
      ]}
    >
      <Space direction="vertical" style={{ width: '100%' }} size="middle">
        <Space.Compact style={{ width: '100%' }}>
          <Input
            aria-label="代码"
            placeholder="如 RKLB、NASDAQ: RKLB、0700.HK"
            value={code}
            onChange={e => onCodeChange(e.target.value)}
            onPressEnter={() => void doProbe()}
          />
          <Button onClick={() => void doProbe()} loading={probing} disabled={!code.trim()} autoInsertSpace={false}>
            查询
          </Button>
        </Space.Compact>

        {error && <Alert type="error" title={error} showIcon />}

        {result && (
          <>
            <Descriptions size="small" column={1} bordered>
              <Descriptions.Item label="公司">
                <Typography.Text strong>{result.name ?? '(未知)'}</Typography.Text>
              </Descriptions.Item>
              <Descriptions.Item label="代码">
                {result.symbol} · {result.exchange ?? (result.market === 'US' ? '美股' : '港股')} · {result.currency ?? ''}
              </Descriptions.Item>
              <Descriptions.Item label="历史">{result.bars} 根日线</Descriptions.Item>
            </Descriptions>

            {result.alreadyListed && <Alert type="warning" title={`${result.symbol} 已在自选股中`} showIcon />}
            {result.deleted && <Alert type="info" title="该标的此前被删除,添加将恢复它(历史需重新扫描)" showIcon />}
            {!result.enough && (
              <Alert
                type="warning"
                showIcon
                title={`历史仅 ${result.bars} 根,不足 120 根`}
                description="可以添加,但暂时不会出信号 —— SuperTrend 预热不足时给出的方向是错的。"
              />
            )}
          </>
        )}
      </Space>
    </Modal>
  )
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm exec vitest run src/components/AddSymbolModal.test.tsx 2>&1 | tail -12`
Expected: PASS,8 条测试全绿

- [ ] **Step 5: 提交**

```bash
cd /Users/lee/myCode/ai-research-hub
git add packages/client/src/components/AddSymbolModal.tsx packages/client/src/components/AddSymbolModal.test.tsx
git commit -m "feat(client): 两步式添加标的弹窗

查询成功前「确认添加」禁用;改动代码即作废上一次探测结果 ——
否则会出现「查的是 RKLB、加进去的是别的代码」。"
```

---

### Task 6: 看板接线 —— 「操作」列与「添加标的」按钮

**Files:**
- Modify: `packages/client/src/pages/SignalsPage.tsx`
- Test: `packages/client/src/pages/SignalsPage.test.tsx`

**Interfaces:**
- Consumes: `useSignals()` 的 `add` / `remove`;`AddSymbolModal` from `../components/AddSymbolModal`
- Produces: 无(页面本身)

**注意:** 该文件此刻有一处**未提交的改动** —— 「来源」列已被收窄到
`width: 100, ellipsis: true`(上一轮的需求)。**不要动它**,那不是本任务的内容。

- [ ] **Step 1: 写失败的测试**

`packages/client/src/pages/SignalsPage.test.tsx` **已经有两个 stub 助手**:
`stubRows`(默认场景,`/auth/me` 落进默认分支等于未登录)与 `stubRowsAsAdmin`
(`/auth/me` 返回 `isAdmin: true`)。**直接用它们,不要 `vi.mock` useAuth** ——
既有写法测的是真 hook,而且两种身份能待在同一个文件里。

`stubRowsAsAdmin` 目前不处理 `DELETE`,给它加一个分支(其余不动):

```tsx
function stubRowsAsAdmin(rows: SignalRow[], onDelete?: () => void) {
  vi.stubGlobal('fetch', vi.fn(async (url: string, init?: RequestInit) => {
    if (init?.method === 'DELETE') { onDelete?.(); return { ok: true, json: async () => ({ ok: true }) } as Response }
    const body = url.includes('/auth/me')
      ? { id: 'u1', username: 'admin', avatarUrl: null, messageCount: 0, limit: 100, unlimited: false, isAdmin: true, remaining: 100 }
      : url.includes('/events') ? { events: [] }
      : url.includes('/log') ? { states: [] }
      : { rows }
    return { ok: true, json: async () => body } as Response
  }))
}
```

把现有测「启用」开关的那条测试删掉,追加这四条:

```tsx
test('非管理员看不到操作列与添加按钮', async () => {
  stubRows([ALB])
  renderPage()
  await waitFor(() => expect(screen.getByText('ALB')).toBeTruthy())
  expect(screen.queryByRole('button', { name: /添加标的/ })).toBeNull()
  expect(screen.queryByRole('button', { name: /删除/ })).toBeNull()
})

test('管理员看得到「添加标的」与操作列的删除', async () => {
  stubRowsAsAdmin([ALB])
  renderPage()
  await waitFor(() => expect(screen.getByRole('button', { name: /添加标的/ })).toBeTruthy())
  expect(screen.getByRole('button', { name: /删除/ })).toBeTruthy()
})

test('删除要二次确认,确认后才发请求', async () => {
  let deleted = false
  stubRowsAsAdmin([ALB], () => { deleted = true })
  renderPage()
  await waitFor(() => expect(screen.getByRole('button', { name: /删除/ })).toBeTruthy())

  await userEvent.click(screen.getByRole('button', { name: /删除/ }))
  expect(deleted).toBe(false)   // 只是弹出确认,还没删

  await waitFor(() => expect(screen.getByText(/确定删除/)).toBeTruthy())
  await userEvent.click(screen.getByRole('button', { name: '确定' }))
  await waitFor(() => expect(deleted).toBe(true))
})

test('点「添加标的」弹出弹窗', async () => {
  stubRowsAsAdmin([ALB])
  renderPage()
  await waitFor(() => expect(screen.getByRole('button', { name: /添加标的/ })).toBeTruthy())
  await userEvent.click(screen.getByRole('button', { name: /添加标的/ }))
  await waitFor(() => expect(screen.getByLabelText('代码')).toBeTruthy())
})
```

文件顶部若尚未 import `userEvent`,补上
`import userEvent from '@testing-library/user-event'`。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm test 2>&1 | tail -12`
Expected: FAIL —— 页面还在用 `setEnabled`(Task 4 已移除),且没有添加按钮/操作列

- [ ] **Step 3: 改页面**

在 `packages/client/src/pages/SignalsPage.tsx` 中:

**① import 区:** 去掉 `Switch`,加 `Popconfirm` 与弹窗、`useState`:

```tsx
import { useState } from 'react'
import { Alert, Button, Popconfirm, Space, Table, Tag, Typography } from 'antd'
import { PlusOutlined, ReloadOutlined, ThunderboltOutlined } from '@ant-design/icons'
import { AddSymbolModal } from '../components/AddSymbolModal'
```

**② hook 解构:** `setEnabled` 换成 `add, remove`:

```tsx
  const { rows, loading, scanning, error, scan, extract, version, lastScan, add, remove } = useSignals()
  const [addOpen, setAddOpen] = useState(false)
```

**③ 把整个 `enabledColumn` 换成 `actionColumn`:**

```tsx
  const actionColumn: ColumnsType<SignalRow>[number] = {
    title: '操作', key: 'action', width: 72,
    render: (_: unknown, r: SignalRow) => (
      <Popconfirm
        title={`确定删除 ${r.symbol}？`}
        description="该标的的历史日志会一并清除,恢复后需要重新扫描。"
        okText="确定"
        cancelText="取消"
        onConfirm={() => void remove(r.symbol)}
      >
        <Button type="link" size="small" danger autoInsertSpace={false}>删除</Button>
      </Popconfirm>
    ),
  }

  // 只有 admin 看得到「操作」列。删除是管理动作,普通访客只读。
  const columns: ColumnsType<SignalRow> = isAdmin ? [...baseColumns, actionColumn] : baseColumns
```

**④ 管理员按钮区加「添加标的」**(放在「重新抽取」之前):

```tsx
            <Button
              icon={<PlusOutlined aria-hidden />}
              onClick={() => setAddOpen(true)}
              autoInsertSpace={false}
            >
              添加标的
            </Button>
```

**⑤ 在 `<Table ... />` 之后挂上弹窗:**

```tsx
      {isAdmin && (
        <AddSymbolModal
          open={addOpen}
          onCancel={() => setAddOpen(false)}
          onConfirm={add}
        />
      )}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm test 2>&1 | tail -12`
Expected: PASS,前端全套绿

- [ ] **Step 5: 类型检查并提交**

```bash
cd /Users/lee/myCode/ai-research-hub/packages/client && pnpm exec tsc --noEmit
cd /Users/lee/myCode/ai-research-hub
git add packages/client/src/pages/SignalsPage.tsx packages/client/src/pages/SignalsPage.test.tsx
git commit -m "feat(client): 看板加「添加标的」,启用开关换成删除

删除带二次确认并说明日志会一并清除。管理员场景复用既有的 stubRowsAsAdmin
(靠 /auth/me 返回 isAdmin),不 mock useAuth —— 测的是真 hook。"
```

---

### Task 7: README 文档

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 更新「股票信号追踪」一节**

在 `## 股票信号追踪` 一节里,找到讲抽取规则的那条要点,在它之后插入:

```markdown
- **抽不到的可以手动加。** 研报标题里写成裸代码的(如 `### 4.2 Rocket Lab（RKLB）`)
  抽取器不认 —— 接受裸字母会把同篇里的 `### 3.5 ETF`、`### AI 研究偏见自查` 当成
  股票代码。管理员可在 `/signals` 点「添加标的」手动补上:输入代码 → 查询 →
  **确认公司身份**后才入库。
  这一步的确认不是多余的:`HBM` 在 Yahoo 上是加拿大铜矿 Hudbay Minerals,
  代码合法、扫描也正常,只是公司不对 —— 这类错只有把公司全名摆在眼前才挡得住
- **删除是软删除。** 删掉的标的会连历史日志一起清除,并留下一条墓碑记录,
  使它**不会被下一次「重新抽取」带回来**。想找回来就再手动添加一次(历史需重新扫描)
- 历史不足 120 根的新股**可以添加**,只是暂时不出信号。例如 SpaceX(`SPCX`)
  2026-06-12 才上市,约需到 2026 年 12 月才够预热
```

同时把该节里提到「启用/禁用」的措辞改掉 —— 禁用功能已不存在,操作栏只有删除。

- [ ] **Step 2: 提交**

```bash
cd /Users/lee/myCode/ai-research-hub
git add README.md
git commit -m "docs: 手动添加自选股说明"
```

---

## 完工检查

```bash
pnpm test && pnpm typecheck
```

Expected: 服务端与前端测试全绿,两个包类型检查都无输出。

**另需浏览器验收**(jsdom 不算布局,且 antd `Popconfirm` / `Modal` 的真实渲染值得一看):

1. 起本地双服务,用管理员身份打开 `/signals`
2. 点「添加标的」→ 输入 `RKLB` → 查询 → 应显示 **Rocket Lab Corporation · NasdaqGS · USD · 1218 根** → 确认添加 → 表格多一行
3. 输入 `HBM` → 查询 → 应显示 **Hudbay Minerals Inc.** —— 确认这个"陷阱"能被人眼当场识破
4. 输入 `SPCX` → 查询 → 应提示历史仅 49 根、不足 120 根,但确认按钮仍可点
5. 输入 `002466` → 查询 → 应报「只支持美股与港股」
6. 删除刚加的 `RKLB` → 二次确认 → 消失;再点「重新抽取」→ **它不应该回来**
7. 确认表格上再没有「启用」开关列
