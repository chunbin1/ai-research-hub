# 研报站 H5 移动端 + Tailwind 迁移 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让研报站在手机浏览器上可用,同时把 `packages/client` 的样式方案从 CSS Modules 全量切到 Tailwind CSS v4,桌面端视觉保持不变。

**Architecture:** 单组件树 + Tailwind 断点(唯一分界 `md` = 768px,mobile-first)。布局差异全部交给 utility class;只有 3 处**行为**差异落到 JS,走一个 `useIsMobile()` hook。阅读页移动端 = 正文常驻 + 目录左抽屉 + 底部问答面板。react-markdown 生成的原生标签挂不上 class,因此报告正文排版保留后代选择器 CSS,放进 `@layer components`。

**Tech Stack:** React 19 · Vite 6 · react-router 7 · Tailwind CSS v4.3.3(`@tailwindcss/vite` 插件模式)· Vitest 4 + happy-dom + @testing-library/react 16

**Spec:** `docs/superpowers/specs/2026-08-01-h5-tailwind-design.md`

## Global Constraints

- **断点只有一个**:`md` = 768px。写法一律 mobile-first(无前缀 = 手机,`md:` = 桌面)。**不得引入第二个断点**。
- **桌面端像素级保持原样**:色值、间距、栏宽、圆角、hover 态照抄现有 CSS。唯一例外见「已知偏差」。
- 栏宽常量:目录 `240px`,问答栏 `360px`。**用任意值语法**(`md:w-[240px]`、`md:right-[360px]`、`md:grid-cols-[240px_minmax(0,1fr)_360px]`),不进 `@theme` 的 `--spacing-*` 命名空间 —— 这是对 spec 4.2 的有意偏差,理由是两个常量只出现在 4 处,任意值语法零风险且更易读。
- 面板/抽屉高度一律用 **`dvh` 不用 `vh`**(iOS 键盘弹起时 `vh` 不变会遮住发送按钮)。
- **像素级 = 逐值核对,不许就近取整**。Tailwind 的间距刻度是 4px 的倍数,原 CSS 里的
  9px / 7px / 3px / 13px 这类值**不在刻度上**,必须用任意值语法(`py-[9px]`),
  不得写成最接近的 `py-2.5`(10px)。迁移每个组件时,把删掉的 `.module.css` 逐条对着
  新 class 走一遍,任何对不上的取值都要报出来由人决定,不得自行判定「可接受」。
- **`text-sm` / `text-xs` 这类字号 utility 会连带设 `line-height`,原 CSS 往往没有。**
  凡是原规则只写了 `font-size: 14px` 而没写 `line-height` 的地方,用 `text-[14px]`,
  **不要**用 `text-sm`(它会附带 `line-height: 1.25rem`,与原先继承来的 1.5 不同)。
  原规则同时写了 `line-height` 的(如 `.assistant` 的 1.7),用 `text-sm leading-[1.7]` 无妨。
- 过渡时长照抄现有 `.24s` → `duration-[240ms]`;所有带过渡的元素必须配 `motion-reduce:transition-none`。
- 每个任务结束时 `pnpm --filter @arh/client exec tsc --noEmit` 必须通过。
- 后端 `packages/server` **零改动**。
- 提交信息用中文,格式 `<type>(<scope>): <描述>`,与仓库现有历史一致(如 `feat(reader): 问答栏支持一键收起/展开`)。

## 已知偏差(唯一一处允许的桌面视觉变化)

`TracesPage/TraceList/TraceStats/SpanWaterfall/TraceDetailPage` 这 5 个 `.module.css` 引用了 `var(--purple)`、`var(--gray-50/100/200/400/600/900)`、`var(--radius)`、`var(--radius-sm)`,但这些变量**全仓库从未定义**(`index.css` 只有 `color-scheme: light`)—— 从 docmind 移植时漏带了全局变量表。这些声明当前是 invalid at computed-value time,实际效果 = 卡片没底色、圆角为 0、文字颜色继承。

Task 7 会把它们映射到 Tailwind 内置 `gray-*` 色阶与真实圆角值,**这会让 `/traces` 两个页面的观感发生可见变化(变成作者本来想要的样子)**。这是修既有缺陷,不是重设计。

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `packages/client/package.json` | 依赖 | 加 tailwindcss / @tailwindcss/vite / @testing-library/react / @testing-library/dom |
| `packages/client/vite.config.ts` | 构建 | 加 tailwind 插件 |
| `packages/client/index.html` | 文档壳 | viewport 加 `viewport-fit=cover` |
| `packages/client/src/index.css` | 全局样式唯一入口 | `@import` + `@theme` + `@layer base` + 报告正文 `@layer components` |
| `packages/client/src/hooks/useIsMobile.ts` | 视口判断(唯一 JS 断点来源) | 新建 |
| `packages/client/src/test/matchMedia.ts` | 测试用 matchMedia 桩 | 新建 |
| `packages/client/src/components/ChatPanel.tsx` | 问答面板内部 | 迁移,高度改 `h-full` |
| `packages/client/src/pages/ReaderPage.tsx` | 阅读页布局 + 抽屉/面板状态 | 重构 |
| `packages/client/src/pages/HomePage.tsx` | 报告流 | 迁移 + 删除按钮 bug 修复 |
| `packages/client/src/components/Trace*.tsx`、`SpanWaterfall.tsx` | trace 可观测页 | 迁移 + 宽内容包 `overflow-x-auto` |
| `packages/client/src/pages/Eval*.tsx` | 评估后台 | 迁移 + 宽表格包 `overflow-x-auto` |
| 8 个 `*.module.css` | — | **全部删除** |

---

### Task 1: 落定基线(提交已有的骨架屏改动)

工作区里 `HomePage.tsx` / `HomePage.module.css` 有未提交的骨架屏改动,**这个功能要保留**。迁移必须从干净的工作区开始,否则后面分不清哪些 diff 是迁移带来的。

`deploy.sh` 的改动与本次无关,**不要碰**。

**Files:**
- Commit: `packages/client/src/pages/HomePage.tsx`、`packages/client/src/pages/HomePage.module.css`

- [ ] **Step 1: 确认待提交内容只有骨架屏**

Run: `git diff --stat packages/client`
Expected: 只有 `HomePage.tsx`(+13/-3)和 `HomePage.module.css`(+6)两个文件。

- [ ] **Step 2: 确认基线可用**

Run: `pnpm test && pnpm typecheck`
Expected: 全部 PASS。

- [ ] **Step 3: 提交**

```bash
git add packages/client/src/pages/HomePage.tsx packages/client/src/pages/HomePage.module.css
git commit -m "feat(home): 报告列表加载态骨架屏"
```

---

### Task 2: 接入 Tailwind v4 + 主题变量 + 报告正文排版层

装 Tailwind 并写好全局样式基座。本任务结束时 `.module.css` 一个都没删,页面外观应当**几乎不变**(preflight 会重置一些标签,由 `@layer base` 和报告排版层兜住)。

**Files:**
- Modify: `packages/client/package.json`
- Modify: `packages/client/vite.config.ts`
- Modify: `packages/client/index.html:5`
- Modify: `packages/client/src/index.css`(整体重写)

**Interfaces:**
- Produces: CSS 变量 `--color-paper` `--color-surface` `--color-gold` `--color-gold-ink` `--color-gold-wash` `--color-gold-edge` `--color-danger`,对应 utility `bg-paper` `bg-surface` `text-gold` `border-gold` `text-gold-ink` `bg-gold-wash` `border-gold-edge` `text-danger`;全局 class `report-body`(报告正文容器)与 `chat-md`(问答气泡内 markdown 容器)。

- [ ] **Step 1: 装依赖**

```bash
pnpm --filter @arh/client add -D tailwindcss@^4.3.3 @tailwindcss/vite@^4.3.3
```

- [ ] **Step 2: 接插件**

`packages/client/vite.config.ts` 改为:

```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    port: 5173,
    proxy: { '/api': { target: 'http://localhost:3001', changeOrigin: true } },
  },
  test: { environment: 'happy-dom' },
})
```

- [ ] **Step 3: viewport meta 加 `viewport-fit=cover`**

`packages/client/index.html` 第 5 行改为:

```html
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
```

- [ ] **Step 4: 重写 `src/index.css`**

关键点:Tailwind preflight 会把 `ul/ol` 的 `list-style` 重置为 `none`、把 `h1~h6` 的 `font-size`/`font-weight` 重置为 `inherit`、把 `a` 的颜色重置为 `inherit`。react-markdown 输出的正文全靠浏览器默认值,**不显式恢复的话报告会丢掉项目符号、标题层级和链接色**。下面的 `report-body` 层已经把这些补回来了,不要删。

```css
@import "tailwindcss";

@theme {
  --color-paper: #fafaf8;       /* body 底 */
  --color-surface: #fbfbf9;     /* 侧栏 / 问答栏 / 移动顶栏 */
  --color-gold: #c9a227;        /* 引用条、来源 chip 描边 */
  --color-gold-ink: #8a6d1b;    /* chip / 额度文字 */
  --color-gold-wash: #faf8f0;   /* chip / 额度底 */
  --color-gold-edge: #e7d9a8;   /* 额度 pill 描边 */
  --color-danger: #b00020;      /* 错误文字 / 删除按钮 */
}

@layer base {
  :root { color-scheme: light; }
  body {
    font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
    color: #1a1a1a;
    background: var(--color-paper);
  }
}

@layer components {
  /* 报告正文:react-markdown 输出原生标签,挂不上 utility class,只能用后代选择器。
     其中 list-style / 标题字号字重 / 链接色是为了抵消 Tailwind preflight 的重置,
     取值 = 迁移前浏览器默认值,删掉会导致正文丢失项目符号和标题层级。 */
  .report-body { line-height: 1.75; }
  .report-body h1 { font-size: 26px; font-weight: 700; margin: 0 0 20px; line-height: 1.35; }
  .report-body h2 { font-size: 24px; font-weight: 700; margin-top: 32px; margin-bottom: 12px;
                    border-bottom: 1px solid #eee; padding-bottom: 6px; }
  .report-body h3 { font-size: 18.72px; font-weight: 700; margin-top: 24px; margin-bottom: 10px; }
  .report-body h4 { font-size: 16px; font-weight: 700; margin: 16px 0; }
  .report-body h5 { font-size: 13.28px; font-weight: 700; margin: 16px 0; }
  .report-body h6 { font-size: 10.72px; font-weight: 700; margin: 16px 0; }
  .report-body p { margin: 12px 0; }
  .report-body a { color: #0969da; text-decoration: underline; }
  .report-body ul { list-style: disc; padding-left: 22px; }
  .report-body ol { list-style: decimal; padding-left: 22px; }
  .report-body li { margin: 4px 0; }
  .report-body table { border-collapse: collapse; width: 100%; margin: 16px 0; font-size: 14px;
                       display: block; overflow-x: auto; }
  .report-body th, .report-body td { border: 1px solid #ddd; padding: 7px 10px; text-align: left; }
  .report-body th { background: #f5f5f2; }
  .report-body blockquote { border-left: 3px solid var(--color-gold); margin: 16px 0; padding: 4px 16px;
                            color: #555; background: var(--color-gold-wash); }
  .report-body pre { background: #f6f6f4; padding: 14px; border-radius: 8px; overflow-x: auto;
                     font-size: 13px; line-height: 1.5; }
  .report-body pre code { white-space: pre; font-family: "SF Mono", Menlo, Consolas, monospace; }
  .report-body hr { border: none; border-top: 1px solid #eee; margin: 28px 0; }
  .report-body :target { background: #fff6d6; transition: background 1.2s; }
  .report-body .flash { animation: flash 1.6s ease; }

  /* 问答气泡里的 assistant markdown,同样是原生标签 */
  .chat-md p { margin: 6px 0; }
  .chat-md ul { list-style: disc; padding-left: 20px; margin: 6px 0; }
  .chat-md ol { list-style: decimal; padding-left: 20px; margin: 6px 0; }
  .chat-md li { margin: 2px 0; }
  .chat-md strong { font-weight: 600; }
  .chat-md code { background: #f0f0ed; border-radius: 4px; padding: 1px 4px; font-size: 13px; }
}

@keyframes flash {
  0% { background: #fff2b8; }
  100% { background: transparent; }
}
```

- [ ] **Step 5: 验证 Tailwind 真的接上了**

```bash
pnpm build:client && grep -c "tw-" packages/client/dist/assets/*.css
```
Expected: 构建成功;`grep` 返回 ≥ 1(Tailwind v4 会往产物里写 `@property --tw-*` 之类的注册变量)。若返回 0 或报 "no such file",说明插件没生效,先解决再往下走。

- [ ] **Step 6: 验证主题变量生成了 utility**

```bash
grep -o "bg-surface\|--color-gold-wash" packages/client/dist/assets/*.css | head
```
Expected: 至少匹配到 `--color-gold-wash`(`@theme` 变量会输出到 `:root`)。`bg-surface` 此刻还没人用,不出现是正常的。

- [ ] **Step 7: 类型检查 + 测试**

Run: `pnpm typecheck && pnpm test`
Expected: 全部 PASS。

- [ ] **Step 8: 提交**

```bash
git add packages/client/package.json packages/client/vite.config.ts packages/client/index.html packages/client/src/index.css ../../pnpm-lock.yaml
git commit -m "build(client): 接入 Tailwind v4 + 主题变量与报告正文排版层"
```

---

### Task 3: `useIsMobile` hook(TDD)

全站唯一的 JS 断点来源。三处行为差异(面板初值、点来源收面板、抽屉锁滚动)都依赖它。

**Files:**
- Create: `packages/client/src/hooks/useIsMobile.ts`
- Create: `packages/client/src/hooks/useIsMobile.test.ts`
- Create: `packages/client/src/test/matchMedia.ts`
- Modify: `packages/client/package.json`

**Interfaces:**
- Produces:
  - `useIsMobile(): boolean` —— 视口窄于 768px 时为 `true`
  - `stubMatchMedia(initialDesktopMatches: boolean): { setDesktop(v: boolean): void; listenerCount: number }` —— 测试桩,`initialDesktopMatches=true` 表示桌面

- [ ] **Step 1: 装测试依赖**

`@testing-library/react` 16 不再把 `@testing-library/dom` 打包进去,必须显式装。

```bash
pnpm --filter @arh/client add -D @testing-library/react@^16.3.2 @testing-library/dom@^10.4.1
```

- [ ] **Step 2: 写 matchMedia 测试桩**

happy-dom 的 `matchMedia` 无法在测试里可控地触发 `change`,所以自己造一个桩。放在共享位置,Task 5 也要用。

`packages/client/src/test/matchMedia.ts`:

```ts
import { vi } from 'vitest'

interface MatchMediaStub {
  /** 切换到桌面(true)/移动(false),并派发 change 事件 */
  setDesktop(isDesktop: boolean): void
  /** 当前注册的监听器数量,用来验证卸载时解绑 */
  readonly listenerCount: number
}

/**
 * 用可控的桩替换全局 matchMedia。
 * @param initialDesktopMatches true = 初始为桌面(min-width:768px 命中)
 */
export function stubMatchMedia(initialDesktopMatches: boolean): MatchMediaStub {
  const listeners = new Set<(e: MediaQueryListEvent) => void>()
  let matches = initialDesktopMatches

  vi.stubGlobal('matchMedia', (media: string) => ({
    media,
    get matches() { return matches },
    addEventListener: (_type: string, cb: (e: MediaQueryListEvent) => void) => { listeners.add(cb) },
    removeEventListener: (_type: string, cb: (e: MediaQueryListEvent) => void) => { listeners.delete(cb) },
  }))

  return {
    setDesktop(isDesktop: boolean) {
      matches = isDesktop
      listeners.forEach(cb => cb({ matches: isDesktop } as MediaQueryListEvent))
    },
    get listenerCount() { return listeners.size },
  }
}
```

- [ ] **Step 3: 写失败的测试**

`packages/client/src/hooks/useIsMobile.test.ts`:

```ts
import { describe, it, expect, afterEach, vi } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { stubMatchMedia } from '../test/matchMedia'
import { useIsMobile } from './useIsMobile'

afterEach(() => { vi.unstubAllGlobals() })

describe('useIsMobile', () => {
  it('桌面视口下为 false', () => {
    stubMatchMedia(true)
    const { result } = renderHook(() => useIsMobile())
    expect(result.current).toBe(false)
  })

  it('移动视口下为 true', () => {
    stubMatchMedia(false)
    const { result } = renderHook(() => useIsMobile())
    expect(result.current).toBe(true)
  })

  it('跨断点时跟随 change 事件翻转', () => {
    const mq = stubMatchMedia(true)
    const { result } = renderHook(() => useIsMobile())
    expect(result.current).toBe(false)

    act(() => { mq.setDesktop(false) })
    expect(result.current).toBe(true)

    act(() => { mq.setDesktop(true) })
    expect(result.current).toBe(false)
  })

  it('卸载时解绑监听器', () => {
    const mq = stubMatchMedia(true)
    const { unmount } = renderHook(() => useIsMobile())
    expect(mq.listenerCount).toBe(1)
    unmount()
    expect(mq.listenerCount).toBe(0)
  })
})
```

- [ ] **Step 4: 跑测试确认失败**

Run: `pnpm --filter @arh/client exec vitest run src/hooks/useIsMobile.test.ts`
Expected: FAIL,报 `Failed to resolve import "./useIsMobile"`。

- [ ] **Step 5: 实现**

`packages/client/src/hooks/useIsMobile.ts`:

```ts
import { useEffect, useState } from 'react'

/** 全站唯一断点,与 Tailwind 默认的 md 保持一致。 */
const DESKTOP_QUERY = '(min-width: 768px)'

/**
 * 视口是否窄于 768px。
 * 只用于**行为**差异(面板初值 / 点来源是否收面板 / 是否锁 body 滚动);
 * 纯布局差异一律交给 Tailwind 的 md: 前缀,不要用这个 hook 去条件渲染布局。
 */
export function useIsMobile(): boolean {
  const [isMobile, setIsMobile] = useState(() => !window.matchMedia(DESKTOP_QUERY).matches)

  useEffect(() => {
    const mql = window.matchMedia(DESKTOP_QUERY)
    const onChange = (e: MediaQueryListEvent): void => { setIsMobile(!e.matches) }
    mql.addEventListener('change', onChange)
    // 挂载与首次渲染之间视口可能已变化,补一次同步
    setIsMobile(!mql.matches)
    return () => { mql.removeEventListener('change', onChange) }
  }, [])

  return isMobile
}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `pnpm --filter @arh/client exec vitest run src/hooks/useIsMobile.test.ts`
Expected: PASS,4 passed。

- [ ] **Step 7: 全量验证**

Run: `pnpm typecheck && pnpm test`
Expected: 全部 PASS。

- [ ] **Step 8: 提交**

```bash
git add packages/client/package.json packages/client/src/hooks/useIsMobile.ts packages/client/src/hooks/useIsMobile.test.ts packages/client/src/test/matchMedia.ts ../../pnpm-lock.yaml
git commit -m "feat(client): 加 useIsMobile hook 与 matchMedia 测试桩"
```

---

### Task 4: ChatPanel 迁到 Tailwind

`ReaderPage` 依赖它,所以先做。两处结构性改动:高度从 `100vh` 改成 `h-full`(由父级定高),宽度从固定 `360px` 改成 `w-full md:w-[360px]`。

**Files:**
- Modify: `packages/client/src/components/ChatPanel.tsx`
- Delete: `packages/client/src/components/ChatPanel.module.css`

**Interfaces:**
- Consumes: `.chat-md`(Task 2 定义)
- Produces: `ChatPanel` 根元素类名含 `flex h-full w-full flex-none flex-col md:w-[360px]` —— Task 5 的 `<aside>` 依赖这个宽度行为

- [ ] **Step 1: 改写组件**

`packages/client/src/components/ChatPanel.tsx` 整体替换为:

```tsx
import { useState } from 'react'
import { useDocChat } from '../hooks/useDocChat'
import { useAuth } from '../hooks/useAuth'
import Markdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

export default function ChatPanel({ docId, onCite }: { docId: string; onCite: (slug: string) => void }) {
  const { messages, send, streaming } = useDocChat(docId)
  const { user, login } = useAuth()
  const [input, setInput] = useState('')

  function submit(e: React.FormEvent) {
    e.preventDefault()
    const text = input.trim()
    if (!text) return
    setInput('')
    void send(text).then(() => window.dispatchEvent(new Event('auth:refresh')))
  }

  const outOfQuota = user && !user.unlimited && (user.remaining ?? 0) <= 0

  return (
    // h-full:高度由父级 aside 决定(移动 70dvh / 桌面撑满 grid 行)
    // w-full md:w-[360px] flex-none:桌面收起时父级列宽压到 0,靠固定宽度 + 不压缩保持面板整体滑出
    <div className="flex h-full w-full flex-none flex-col md:w-[360px]">
      <div className="flex flex-none items-center justify-between border-b border-[#eee] p-4 font-semibold">
        问这篇报告
        {user && !user.unlimited && (
          <span className="rounded-full border border-gold-edge bg-gold-wash px-2 py-0.5 text-[12px] text-gold-ink">
            剩余 {user.remaining}
          </span>
        )}
      </div>

      <div className="flex flex-1 flex-col gap-3.5 overflow-y-auto p-4">
        {messages.length === 0 && (
          <p className="text-[13px] text-[#999]">就当前报告提问,答案会标注来源章节。</p>
        )}
        {messages.map((m, i) => (
          <div
            key={i}
            className={
              m.role === 'user'
                ? 'max-w-[85%] self-end rounded-[12px_12px_2px_12px] bg-[#1a1a1a] px-3 py-2 text-[14px] text-white'
                : 'chat-md max-w-full self-start text-sm leading-[1.7]'
            }
          >
            {m.role === 'assistant'
              ? <Markdown remarkPlugins={[remarkGfm]}>{m.content || '…'}</Markdown>
              : m.content}
            {m.sources && m.sources.length > 0 && (
              <div className="mt-2 flex flex-wrap gap-1.5">
                {m.sources.map((s, j) => (
                  <button
                    key={j}
                    className="cursor-pointer rounded-full border border-gold bg-gold-wash px-2.5 py-[3px] text-[12px] text-gold-ink hover:bg-[#f3ead0]"
                    onClick={() => onCite(s.section_slug)}
                  >
                    来源 §{s.section_title || '引言'}
                  </button>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>

      {user ? (
        <form
          className="flex flex-none gap-2 border-t border-[#eee] p-3 pb-[max(0.75rem,env(safe-area-inset-bottom))] md:pb-3"
          onSubmit={submit}
        >
          <input
            className="flex-1 rounded-lg border border-[#ddd] px-3 py-[9px] text-[14px]"
            value={input}
            onChange={e => setInput(e.target.value)}
            placeholder={outOfQuota ? '次数已用完' : streaming ? '生成中…' : '输入问题,回车发送'}
            disabled={streaming || !!outOfQuota}
          />
          <button
            type="submit"
            className="cursor-pointer rounded-lg bg-[#1a1a1a] px-4 py-[9px] text-white disabled:cursor-default disabled:opacity-50"
            disabled={streaming || !!outOfQuota || !input.trim()}
          >
            发送
          </button>
        </form>
      ) : (
        <div className="flex flex-none flex-col items-center gap-2.5 border-t border-[#eee] px-4 py-5 text-[14px] text-[#666]">
          <span>登录后即可就本篇报告提问</span>
          <button
            className="cursor-pointer rounded-lg bg-[#1a1a1a] px-4 py-[9px] text-white"
            onClick={login}
          >
            GitHub 登录
          </button>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 删掉 CSS Module**

```bash
rm packages/client/src/components/ChatPanel.module.css
```

- [ ] **Step 3: 确认没有残留引用**

Run: `grep -rn "ChatPanel.module.css" packages/client/src`
Expected: 无输出。

- [ ] **Step 4: 类型检查 + 构建**

Run: `pnpm typecheck && pnpm build:client`
Expected: 全部成功。

- [ ] **Step 5: 提交**

```bash
git add -A packages/client/src/components
git commit -m "refactor(chat): ChatPanel 迁到 Tailwind,高度改由父级决定"
```

---

### Task 5: ReaderPage 移动端重构(TDD)

本计划的核心。桌面三栏保持像素级不变,移动端变成「移动顶栏 + 目录抽屉 + 正文 + 底部问答面板」。

**布局要点(实现时别改动这几条,它们互相咬合):**
- 底部 toggle 条在**正常流**里,是 flex 列的最后一个子元素,恒定贴在视口底部。
- 问答面板 `fixed bottom-11`(44px = toggle 条高度),正好压在 toggle 条上方,两者不重叠 —— 所以面板展开时 toggle 条仍可点,能收起。
- toggle 条给 `z-40`,面板 `z-30`:面板收起时 `translate-y-full` 会让它盖住 toggle 条区域,靠 z 序压住。
- toggle 按钮始终在 `<aside>` **外面**,这样面板 `inert` 时按钮仍可交互(现有结构已如此)。

**Files:**
- Modify: `packages/client/src/pages/ReaderPage.tsx`(整体重写)
- Delete: `packages/client/src/pages/ReaderPage.module.css`
- Create: `packages/client/src/pages/ReaderPage.test.tsx`

**Interfaces:**
- Consumes: `useIsMobile()`(Task 3);`ChatPanel` 的 `{ docId, onCite }` props(Task 4);`report-body` class(Task 2);`extractToc(markdown)` 返回 `{ title, slug, level }[]`;`api.getDocument(id)` 返回 `{ document: { filename: string }, markdown: string }`
- Produces: DOM 契约 —— 正文滚动容器 `id="report-content"`;问答面板 `id="chat-panel"`;目录抽屉 `id="toc-drawer"`;toggle 按钮带 `aria-controls="chat-panel"` + `aria-expanded={chatOpen}`

- [ ] **Step 1: 写失败的测试**

`packages/client/src/pages/ReaderPage.test.tsx`:

```tsx
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import { MemoryRouter, Routes, Route } from 'react-router-dom'
import { stubMatchMedia } from '../test/matchMedia'
import ReaderPage from './ReaderPage'

// ChatPanel 真身依赖 SSE / fetch,这里换成只暴露一个「来源」按钮的替身,
// 测的是 ReaderPage 自己的溯源回链行为,不是 ChatPanel 内部。
vi.mock('../components/ChatPanel', () => ({
  default: ({ onCite }: { onCite: (slug: string) => void }) => (
    <button onClick={() => onCite('第一节')}>来源 §第一节</button>
  ),
}))

vi.mock('../api', () => ({
  api: {
    getDocument: vi.fn().mockResolvedValue({
      document: { filename: '某公司深度报告.md' },
      markdown: '# 标题\n\n## 第一节\n\n正文内容',
    }),
  },
}))

function renderReader() {
  return render(
    <MemoryRouter initialEntries={['/reports/doc-1']}>
      <Routes>
        <Route path="/reports/:id" element={<ReaderPage />} />
      </Routes>
    </MemoryRouter>,
  )
}

/** toggle 按钮的展开态:靠 aria-controls 定位,不依赖文案 */
function chatToggle(): HTMLElement {
  const btn = document.querySelector('button[aria-controls="chat-panel"]')
  if (!btn) throw new Error('找不到问答栏 toggle 按钮')
  return btn as HTMLElement
}
function chatExpanded(): string | null {
  return chatToggle().getAttribute('aria-expanded')
}

beforeEach(() => {
  localStorage.clear()
  // happy-dom 没实现元素级 scrollTo,补一个 spy
  Element.prototype.scrollTo = vi.fn()
})

afterEach(() => {
  vi.unstubAllGlobals()
  document.body.style.overflow = ''
})

describe('ReaderPage — 问答栏初始状态', () => {
  it('桌面端默认展开', async () => {
    stubMatchMedia(true)
    renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('true'))
  })

  it('桌面端沿用 localStorage 的收起状态', async () => {
    localStorage.setItem('reader.chatOpen', '0')
    stubMatchMedia(true)
    renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('false'))
  })

  it('移动端恒为收起,且忽略 localStorage', async () => {
    localStorage.setItem('reader.chatOpen', '1')
    stubMatchMedia(false)
    renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('false'))
  })
})

describe('ReaderPage — 点来源 chip 的溯源回链', () => {
  it('移动端:先收起问答面板,再滚动到章节', async () => {
    stubMatchMedia(false)
    const { getByText } = renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('false'))

    // 先展开面板
    chatToggle().click()
    await waitFor(() => expect(chatExpanded()).toBe('true'))

    getByText('来源 §第一节').click()

    await waitFor(() => expect(chatExpanded()).toBe('false'))
    expect(Element.prototype.scrollTo).toHaveBeenCalled()
  })

  it('移动端:收面板不写 localStorage', async () => {
    stubMatchMedia(false)
    const { getByText } = renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('false'))

    chatToggle().click()
    getByText('来源 §第一节').click()

    await waitFor(() => expect(chatExpanded()).toBe('false'))
    expect(localStorage.getItem('reader.chatOpen')).toBeNull()
  })

  it('桌面端:点来源后面板保持展开', async () => {
    stubMatchMedia(true)
    const { getByText } = renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('true'))

    getByText('来源 §第一节').click()

    expect(chatExpanded()).toBe('true')
    expect(Element.prototype.scrollTo).toHaveBeenCalled()
  })

  it('桌面端:手动收起会写进 localStorage', async () => {
    stubMatchMedia(true)
    renderReader()
    await waitFor(() => expect(chatExpanded()).toBe('true'))

    chatToggle().click()

    await waitFor(() => expect(localStorage.getItem('reader.chatOpen')).toBe('0'))
  })
})

describe('ReaderPage — 目录抽屉', () => {
  it('移动端打开抽屉时锁住 body 滚动,关闭后解锁', async () => {
    stubMatchMedia(false)
    renderReader()
    const menu = await screen.findByLabelText('打开目录')

    menu.click()
    await waitFor(() => expect(document.body.style.overflow).toBe('hidden'))

    const mask = document.querySelector('[data-testid="toc-mask"]')
    expect(mask).toBeTruthy()
    ;(mask as HTMLElement).click()
    await waitFor(() => expect(document.body.style.overflow).toBe(''))
  })
})
```

断言全部用原生 `getAttribute` 比较,**不要**改成 `toHaveAttribute` —— 那是 `@testing-library/jest-dom` 的匹配器,本项目没装也不需要装。

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @arh/client exec vitest run src/pages/ReaderPage.test.tsx`
Expected: FAIL —— 当前 `ReaderPage` 的 toggle 按钮没有稳定的 `aria-controls` 定位、移动端逻辑不存在,多条用例报错。

- [ ] **Step 3: 重写 ReaderPage**

`packages/client/src/pages/ReaderPage.tsx` 整体替换为:

```tsx
import { useEffect, useState } from 'react'
import { useParams, Link } from 'react-router-dom'
import { api } from '../api'
import { extractToc } from '../lib/toc'
import { useIsMobile } from '../hooks/useIsMobile'
import ReportMarkdown from '../components/ReportMarkdown'
import ChatPanel from '../components/ChatPanel'

const CHAT_OPEN_KEY = 'reader.chatOpen'

export default function ReaderPage() {
  const { id = '' } = useParams()
  const isMobile = useIsMobile()
  const [markdown, setMarkdown] = useState('')
  const [filename, setFilename] = useState('')
  const [error, setError] = useState('')
  const [tocOpen, setTocOpen] = useState(false)
  // 桌面:沿用 localStorage 记忆,默认展开。
  // 手机:恒为收起且不写 localStorage——否则一进阅读页就被 70dvh 面板盖掉大半正文。
  // 跨断点缩放窗口时不重置该值:它在两侧语义一致(问答是否可见),重置会让面板莫名开合。
  const [chatOpen, setChatOpen] = useState(() =>
    isMobile ? false : localStorage.getItem(CHAT_OPEN_KEY) !== '0',
  )

  useEffect(() => {
    api.getDocument(id)
      .then(({ document: doc, markdown }) => {
        setMarkdown(markdown)
        setFilename(doc.filename)
        document.title = `${doc.filename} — 研报站`
      })
      .catch(e => setError(String(e.message)))
  }, [id])

  // 抽屉打开时锁住背景滚动,否则正文会跟着抽屉一起滑
  useEffect(() => {
    if (!tocOpen || !isMobile) return
    document.body.style.overflow = 'hidden'
    return () => { document.body.style.overflow = '' }
  }, [tocOpen, isMobile])

  const toc = extractToc(markdown)

  // 溯源回链:滚动到章节标题并临时高亮。
  // 注:显式滚动 #report-content 容器,而非 el.scrollIntoView()——后者对
  // 嵌套滚动容器 + smooth 存在浏览器兼容问题,常常不生效。
  function jump(slug: string) {
    // 手机上问答面板占 70dvh,不收起的话答案会盖着要跳过去的正文。
    // 面板是 fixed 定位、不占布局流,所以不影响下面的 scrollTop 几何,无需等动画帧。
    if (isMobile) {
      setChatOpen(false)
      setTocOpen(false)
    }
    const el = document.getElementById(slug)
    const container = document.getElementById('report-content')
    if (!el || !container) return
    const top = container.scrollTop + (el.getBoundingClientRect().top - container.getBoundingClientRect().top) - 16
    // 用 'auto' 而非 'smooth':部分环境(reduced-motion / 自动化浏览器)会静默忽略
    // smooth 滚动导致完全不跳转;瞬时跳转 + flash 高亮反而更稳、反馈更即时。
    container.scrollTo({ top, behavior: 'auto' })
    el.classList.remove('flash')
    void el.offsetWidth // 强制回流,便于重复点击重新触发动画
    el.classList.add('flash')
    window.setTimeout(() => el.classList.remove('flash'), 1600)
  }

  function toggleChat() {
    setChatOpen(open => {
      const next = !open
      if (!isMobile) localStorage.setItem(CHAT_OPEN_KEY, next ? '1' : '0')
      return next
    })
  }

  if (error) {
    return (
      <div className="p-10">
        <Link to="/" className="mb-4 block text-[14px] text-[#555] hover:text-black">← 全部报告</Link>
        <p>{error}</p>
      </div>
    )
  }

  return (
    <div
      className={`relative flex h-dvh flex-col md:grid md:h-screen md:transition-[grid-template-columns] md:duration-[240ms] md:ease-[ease] motion-reduce:md:transition-none ${
        chatOpen
          ? 'md:grid-cols-[240px_minmax(0,1fr)_360px]'
          : 'md:grid-cols-[240px_minmax(0,1fr)_0px]'
      }`}
    >
      {/* 移动顶栏:桌面不渲染,桌面的「← 全部报告」在目录栏顶部 */}
      <header className="flex h-12 flex-none items-center gap-3 border-b border-[#eee] bg-surface px-3 md:hidden">
        <button
          type="button"
          aria-label="打开目录"
          aria-expanded={tocOpen}
          aria-controls="toc-drawer"
          onClick={() => setTocOpen(true)}
          className="flex h-9 w-9 flex-none items-center justify-center rounded-lg text-lg text-[#555]"
        >
          ☰
        </button>
        <span className="truncate text-sm font-medium">{filename}</span>
      </header>

      {/* 抽屉遮罩:仅移动端 */}
      {tocOpen && (
        <div
          data-testid="toc-mask"
          className="fixed inset-0 z-20 bg-black/40 md:hidden"
          onClick={() => setTocOpen(false)}
        />
      )}

      <aside
        id="toc-drawer"
        className={`fixed inset-y-0 left-0 z-30 w-70 overflow-y-auto border-r border-[#eee] bg-surface px-3 py-5 transition-transform duration-[240ms] motion-reduce:transition-none md:static md:z-auto md:w-[240px] md:translate-x-0 md:transition-none ${
          tocOpen ? 'translate-x-0' : '-translate-x-full'
        }`}
      >
        <Link to="/" className="mb-4 block text-[14px] text-[#555] hover:text-black">← 全部报告</Link>
        <nav>
          {toc.map((t, i) => (
            <a
              key={i}
              className="flex min-h-11 cursor-pointer items-center rounded-md px-2 py-[5px] text-[13px] leading-[1.4] text-[#444] hover:bg-[#eee] hover:text-black md:block md:min-h-0"
              style={{ paddingLeft: 8 + (t.level - 1) * 12 }}
              onClick={() => jump(t.slug)}
            >
              {t.title}
            </a>
          ))}
        </nav>
      </aside>

      <main id="report-content" className="report-body flex-1 overflow-y-auto px-5 py-6 md:px-14 md:py-10">
        <ReportMarkdown markdown={markdown} />
      </main>

      {/*
        toggle 按钮:移动端是流内的底部整条(z-40 压住收起态滑出屏外的面板);
        桌面端是贴在问答栏左沿的 18×60 竖条。始终在 <aside> 外面,
        这样面板 inert 时按钮依然可点。
      */}
      <button
        type="button"
        onClick={toggleChat}
        aria-expanded={chatOpen}
        aria-controls="chat-panel"
        title={chatOpen ? '收起问答栏' : '展开问答栏'}
        className={`relative z-40 flex h-11 w-full flex-none items-center justify-center border-t border-[#eee] bg-white text-sm text-[#555] pb-[env(safe-area-inset-bottom)] md:absolute md:top-1/2 md:z-5 md:h-15 md:w-[18px] md:-translate-y-1/2 md:rounded-l-lg md:border md:border-r-0 md:border-[#eee] md:bg-white md:p-0 md:text-[13px] md:text-[#999] md:transition-[right,color,background] md:duration-[240ms] md:hover:bg-[#f3f3f0] md:hover:text-black motion-reduce:md:transition-none ${
          chatOpen ? 'md:right-[360px]' : 'md:right-0'
        }`}
      >
        <span className="md:hidden">{chatOpen ? '收起问答 ▽' : '问这篇报告 △'}</span>
        <span className="hidden md:inline">{chatOpen ? '›' : '‹'}</span>
      </button>

      {/*
        问答面板:移动端 fixed 在 toggle 条(h-11 = 44px)正上方,两者不重叠;
        桌面端回到 grid 第三列,收起时列宽压到 0、面板被 overflow-hidden 裁掉。
        高度用 dvh 不用 vh:iOS 键盘弹起时 vh 不变会遮住发送按钮。
      */}
      <aside
        id="chat-panel"
        inert={!chatOpen}
        className={`fixed inset-x-0 bottom-11 z-30 flex h-[70dvh] flex-col overflow-hidden border-t border-[#eee] bg-surface transition-transform duration-[240ms] motion-reduce:transition-none md:static md:z-auto md:h-auto md:translate-y-0 md:border-t-0 md:transition-none ${
          chatOpen ? 'translate-y-0' : 'translate-y-full'
        } ${chatOpen ? 'md:border-l md:border-l-[#eee]' : ''}`}
      >
        <ChatPanel docId={id} onCite={jump} />
      </aside>
    </div>
  )
}
```

- [ ] **Step 4: 删掉 CSS Module**

```bash
rm packages/client/src/pages/ReaderPage.module.css
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm --filter @arh/client exec vitest run src/pages/ReaderPage.test.tsx`
Expected: PASS,8 passed。

- [ ] **Step 6: 确认没有残留引用 + 全量验证**

```bash
grep -rn "ReaderPage.module.css" packages/client/src
pnpm typecheck && pnpm test && pnpm build:client
```
Expected: `grep` 无输出;其余全部成功。

- [ ] **Step 7: 提交**

```bash
git add -A packages/client/src/pages
git commit -m "feat(reader): 阅读页支持 H5(目录抽屉 + 底部问答面板),迁到 Tailwind"
```

---

### Task 6: HomePage 迁移 + 删除按钮触屏 bug 修复

`.del { opacity: 0 }` 只靠 `.card:hover` 显形,触摸屏没有 hover,**管理员在手机上永远删不掉报告**。这次一并修掉。

**Files:**
- Modify: `packages/client/src/pages/HomePage.tsx`
- Delete: `packages/client/src/pages/HomePage.module.css`

- [ ] **Step 1: 整体重写 HomePage.tsx**

第 1–34 行(import 到 `onDelete` 结束)只删掉 `import styles from './HomePage.module.css'` 这一行,其余逻辑一字不改。第 36–83 行的 `return` 替换为下面内容。注意功能元素与现状严格一致:`🔍 trace`、`📊 评估`、上传按钮**都是 `isAdmin` 才渲染**,上传排在用户区**之前**,登出按钮文案是「登出」,卡片 meta 是「日期 + `chunk_count` 段」。

```tsx
  return (
    <div className="mx-auto max-w-[1100px] px-4 py-5 md:px-6 md:py-8">
      <header className="mb-7 flex flex-wrap items-center justify-between gap-3">
        <h1 className="m-0 text-2xl tracking-[2px]">研报站</h1>
        <div className="flex flex-wrap items-center gap-3.5">
          {isAdmin && (
            <Link to="/traces" className="rounded-lg border border-[#e0e0e0] px-3 py-2 text-[14px] text-[#555] hover:bg-[#f3f3f0] hover:text-black">
              🔍 trace
            </Link>
          )}
          {isAdmin && (
            <Link to="/eval" className="rounded-lg border border-[#e0e0e0] px-3 py-2 text-[14px] text-[#555] hover:bg-[#f3f3f0] hover:text-black">
              📊 评估
            </Link>
          )}
          {isAdmin && (
            <label className="cursor-pointer rounded-lg bg-[#1a1a1a] px-4 py-[9px] text-[14px] text-white">
              {uploading ? '上传中…' : '+ 上传研报 (.md)'}
              <input ref={fileRef} type="file" accept=".md,.markdown,.txt" onChange={onFile} hidden />
            </label>
          )}
          {user ? (
            <span className="flex items-center gap-2 text-[14px]">
              {user.avatarUrl && <img className="h-[26px] w-[26px] rounded-full" src={user.avatarUrl} alt="" />}
              <span className="text-[#333]">{user.username}</span>
              {!user.unlimited && (
                <span className="rounded-full border border-gold-edge bg-gold-wash px-2 py-0.5 text-[12px] text-gold-ink">
                  剩余 {user.remaining}
                </span>
              )}
              <button
                className="cursor-pointer rounded-lg border border-[#ddd] bg-white px-3 py-[7px] text-[14px] hover:bg-[#f3f3f0]"
                onClick={() => void logout()}
              >
                登出
              </button>
            </span>
          ) : (
            <button
              className="cursor-pointer rounded-lg border border-[#1a1a1a] bg-[#1a1a1a] px-3 py-[7px] text-[14px] text-white"
              onClick={login}
            >
              GitHub 登录
            </button>
          )}
        </div>
      </header>

      {error && <div className="mb-4 text-danger">{error}</div>}

      <div className="grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-4">
        {loading && Array.from({ length: 6 }, (_, i) => (
          <div key={i} className="rounded-xl border border-[#ececec] bg-white p-5" aria-hidden>
            <div className="mb-4 h-[17px] w-4/5 animate-pulse rounded-md bg-[#eee] motion-reduce:animate-none" />
            <div className="h-[13px] w-[45%] animate-pulse rounded-md bg-[#eee] motion-reduce:animate-none" />
          </div>
        ))}
        {!loading && docs.map(d => (
          <article
            key={d.id}
            className="group relative cursor-pointer rounded-xl border border-[#ececec] bg-white p-5 transition-[box-shadow,transform] duration-150 hover:-translate-y-0.5 hover:shadow-[0_6px_24px_rgba(0,0,0,.08)] motion-reduce:transition-none"
            onClick={() => navigate(`/reports/${d.id}`)}
          >
            <h2 className="mb-3 text-[17px] leading-[1.4]">{d.filename}</h2>
            <div className="flex gap-3 text-[13px] text-[#888]">
              <span>{new Date(d.created_at).toLocaleDateString('zh-CN')}</span>
              <span>{d.chunk_count} 段</span>
            </div>
            {/* 触屏没有 hover:移动端常显,桌面维持「悬停才出现」的原样 */}
            {isAdmin && (
              <button
                className="absolute right-3.5 top-3.5 cursor-pointer text-[12px] text-danger opacity-100 md:opacity-0 md:group-hover:opacity-100"
                onClick={e => onDelete(e, d.id)}
              >
                删除
              </button>
            )}
          </article>
        ))}
        {!loading && docs.length === 0 && (
          <p className="col-span-full py-15 text-center text-[#999]">还没有报告。</p>
        )}
      </div>
    </div>
  )
```

- [ ] **Step 2: 删掉 CSS Module**

```bash
rm packages/client/src/pages/HomePage.module.css
```

- [ ] **Step 3: 验证**

```bash
grep -rn "HomePage.module.css\|styles\." packages/client/src/pages/HomePage.tsx
pnpm typecheck && pnpm test && pnpm build:client
```
Expected: `grep` 无输出;其余全部成功。

- [ ] **Step 4: 提交**

```bash
git add -A packages/client/src/pages
git commit -m "feat(home): 首页迁到 Tailwind,修复删除按钮在触屏上不可点"
```

---

### Task 7: traces 五组件迁移 + 补上缺失的设计变量

`TracesPage`、`TraceList`、`TraceStats`、`SpanWaterfall`、`TraceDetailPage` 的 CSS 引用了从未定义的 `--purple` / `--gray-*` / `--radius*`。迁移时映射到 Tailwind 内置 `gray-*` 与真实圆角,**这会让这两个页面变成作者本来想要的样子**(见「已知偏差」)。

映射表(实现时严格照此,别自由发挥):

| 原变量 | 迁移后 |
|---|---|
| `var(--purple)` | `text-violet-600` |
| `var(--gray-50)` | `bg-gray-50` |
| `var(--gray-100)` | `bg-gray-100` / `border-gray-100` |
| `var(--gray-200)` | `border-gray-200` |
| `var(--gray-400)` | `text-gray-400` |
| `var(--gray-600)` | `text-gray-600` |
| `var(--gray-900)` | `text-gray-900` / `bg-gray-900` |
| `var(--radius-sm)` | `rounded` |
| `var(--radius)` | `rounded-lg` |

**Files:**
- Modify: `packages/client/src/components/TracesPage.tsx`、`TraceList.tsx`、`TraceStats.tsx`、`SpanWaterfall.tsx`、`TraceDetailPage.tsx`
- Delete: 对应 5 个 `.module.css`

- [ ] **Step 1: 逐个改写组件**

规则:
- 每个文件删掉 `import styles from './X.module.css'`,把 `className={styles.foo}` 换成等价 utility。**取值以对应的 `.module.css` 为准**(此刻这些文件都还在磁盘上,先读再改),除上表映射外不改任何数值。
- `TraceList` 的 `badgeClass` / `SpanWaterfall` 的 `barClass` 这类 `Record<TraceStatus, string>` 映射,改成存 utility 字符串。

`TraceList.tsx` 完整改写后如下,其余 4 个文件照同样手法处理:

```tsx
import type { TraceRecord, TraceStatus } from '../types'

interface Props {
  traces: TraceRecord[]
  onSelect: (id: string) => void
}

const statusLabel: Record<TraceStatus, string> = {
  ok: '正常',
  degraded: '降级',
  error: '错误',
}

const badgeClass: Record<TraceStatus, string> = {
  ok: 'bg-[#dcfce7] text-[#16a34a]',
  degraded: 'bg-[#fef3c7] text-[#d97706]',
  error: 'bg-[#fee2e2] text-[#dc2626]',
}

export function TraceList({ traces, onSelect }: Props) {
  if (traces.length === 0) {
    return <div className="p-12 text-center text-gray-400">暂无 trace 记录</div>
  }
  return (
    // 横向滚动发生在组件内部,而不是整个 body
    <div className="overflow-x-auto">
      <table className="w-full border-collapse text-[13px]">
        <thead>
          <tr className="[&>th]:border-b [&>th]:border-gray-200 [&>th]:px-3 [&>th]:py-2 [&>th]:text-left [&>th]:font-semibold [&>th]:whitespace-nowrap [&>th]:text-gray-600">
            <th>状态</th><th>路由</th><th>耗时</th><th>span</th><th>降级/错误</th><th>时间</th>
          </tr>
        </thead>
        <tbody>
          {traces.map(t => (
            <tr
              key={t.id}
              className="cursor-pointer border-b border-gray-100 hover:bg-gray-50 [&>td]:px-3 [&>td]:py-2 [&>td]:text-gray-900"
              onClick={() => onSelect(t.id)}
            >
              <td>
                <span className={`inline-block rounded px-2 py-0.5 text-[11px] font-semibold ${badgeClass[t.status]}`}>
                  {statusLabel[t.status]}
                </span>
              </td>
              <td className="font-mono text-[12px]">{t.route}</td>
              <td className="whitespace-nowrap">{t.duration_ms}ms</td>
              <td>{t.span_count}</td>
              <td className="whitespace-nowrap">{t.degraded_count}/{t.error_count}</td>
              <td className="whitespace-nowrap text-gray-400">{new Date(t.started_at).toLocaleString()}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

- [ ] **Step 2: 其余宽内容包 `overflow-x-auto`**

`SpanWaterfall` 的最外层容器同理,并给内层行一个最小宽度,免得 220px 标签列被压扁:

```tsx
return (
  <div className="mt-4 overflow-x-auto">
    <div className="flex min-w-[560px] flex-col gap-0.5">
      {/* ...原 rows... */}
    </div>
  </div>
)
```

`TraceStats` 的 `.container` 原为 `display:flex` 横排,窄屏会挤:改成 `flex flex-col gap-4 md:flex-row md:items-start md:gap-6`。`.reasonRow` 的 `grid-template-columns: 160px 1fr 40px` 改成 `grid-cols-[100px_1fr_32px] md:grid-cols-[160px_1fr_40px]`。

- [ ] **Step 3: 页面容器 padding 收窄**

`TracesPage` 与 `TraceDetailPage` 的 `.page`(原 `max-width:1100px; margin:0 auto; padding:24px`)改为:

```tsx
className="mx-auto max-w-[1100px] px-4 py-5 md:p-6"
```

`TracesPage` 的 `.header`(原 `flex; gap:16px`)加 `flex-wrap`:`mb-5 flex flex-wrap items-center gap-4`。

- [ ] **Step 4: 删掉 5 个 CSS Module**

```bash
rm packages/client/src/components/TracesPage.module.css \
   packages/client/src/components/TraceList.module.css \
   packages/client/src/components/TraceStats.module.css \
   packages/client/src/components/SpanWaterfall.module.css \
   packages/client/src/components/TraceDetailPage.module.css
```

- [ ] **Step 5: 验证**

```bash
grep -rn "module.css" packages/client/src/components
pnpm typecheck && pnpm test && pnpm build:client
```
Expected: `grep` 无输出;其余全部成功。

- [ ] **Step 6: 提交**

```bash
git add -A packages/client/src/components
git commit -m "refactor(traces): trace 页迁到 Tailwind,补齐移植时漏带的设计变量"
```

---

### Task 8: eval 两页迁移

**Files:**
- Modify: `packages/client/src/pages/EvalDashboard.tsx`、`EvalDetailPage.tsx`
- Delete: `packages/client/src/pages/EvalDashboard.module.css`、`EvalDetailPage.module.css`

- [ ] **Step 1: 改写 EvalDashboard**

`.page` → `mx-auto max-w-[1100px] px-4 py-5 md:px-6 md:py-7`;`.header` → `mb-5 flex flex-wrap items-center gap-4`;`.stats` → `mb-6 flex flex-wrap gap-3.5`;`.stat` → `min-w-[120px] rounded-xl border border-[#ececec] bg-white px-[22px] py-4`;`.v` → `text-2xl font-semibold`;`.l` → `mt-1 text-[13px] text-[#888]`。

状态色的 `styles[\`s_${r.status}\`]` 动态索引换成显式映射(Tailwind 扫描不到拼接出来的类名):

```tsx
const statusClass: Record<EvalStatus, string> = {
  none: 'text-[#999]',
  running: 'text-[#c9821b]',
  done: 'text-[#1a7f37]',
  failed: 'text-danger',
}
```
用法:`<td className={statusClass[r.status]}>`。

表格包一层横向滚动容器:

```tsx
<div className="overflow-x-auto">
  <table className="w-full border-collapse whitespace-nowrap text-[14px]">
    {/* ...原内容,th/td 类名 → "border-b border-[#eee] px-3 py-2.5 text-left" ... */}
  </table>
</div>
```

`.name` → `cursor-pointer text-[#1a1a1a] hover:text-gold`;`.runBtn` → `cursor-pointer rounded-lg border border-[#1a1a1a] bg-[#1a1a1a] px-3 py-[5px] text-[13px] text-white disabled:cursor-default disabled:opacity-50`;`.empty` → `py-10 text-center text-[#999]`。

- [ ] **Step 2: 改写 EvalDetailPage**

`.page` → `mx-auto max-w-[860px] px-4 py-5 md:px-6 md:py-7`;`.back` → `text-[14px] text-[#555]`;`.h1` → `mt-3 mb-1.5 text-[22px]`;`.summary` → `mb-5 text-[#666]`;`.card` → `mb-3.5 rounded-xl border border-[#ececec] bg-white p-4`;`.q` → `mb-2 font-semibold`;`.scores` → `mb-2.5 flex flex-wrap gap-3 text-[13px]`;`.ok` → `text-[#1a7f37]`;`.bad` → `text-danger`;`.row` → `my-1.5 text-[14px] leading-[1.6]`;`.row b` → 给 `<b>` 加 `className="mr-1 text-[#555]"`;`.reason` → `mt-2 border-t border-dashed border-[#eee] pt-2 text-[12px] text-[#888]`;`.empty` → `text-[#999]`。

- [ ] **Step 3: 删掉两个 CSS Module**

```bash
rm packages/client/src/pages/EvalDashboard.module.css packages/client/src/pages/EvalDetailPage.module.css
```

- [ ] **Step 4: 验证**

```bash
grep -rn "module.css" packages/client/src
pnpm typecheck && pnpm test && pnpm build:client
```
Expected: `grep` **完全无输出**(全站 8 个 module.css 至此清零);其余全部成功。

- [ ] **Step 5: 提交**

```bash
git add -A packages/client/src/pages
git commit -m "refactor(eval): 评估后台迁到 Tailwind,宽表格改为内部横向滚动"
```

---

### Task 9: 全量验收

**Files:**
- 无代码改动(除非验收发现问题)

- [ ] **Step 1: 确认 CSS Modules 清零**

```bash
find packages/client/src -name "*.module.css" | wc -l
```
Expected: `0`

- [ ] **Step 2: 自动化验收**

```bash
pnpm test && pnpm typecheck && pnpm build:client
```
Expected: 全部 PASS / 构建成功。

- [ ] **Step 3: 起开发服务器**

```bash
pnpm dev:client
```
后端与 chroma 需按 README 先起好,否则报告列表为空、问答不可用。

- [ ] **Step 4: 移动端人工验收(375 / 390 / 768 三档宽度)**

375 = iPhone SE/13 mini,390 = iPhone 14/15,768 = 断点临界值(边界最易破)。逐项勾:

- [ ] 6 个路由均无横向溢出。在每个页面的 devtools console 跑一次:
  `document.documentElement.scrollWidth === document.documentElement.clientWidth` → 必须为 `true`
- [ ] 阅读页:点 ☰ 抽屉滑入、点遮罩关闭、点目录项跳转并自动关抽屉
- [ ] 阅读页:抽屉打开时背景正文**不能**跟着滑
- [ ] 阅读页:点底部条展开问答面板,面板顶不到底部条(两者不重叠,收起按钮始终可点)
- [ ] 阅读页:输入框聚焦、软键盘弹起时发送按钮**没有**被键盘遮住
- [ ] 阅读页:点「来源 §xxx」→ 面板收起 + 正文跳到对应章节 + flash 高亮肉眼可见
- [ ] 阅读页:刷新页面后面板仍默认收起(即移动端没写 localStorage)
- [ ] 首页:以管理员登录,删除按钮**默认可见**且能点中
- [ ] 768px 两侧各 1px(767 / 769)布局均无破损

- [ ] **Step 5: 桌面人工验收(1280 宽)**

「像素级保持」没有自动化保障(项目无截图回归基建,本次不引入 Playwright),靠肉眼对照迁移前的样子:

- [ ] 首页:卡片间距、hover 抬升 + 阴影、header 各元素间距、额度 pill 描边与底色
- [ ] 阅读页:三栏宽度 240 / 1fr / 360,目录项 hover 底色,右侧竖条位置与收起动画
- [ ] 阅读页正文:**项目符号还在**(preflight 重置的重灾区)、h2 下划线与字号层级、表格边框与表头底色、金色引用条、代码块底色与圆角、flash 高亮
- [ ] 问答面板:用户气泡黑底与不对称圆角、来源 chip 金色描边、输入行与禁用态
- [ ] eval 两页:统计卡片、表格、状态色
- [ ] traces 两页:确认变成了「有卡片底色 + 有圆角」的正常样子(这是本次唯一的预期变化)

- [ ] **Step 6: 收尾提交**

若上述验收未改动任何文件,跳过本步。若有修复:

```bash
git add -A packages/client
git commit -m "fix(client): H5 迁移验收发现的样式问题"
```

---

## 完成后

参考 `superpowers:finishing-a-development-branch` 决定合并方式。注意工作区里 `deploy.sh` 的改动与本次无关,不要一起提交。
