# 研报站(ai-research-hub)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 本地运行的精致研报阅读站 + 单篇 AI 问答(带溯源回链),以 docmind 为叶子服务来源。

**Architecture:** pnpm monorepo(Fastify + TS 后端 / React19 + Vite 前端)。采用"干净脚手架 + 精选移植"策略:从 `~/myCode/docmind` 逐字复制可复用的底层服务(智谱 LLM 流式、embedding、ChromaDB 向量、RAG 检索配置),路由与前端全部重写为无登录、单文档、带章节溯源的最小实现。markdown 按标题分块,每块携带章节 slug;检索命中的块作为"来源"随 SSE 返回,前端渲染成可点 chip,点击滚动到对应标题并高亮。

**Tech Stack:** Node.js + Fastify 5 + tsx + better-sqlite3 + ChromaDB + 智谱 GLM/embedding-3;React 19 + Vite 6 + react-router 7 + react-markdown 9 + remark-gfm + rehype-slug;github-slugger(后端分块)。测试:`tsx --test`(后端)、vitest(前端)。

## Global Constraints

- provider 全用智谱:`ZHIPU_API_KEY` 必填;生成用 `ZHIPU_MODEL`(默认 `glm-4.7`,逗号分隔可多模型备用),向量用 `ZHIPU_EMBEDDING_MODEL`(默认 `embedding-3`)。不配置 Anthropic。
- 无登录、单用户:所有数据不做 user 维度隔离。
- 报告展示名 = 上传文件名去扩展名(或正文首个 `#` H1),不解析公司/类型/评级元数据。
- 问答范围限当前单篇文档(检索 `where doc_id = 当前报告`)。
- markdown 分块必须携带章节信息:`section_title`、`section_slug`(github-slugger 生成)、`section_path`、`char_start`、`char_end`。
- 章节锚点约定:前端 react-markdown 用 `rehype-slug` 给标题生成 `id`;后端分块用 `github-slugger` 生成同款 slug,两侧对同一份 markdown 按文档顺序处理 → slug 一致。
- 包管理器用 pnpm;Node ≥ 20。
- ChromaDB 集合名:`research_docs`。SQLite 文件:`data/research.db`。上传原文存于 `data/raw/<docId>.md`。
- 复制自 docmind 的文件除下述明确编辑外**逐字保留**,不擅自重构。

---

### Task 1: Monorepo 脚手架

**Files:**
- Create: `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`
- Create: `packages/server/package.json`, `packages/server/tsconfig.json`, `packages/server/.env.example`, `packages/server/src/index.ts`
- Create: `packages/client/package.json`, `packages/client/tsconfig.json`, `packages/client/vite.config.ts`, `packages/client/index.html`, `packages/client/src/main.tsx`, `packages/client/src/App.tsx`, `packages/client/src/index.css`, `packages/client/src/vite-env.d.ts`
- Create: `docker-compose.dev.yml`

**Interfaces:**
- Produces: 可启动的空壳。`pnpm dev:server` → Fastify 监听 3001,`GET /health` 返回 `{status:'ok'}`。`pnpm dev:client` → Vite 5173,渲染占位页,`/api` 代理到 3001。

- [ ] **Step 1: 根 workspace 配置**

`pnpm-workspace.yaml`:
```yaml
packages:
  - 'packages/*'
```

`package.json`:
```json
{
  "name": "ai-research-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter @arh/server dev",
    "dev:client": "pnpm --filter @arh/client dev",
    "build:client": "pnpm --filter @arh/client build",
    "typecheck": "pnpm --filter @arh/server typecheck && pnpm --filter @arh/client exec tsc --noEmit",
    "test": "pnpm --filter @arh/server test && pnpm --filter @arh/client test"
  }
}
```

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

- [ ] **Step 2: server 包**

`packages/server/package.json`:
```json
{
  "name": "@arh/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/services/*.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@fastify/multipart": "^9.0.3",
    "@anthropic-ai/sdk": "^0.39.0",
    "better-sqlite3": "^12.9.0",
    "chromadb": "^3.4.3",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "github-slugger": "^2.0.0",
    "openai": "^6.34.0",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.6.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
```
> `@anthropic-ai/sdk` 仅因逐字复制的 `llm.ts` 引用而保留;实际不使用 Anthropic。

`packages/server/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "dist", "rootDir": "src" },
  "include": ["src"]
}
```

`packages/server/.env.example`:
```
ZHIPU_API_KEY=
ZHIPU_MODEL=glm-4.7
ZHIPU_EMBEDDING_MODEL=embedding-3
PORT=3001
CHROMA_URL=http://localhost:8000
# LOG_RETRIEVAL=1
```

`packages/server/src/index.ts`(本任务先只挂 /health,后续任务补 init 与路由):
```ts
// packages/server/src/index.ts
import Fastify from 'fastify'
import cors from '@fastify/cors'
import multipart from '@fastify/multipart'
import 'dotenv/config'

const app = Fastify({
  logger: { transport: { target: 'pino-pretty', options: { colorize: true } } },
})

await app.register(cors, {
  origin: ['http://localhost:5173', 'http://localhost:4173'],
})
await app.register(multipart, { limits: { fileSize: 20 * 1024 * 1024 } })

app.get('/health', async () => ({ status: 'ok', timestamp: new Date().toISOString() }))

const port = Number(process.env.PORT) || 3001
try {
  await app.listen({ port, host: '0.0.0.0' })
} catch (err) {
  app.log.error(err)
  process.exit(1)
}
```

- [ ] **Step 3: client 包**

`packages/client/package.json`:
```json
{
  "name": "@arh/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-markdown": "^9.0.1",
    "react-router-dom": "^7.18.1",
    "rehype-slug": "^6.0.0",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "happy-dom": "^20.10.6",
    "typescript": "^6.0.3",
    "vite": "^6.1.0",
    "vitest": "^4.1.9"
  }
}
```

`packages/client/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true
  },
  "include": ["src"]
}
```

`packages/client/vite.config.ts`(与 docmind 一致):
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: { '/api': { target: 'http://localhost:3001', changeOrigin: true } },
  },
  test: { environment: 'happy-dom' },
})
```

`packages/client/index.html`:
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>研报站</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`packages/client/src/vite-env.d.ts`:
```ts
/// <reference types="vite/client" />
```

`packages/client/src/main.tsx`:
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import './index.css'
import App from './App'

const rootEl = document.getElementById('root')
if (!rootEl) throw new Error('Root element #root not found')

createRoot(rootEl).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
)
```

`packages/client/src/App.tsx`(占位,Task 8 重写):
```tsx
export default function App() {
  return <div style={{ padding: 24 }}>研报站 — 脚手架就绪</div>
}
```

`packages/client/src/index.css`:
```css
:root { color-scheme: light; }
* { box-sizing: border-box; }
body { margin: 0; font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif; color: #1a1a1a; background: #fafaf8; }
```

- [ ] **Step 4: docker-compose(仅 chroma,dev 用)**

`docker-compose.dev.yml`:
```yaml
services:
  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - ./data/chroma:/data
```

- [ ] **Step 5: 安装 + 启动验证**

Run:
```bash
pnpm install
docker compose -f docker-compose.dev.yml up chroma -d
pnpm dev:server &
sleep 3 && curl -s localhost:3001/health
```
Expected: `{"status":"ok","timestamp":"..."}`。再 `pnpm dev:client`,浏览器 `localhost:5173` 显示"研报站 — 脚手架就绪"。停掉后台 server。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: monorepo 脚手架(server Fastify + client Vite)"
```

---

### Task 2: 移植 docmind 叶子服务(LLM/embedding/RAG 配置)

**Files:**
- Create (copy verbatim from docmind): `packages/server/src/llm.ts`, `packages/server/src/llmLog.ts`, `packages/server/src/services/embeddings.ts`, `packages/server/src/services/ragConfig.ts`
- Create: `packages/server/src/services/tracing.ts`(no-op shim)
- Create: `packages/server/src/services/db.ts`
- Create: `packages/server/src/types.ts`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `streamChat(opts: StreamChatOptions): AsyncGenerator<string>`、`PROVIDER: LLMProvider`(from `llm.ts`)
  - `embedBatch(texts: string[]): Promise<number[][]>`、`isEmbeddingAvailable(): boolean`、`class ZhipuEmbeddingFunction`(from `embeddings.ts`)
  - `RAG`、`candidatePool(maxK: number): number`(from `ragConfig.ts`)
  - `markDegraded(reason?: string, meta?: unknown): void`(no-op)
  - `initDb(): DB`、`type DB`(from `db.ts`,`DB = ReturnType<typeof Database>`)
  - types:`LLMProvider`、`MessageRole`、`LLMMessage`、`StreamChatOptions`、`Document`、`DocumentChunk`

- [ ] **Step 1: 复制 4 个叶子文件**

Run(源为用户本机 docmind):
```bash
D=~/myCode/docmind/packages/server/src
S=packages/server/src
cp "$D/llm.ts"                 "$S/llm.ts"
cp "$D/llmLog.ts"              "$S/llmLog.ts"
cp "$D/services/embeddings.ts" "$S/services/embeddings.ts"
cp "$D/services/ragConfig.ts"  "$S/services/ragConfig.ts"
```
不修改这四个文件的内容(`llm.ts` 与 `embeddings.ts` 对 `./services/tracing.js` / `../types.js` 的 import 由后续步骤满足)。

- [ ] **Step 2: tracing no-op shim**

`packages/server/src/services/tracing.ts`:
```ts
// 首版不做分布式追踪;仅提供 llm.ts / documentVector.ts 引用的 markDegraded 空实现。
export function markDegraded(_reason?: string, _meta?: unknown): void {
  /* no-op */
}
```

- [ ] **Step 3: SQLite 连接单例**

`packages/server/src/services/db.ts`:
```ts
// packages/server/src/services/db.ts
import Database from 'better-sqlite3'
import { mkdirSync } from 'node:fs'
import { dirname } from 'node:path'

export type DB = Database.Database

const DB_PATH = process.env.DB_PATH ?? 'data/research.db'

let _db: DB | null = null

export function initDb(): DB {
  if (_db) return _db
  mkdirSync(dirname(DB_PATH), { recursive: true })
  _db = new Database(DB_PATH)
  _db.pragma('journal_mode = WAL')
  return _db
}

export function getDb(): DB {
  if (!_db) throw new Error('db not initialized — call initDb() first')
  return _db
}
```

- [ ] **Step 4: 精简 types.ts**

`packages/server/src/types.ts`:
```ts
// packages/server/src/types.ts
export type LLMProvider = 'anthropic' | 'zhipu'
export type MessageRole = 'user' | 'assistant'

export interface LLMMessage {
  role: MessageRole
  content: string
}

export interface StreamChatOptions {
  messages: LLMMessage[]
  system?: string
  maxTokens?: number
  tag?: string
  signal?: AbortSignal
  onReasoning?: (delta: string) => void
}

/** SQLite documents 行 */
export interface Document {
  id: string
  filename: string       // 展示名(去扩展名)
  size_bytes: number
  chunk_count: number
  created_at: string
}

/** 从 ChromaDB 检索出的一个块(含章节信息,用于溯源) */
export interface DocumentChunk {
  doc_id: string
  filename: string
  chunk_index: number
  content: string
  distance: number
  section_title: string
  section_slug: string
}
```

- [ ] **Step 5: 类型检查**

Run: `pnpm --filter @arh/server typecheck`
Expected: 通过(0 errors)。若 `llm.ts` 因 `PROVIDER` 在无 key 时抛错——typecheck 不执行运行时代码,不受影响。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: 移植 docmind LLM/embedding/RAG 叶子服务 + db/types/tracing shim"
```

---

### Task 3: markdownParser —— 按标题分块(核心,TDD)

**Files:**
- Create: `packages/server/src/services/markdownParser.ts`
- Test: `packages/server/src/services/markdownParser.test.ts`

**Interfaces:**
- Consumes: `github-slugger`。
- Produces:
  - `interface MdChunk { content: string; chunk_index: number; section_title: string; section_slug: string; section_path: string; char_start: number; char_end: number }`
  - `parseMarkdown(md: string, opts?: { maxChars?: number }): { displayName: string; chunks: MdChunk[] }`
    - `displayName`:正文首个 `#` H1 文本;无则空串。
    - 按 ATX 标题(`#`~`######`,必须行首)切段,每段归属"最近的标题";标题前的引言归属一个空标题段(`section_title: ''`,`section_slug: ''`)。
    - `section_slug`:对所有标题按文档顺序用**同一个** `GithubSlugger` 实例生成,保证与前端 rehype-slug 一致。
    - `section_path`:祖先标题(按 level 栈)用 ` / ` 连接,如 `第一部分 / 1.2 重大事件编年`。
    - 段落超过 `maxChars`(默认 1200)时按 `\n\n` 边界二次切分为多个 chunk,共享同一章节字段,`chunk_index` 全局递增。
    - `char_start`/`char_end`:该 chunk 内容在原始 `md` 字符串中的起止偏移。

- [ ] **Step 1: 写失败测试**

`packages/server/src/services/markdownParser.test.ts`:
```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { parseMarkdown } from './markdownParser.ts'

const SAMPLE = `# 碳酸锂产业链投资研究报告

导言一句话。

## 1.2 重大事件编年

事件表格内容。

## 1.3 大事件的规律与启示

三条铁律:一供给、二种子、三政策。
`

test('displayName 取首个 H1', () => {
  const { displayName } = parseMarkdown(SAMPLE)
  assert.equal(displayName, '碳酸锂产业链投资研究报告')
})

test('按标题切出正确的章节数(引言 + H1 + 两个 H2)', () => {
  const { chunks } = parseMarkdown(SAMPLE)
  const titles = chunks.map(c => c.section_title)
  assert.ok(titles.includes('1.2 重大事件编年'))
  assert.ok(titles.includes('1.3 大事件的规律与启示'))
})

test('section_slug 与 github-slugger 对同序标题一致', () => {
  const { chunks } = parseMarkdown(SAMPLE)
  const c = chunks.find(c => c.section_title === '1.3 大事件的规律与启示')!
  assert.equal(c.section_slug, '13-大事件的规律与启示')
})

test('section_path 含祖先标题', () => {
  const { chunks } = parseMarkdown(SAMPLE)
  const c = chunks.find(c => c.section_title === '1.2 重大事件编年')!
  assert.equal(c.section_path, '碳酸锂产业链投资研究报告 / 1.2 重大事件编年')
})

test('char_start/char_end 定位准确', () => {
  const { chunks } = parseMarkdown(SAMPLE)
  const c = chunks.find(c => c.section_title === '1.3 大事件的规律与启示')!
  assert.ok(SAMPLE.slice(c.char_start, c.char_end).includes('三条铁律'))
})

test('超长章节按段落二次切分,共享章节字段', () => {
  const big = '# H\n\n' + Array.from({ length: 20 }, (_, i) => `第${i}段。`.repeat(30)).join('\n\n')
  const { chunks } = parseMarkdown(big, { maxChars: 300 })
  assert.ok(chunks.length > 1)
  assert.ok(chunks.every(c => c.section_title === 'H'))
  chunks.forEach((c, i) => assert.equal(c.chunk_index, i))
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @arh/server exec tsx --test src/services/markdownParser.test.ts`
Expected: FAIL(`parseMarkdown` 未定义 / 模块不存在)。

- [ ] **Step 3: 实现 parseMarkdown**

`packages/server/src/services/markdownParser.ts`:
```ts
// packages/server/src/services/markdownParser.ts
import GithubSlugger from 'github-slugger'

export interface MdChunk {
  content: string
  chunk_index: number
  section_title: string
  section_slug: string
  section_path: string
  char_start: number
  char_end: number
}

interface RawSection {
  title: string
  slug: string
  path: string
  bodyStart: number  // 正文(标题行之后)在原文中的起始偏移
  bodyEnd: number
}

const HEADING_RE = /^(#{1,6})\s+(.+?)\s*#*\s*$/

/** 扫描所有 ATX 标题,返回 {level,title,lineStart(行首偏移),bodyStart(该行末尾+1)} */
function scanHeadings(md: string) {
  const slugger = new GithubSlugger()
  const heads: Array<{ level: number; title: string; slug: string; lineStart: number; bodyStart: number }> = []
  let offset = 0
  for (const line of md.split('\n')) {
    const m = HEADING_RE.exec(line)
    if (m) {
      heads.push({
        level: m[1].length,
        title: m[2].trim(),
        slug: slugger.slug(m[2].trim()),
        lineStart: offset,
        bodyStart: offset + line.length + 1,
      })
    }
    offset += line.length + 1 // +1 换行符
  }
  return heads
}

function buildSections(md: string): RawSection[] {
  const heads = scanHeadings(md)
  const sections: RawSection[] = []

  // 引言:首个标题之前的内容
  if (heads.length === 0 || heads[0].lineStart > 0) {
    const end = heads.length ? heads[0].lineStart : md.length
    if (md.slice(0, end).trim()) {
      sections.push({ title: '', slug: '', path: '', bodyStart: 0, bodyEnd: end })
    }
  }

  const stack: Array<{ level: number; title: string }> = []
  for (let i = 0; i < heads.length; i++) {
    const h = heads[i]
    while (stack.length && stack[stack.length - 1].level >= h.level) stack.pop()
    stack.push({ level: h.level, title: h.title })
    const path = stack.map(s => s.title).join(' / ')
    const bodyEnd = i + 1 < heads.length ? heads[i + 1].lineStart : md.length
    sections.push({ title: h.title, slug: h.slug, path, bodyStart: h.bodyStart, bodyEnd })
  }
  return sections
}

/** 在 [start,end) 内按 \n\n 边界把内容切成 ≤ maxChars 的片段,返回带绝对偏移的片段。 */
function splitByParagraph(md: string, start: number, end: number, maxChars: number) {
  const text = md.slice(start, end)
  if (text.trim().length <= maxChars) return [{ start, end }]
  const pieces: Array<{ start: number; end: number }> = []
  let cur = start
  let acc = start
  const paras = text.split(/\n\n+/)
  let cursor = start
  for (const p of paras) {
    const pStart = md.indexOf(p, cursor)
    const pEnd = pStart + p.length
    cursor = pEnd
    if (pEnd - cur > maxChars && cur < pStart) {
      pieces.push({ start: cur, end: pStart })
      cur = pStart
    }
    acc = pEnd
  }
  pieces.push({ start: cur, end })
  return pieces
}

export function parseMarkdown(md: string, opts: { maxChars?: number } = {}): { displayName: string; chunks: MdChunk[] } {
  const maxChars = opts.maxChars ?? 1200
  const h1 = /^#\s+(.+?)\s*#*\s*$/m.exec(md)
  const displayName = h1 ? h1[1].trim() : ''

  const sections = buildSections(md)
  const chunks: MdChunk[] = []
  let idx = 0
  for (const s of sections) {
    for (const piece of splitByParagraph(md, s.bodyStart, s.bodyEnd, maxChars)) {
      const content = md.slice(piece.start, piece.end).trim()
      if (!content) continue
      chunks.push({
        content,
        chunk_index: idx++,
        section_title: s.title,
        section_slug: s.slug,
        section_path: s.path,
        char_start: piece.start,
        char_end: piece.end,
      })
    }
  }
  return { displayName, chunks }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @arh/server exec tsx --test src/services/markdownParser.test.ts`
Expected: PASS(6 tests)。若 `section_slug` 断言失败,核对 github-slugger 实际输出并把测试期望改为其真实值(以库为准),不要改库行为。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: markdownParser 按标题分块 + 章节 slug/path/offset(TDD)"
```

---

### Task 4: documentStore —— SQLite 文档表 + 原文落盘

**Files:**
- Create: `packages/server/src/services/documentStore.ts`
- Test: `packages/server/src/services/documentStore.test.ts`

**Interfaces:**
- Consumes: `initDb`/`DB` from `db.ts`;`Document` from `types.ts`。
- Produces:
  - `initDocumentTable(db: DB): void`
  - `saveDocument(opts: { filename: string; size_bytes: number; chunk_count: number }): Document`
  - `getAllDocuments(): Document[]`(按 created_at DESC)
  - `getDocument(id: string): Document | null`
  - `deleteDocument(id: string): void`
  - `saveRawMarkdown(id: string, md: string): void`(写 `data/raw/<id>.md`)
  - `readRawMarkdown(id: string): string | null`
  - `deleteRawMarkdown(id: string): void`

- [ ] **Step 1: 写失败测试**

`packages/server/src/services/documentStore.test.ts`:
```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import Database from 'better-sqlite3'
import { initDocumentTable, saveDocument, getAllDocuments, getDocument, deleteDocument } from './documentStore.ts'

test('save/get/list/delete 往返', () => {
  const db = new Database(':memory:')
  initDocumentTable(db)
  const doc = saveDocument({ filename: '碳酸锂', size_bytes: 100, chunk_count: 5 })
  assert.equal(getDocument(doc.id)?.filename, '碳酸锂')
  assert.equal(getAllDocuments().length, 1)
  deleteDocument(doc.id)
  assert.equal(getAllDocuments().length, 0)
})
```
> 测试通过 `initDocumentTable(db)` 注入内存库,故 store 需支持被注入 db(见实现)。

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @arh/server exec tsx --test src/services/documentStore.test.ts`
Expected: FAIL(模块不存在)。

- [ ] **Step 3: 实现**

`packages/server/src/services/documentStore.ts`:
```ts
// packages/server/src/services/documentStore.ts
import { mkdirSync, writeFileSync, readFileSync, rmSync, existsSync } from 'node:fs'
import { join } from 'node:path'
import type { DB } from './db.js'
import type { Document } from '../types.js'

const RAW_DIR = process.env.RAW_DIR ?? 'data/raw'
let _db: DB | null = null

export function initDocumentTable(db: DB): void {
  _db = db
  db.exec(`
    CREATE TABLE IF NOT EXISTS documents (
      id          TEXT PRIMARY KEY,
      filename    TEXT NOT NULL,
      size_bytes  INTEGER NOT NULL,
      chunk_count INTEGER NOT NULL,
      created_at  TEXT NOT NULL
    );
  `)
}

function db(): DB {
  if (!_db) throw new Error('documentStore not initialized — call initDocumentTable() first')
  return _db
}

function genId(): string {
  return `doc_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`
}

export function saveDocument(opts: { filename: string; size_bytes: number; chunk_count: number }): Document {
  const id = genId()
  const created_at = new Date().toISOString()
  db().prepare(
    'INSERT INTO documents (id, filename, size_bytes, chunk_count, created_at) VALUES (?, ?, ?, ?, ?)',
  ).run(id, opts.filename, opts.size_bytes, opts.chunk_count, created_at)
  return { id, ...opts, created_at }
}

export function getAllDocuments(): Document[] {
  return db().prepare('SELECT * FROM documents ORDER BY created_at DESC').all() as Document[]
}

export function getDocument(id: string): Document | null {
  return (db().prepare('SELECT * FROM documents WHERE id = ?').get(id) as Document) ?? null
}

export function deleteDocument(id: string): void {
  db().prepare('DELETE FROM documents WHERE id = ?').run(id)
}

export function saveRawMarkdown(id: string, md: string): void {
  mkdirSync(RAW_DIR, { recursive: true })
  writeFileSync(join(RAW_DIR, `${id}.md`), md, 'utf8')
}

export function readRawMarkdown(id: string): string | null {
  const p = join(RAW_DIR, `${id}.md`)
  return existsSync(p) ? readFileSync(p, 'utf8') : null
}

export function deleteRawMarkdown(id: string): void {
  const p = join(RAW_DIR, `${id}.md`)
  if (existsSync(p)) rmSync(p)
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @arh/server exec tsx --test src/services/documentStore.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat: documentStore SQLite 文档表 + 原文落盘"
```

---

### Task 5: documentVector —— 携带章节元数据的向量存取

**Files:**
- Create: `packages/server/src/services/documentVector.ts`(基于 docmind 同名文件改造)

**Interfaces:**
- Consumes: `embedBatch`/`isEmbeddingAvailable`/`ZhipuEmbeddingFunction`(embeddings.ts)、`RAG`/`candidatePool`(ragConfig.ts)、`markDegraded`(tracing.ts)、`MdChunk`(markdownParser.ts)、`DocumentChunk`(types.ts)。
- Produces:
  - `initDocCollection(): Promise<void>`、`isDocVectorAvailable(): boolean`
  - `upsertChunks(docId: string, filename: string, chunks: MdChunk[]): Promise<void>`(metadata 写入 `section_title`/`section_slug`)
  - `searchChunks(query: string, docId: string, maxK?: number): Promise<DocumentChunk[]>`(结果含 `section_title`/`section_slug`)
  - `deleteByDocId(docId: string): Promise<void>`

- [ ] **Step 1: 以 docmind 版为基,写改造后的文件**

`packages/server/src/services/documentVector.ts`:
```ts
// packages/server/src/services/documentVector.ts
import { ChromaClient } from 'chromadb'
import { embedBatch, isEmbeddingAvailable, ZhipuEmbeddingFunction } from './embeddings.js'
import { RAG, candidatePool } from './ragConfig.js'
import { markDegraded } from './tracing.js'
import type { MdChunk } from './markdownParser.js'
import type { DocumentChunk } from '../types.js'

const COLLECTION_NAME = 'research_docs'
const CHROMA_URL = process.env.CHROMA_URL ?? 'http://localhost:8000'
const LOG_RETRIEVAL = /^(1|true)$/i.test(process.env.LOG_RETRIEVAL ?? '')

type ChromaCollection = Awaited<ReturnType<ChromaClient['getOrCreateCollection']>>

let _client: ChromaClient | null = null
let _collection: ChromaCollection | null = null
let _available = false

export async function initDocCollection(): Promise<void> {
  if (!isEmbeddingAvailable()) {
    console.warn('[documentVector] Embedding 不可用 — 文档检索关闭')
    return
  }
  try {
    _client = new ChromaClient({ path: CHROMA_URL })
    _collection = await _client.getOrCreateCollection({
      name: COLLECTION_NAME,
      embeddingFunction: new ZhipuEmbeddingFunction(),
    })
    _available = true
    console.info(`[documentVector] ChromaDB connected — collection "${COLLECTION_NAME}"`)
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    console.warn(`[documentVector] ChromaDB unavailable (${msg}) — 检索关闭`)
  }
}

export function isDocVectorAvailable(): boolean {
  return _available
}

export async function upsertChunks(docId: string, filename: string, chunks: MdChunk[]): Promise<void> {
  if (!_available || !_collection || chunks.length === 0) return
  try {
    const texts = chunks.map(c => c.content)
    const embeddings = await embedBatch(texts)
    const ids = chunks.map(c => `${docId}_chunk_${c.chunk_index}`)
    const metadatas = chunks.map(c => ({
      doc_id: docId,
      filename,
      chunk_index: c.chunk_index,
      section_title: c.section_title,
      section_slug: c.section_slug,
    }))
    await _collection.upsert({ ids, embeddings, documents: texts, metadatas })
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    console.warn(`[documentVector] upsertChunks failed: ${msg}`)
  }
}

export async function searchChunks(query: string, docId: string, maxK = RAG.maxK): Promise<DocumentChunk[]> {
  if (!_available || !_collection) return []
  try {
    const queryEmbedding = (await embedBatch([query]))[0]
    const results = await _collection.query({
      queryEmbeddings: [queryEmbedding],
      nResults: candidatePool(maxK),
      where: { doc_id: { $eq: docId } },
    })
    const documents = results.documents[0] ?? []
    const metadatas = results.metadatas[0] ?? []
    const distances = results.distances?.[0] ?? []
    const meta = (i: number) => (metadatas[i] as Record<string, unknown>) ?? {}

    const candidates: DocumentChunk[] = documents.map((_, i) => ({
      doc_id: String(meta(i).doc_id ?? ''),
      filename: String(meta(i).filename ?? ''),
      chunk_index: Number(meta(i).chunk_index ?? 0),
      content: documents[i] ?? '',
      distance: distances[i] ?? 0,
      section_title: String(meta(i).section_title ?? ''),
      section_slug: String(meta(i).section_slug ?? ''),
    }))

    const within = candidates.filter(c => c.distance <= RAG.distanceThreshold)
    let kept = within.slice(0, maxK)
    if (kept.length < RAG.minK) {
      kept = candidates.slice(0, RAG.minK)
      markDegraded('doc_retrieval_minK', { topDistance: candidates[0]?.distance ?? null })
    }
    if (LOG_RETRIEVAL) {
      console.error(`🔍 检索 q="${query.slice(0, 40)}" 候选 ${candidates.length} → 保留 ${kept.length}`)
    }
    return kept
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    console.warn(`[documentVector] searchChunks failed: ${msg}`)
    return []
  }
}

export async function deleteByDocId(docId: string): Promise<void> {
  if (!_available || !_collection) return
  try {
    await _collection.delete({ where: { doc_id: { $eq: docId } } })
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    console.warn(`[documentVector] deleteByDocId failed: ${msg}`)
  }
}
```

- [ ] **Step 2: 类型检查**

Run: `pnpm --filter @arh/server typecheck`
Expected: 通过。(向量库集成需真实 ChromaDB,留待 Task 6 端到端手测。)

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: documentVector 携带章节元数据的向量存取(集合 research_docs)"
```

---

### Task 6: documents 路由 —— 上传 .md → 解析 → 落盘 → 索引

**Files:**
- Create: `packages/server/src/routes/documents.ts`
- Modify: `packages/server/src/index.ts`(init 文档表 + chroma、注册路由)

**Interfaces:**
- Consumes: markdownParser、documentStore、documentVector。
- Produces(REST,均挂 `/api` 前缀):
  - `GET /api/documents` → `{ documents: Document[] }`
  - `POST /api/documents`(multipart 单文件 `.md`)→ `{ document: Document }`
  - `GET /api/documents/:id` → `{ document: Document, markdown: string }`
  - `DELETE /api/documents/:id` → `{ success: true }`

- [ ] **Step 1: 实现路由**

`packages/server/src/routes/documents.ts`:
```ts
// packages/server/src/routes/documents.ts
import type { FastifyPluginAsync } from 'fastify'
import { parseMarkdown } from '../services/markdownParser.js'
import {
  saveDocument, getAllDocuments, getDocument, deleteDocument,
  saveRawMarkdown, readRawMarkdown, deleteRawMarkdown,
} from '../services/documentStore.js'
import { upsertChunks, deleteByDocId, isDocVectorAvailable } from '../services/documentVector.js'

export const documentRoutes: FastifyPluginAsync = async (app) => {
  app.get('/documents', async () => ({ documents: getAllDocuments() }))

  app.get<{ Params: { id: string } }>('/documents/:id', async (request, reply) => {
    const doc = getDocument(request.params.id)
    if (!doc) return reply.status(404).send({ error: 'not_found' })
    const markdown = readRawMarkdown(doc.id) ?? ''
    return { document: doc, markdown }
  })

  app.post('/documents', async (request, reply) => {
    const data = await request.file()
    if (!data) return reply.status(400).send({ error: '未上传文件' })
    if (!/\.(md|markdown|txt)$/i.test(data.filename)) {
      return reply.status(400).send({ error: '只支持 .md / .markdown / .txt 文件' })
    }
    const buffer = await data.toBuffer()
    const md = buffer.toString('utf8')
    if (!md.trim()) return reply.status(422).send({ error: '文件内容为空' })

    const { displayName, chunks } = parseMarkdown(md)
    const baseName = data.filename.replace(/\.(md|markdown|txt)$/i, '')
    const filename = displayName || baseName

    const doc = saveDocument({ filename, size_bytes: buffer.length, chunk_count: chunks.length })
    saveRawMarkdown(doc.id, md)

    if (isDocVectorAvailable()) {
      upsertChunks(doc.id, doc.filename, chunks).catch((err: unknown) => {
        const msg = err instanceof Error ? err.message : String(err)
        app.log.warn(`[documents] upsertChunks failed for ${doc.id}: ${msg}`)
      })
    }
    return { document: doc }
  })

  app.delete<{ Params: { id: string } }>('/documents/:id', async (request, reply) => {
    const doc = getDocument(request.params.id)
    if (!doc) return reply.status(404).send({ error: 'not_found' })
    await deleteByDocId(doc.id)
    deleteRawMarkdown(doc.id)
    deleteDocument(doc.id)
    return { success: true }
  })
}
```

- [ ] **Step 2: 更新 index.ts**

把 `packages/server/src/index.ts` 改为(在 Task 1 版本基础上增加 init 与路由注册):
```ts
// packages/server/src/index.ts
import Fastify from 'fastify'
import cors from '@fastify/cors'
import multipart from '@fastify/multipart'
import 'dotenv/config'

import { initDb } from './services/db.js'
import { initDocumentTable } from './services/documentStore.js'
import { initDocCollection } from './services/documentVector.js'
import { documentRoutes } from './routes/documents.js'
import { chatRoutes } from './routes/chat.js'

const app = Fastify({
  logger: { transport: { target: 'pino-pretty', options: { colorize: true } } },
})

await app.register(cors, { origin: ['http://localhost:5173', 'http://localhost:4173'] })
await app.register(multipart, { limits: { fileSize: 20 * 1024 * 1024 } })

const db = initDb()
initDocumentTable(db)
await initDocCollection()

await app.register(documentRoutes, { prefix: '/api' })
await app.register(chatRoutes, { prefix: '/api' })

app.get('/health', async () => ({ status: 'ok', timestamp: new Date().toISOString() }))

const port = Number(process.env.PORT) || 3001
try {
  await app.listen({ port, host: '0.0.0.0' })
} catch (err) {
  app.log.error(err)
  process.exit(1)
}
```
> `chatRoutes` 在 Task 7 创建;本步骤为让 index.ts 一次成型而先引入。若想让 Task 6 独立可跑,可临时注释 chat 相关两行,Task 7 再放开。

- [ ] **Step 3: 端到端手测(需真实 ZHIPU_API_KEY + ChromaDB)**

先在 `packages/server/.env` 填入 `ZHIPU_API_KEY`。临时注释 index.ts 中 chat 两行以便独立启动。
Run:
```bash
docker compose -f docker-compose.dev.yml up chroma -d
pnpm dev:server &   # 等日志出现 collection "research_docs"
sleep 4
curl -s -F "file=@$HOME/碳酸锂产业链投资研究报告.md" localhost:3001/api/documents
curl -s localhost:3001/api/documents
```
Expected: 第一条返回 `{"document":{"id":"doc_...","filename":"碳酸锂产业链投资研究报告","chunk_count":<N>,...}}`;第二条列表含该文档。日志无 upsert 报错。停掉后台 server。

- [ ] **Step 4: 提交**

```bash
git add -A && git commit -m "feat: documents 路由(上传 md → 解析 → 落盘 → 向量索引)+ index 装配"
```

---

### Task 7: chat 路由 —— 单篇 RAG 流式问答 + 来源事件

**Files:**
- Create: `packages/server/src/routes/chat.ts`

**Interfaces:**
- Consumes: `streamChat`/`PROVIDER`(llm.ts)、`searchChunks`/`isDocVectorAvailable`(documentVector.ts)、`getDocument`(documentStore.ts)。
- Produces:
  - `POST /api/chat/stream`,body `{ docId: string, message: string }`,SSE 事件流:
    - `data: {"sources":[{"section_title","section_slug","chunk_index"}]}`(首个事件,检索到的来源)
    - `data: {"text":"..."}`(多个,生成增量)
    - `data: {"done":true}` 或 `data: {"error":"..."}`
  - `GET /api/chat/health` → `{ status, provider }`

- [ ] **Step 1: 实现路由**

`packages/server/src/routes/chat.ts`:
```ts
// packages/server/src/routes/chat.ts
import type { FastifyPluginAsync } from 'fastify'
import { streamChat, PROVIDER } from '../llm.js'
import { searchChunks, isDocVectorAvailable } from '../services/documentVector.js'
import { getDocument } from '../services/documentStore.js'
import type { LLMMessage } from '../types.js'

const SYSTEM_BASE = `你是投研报告阅读助手。只依据"文档参考"中的内容回答用户关于当前这篇研报的问题。
规则:
- 严格基于文档参考,不要编造数字或结论;文档中没有的,明确说"报告中未提及"。
- 回答用中文,简洁、分点。
- 在引用具体结论时,用【来源:§章节名】标注它出自哪一节(章节名取文档参考中给出的)。`

interface StreamBody {
  docId: string
  message: string
}

type SSE =
  | { sources: Array<{ section_title: string; section_slug: string; chunk_index: number }> }
  | { text: string }
  | { done: true }
  | { error: string }

export const chatRoutes: FastifyPluginAsync = async (app) => {
  app.get('/chat/health', async () => ({ status: 'ok', provider: PROVIDER }))

  app.post<{ Body: StreamBody }>('/chat/stream', async (request, reply) => {
    const { docId, message } = request.body ?? ({} as StreamBody)
    if (!docId) return reply.status(400).send({ error: 'docId is required' })
    if (!message?.trim()) return reply.status(400).send({ error: 'message is required' })
    if (!getDocument(docId)) return reply.status(404).send({ error: 'document not found' })

    const chunks = isDocVectorAvailable() ? await searchChunks(message, docId) : []

    reply.raw.writeHead(200, {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
      'X-Accel-Buffering': 'no',
    })
    const send = (p: SSE) => { try { reply.raw.write(`data: ${JSON.stringify(p)}\n\n`) } catch { /* client gone */ } }

    // 先把来源发给前端(去重章节)
    const seen = new Set<string>()
    const sources = chunks
      .filter(c => (seen.has(c.section_slug) ? false : (seen.add(c.section_slug), true)))
      .map(c => ({ section_title: c.section_title, section_slug: c.section_slug, chunk_index: c.chunk_index }))
    send({ sources })

    const docSection = chunks.length
      ? chunks.map(c => `[§${c.section_title || '引言'}] ${c.content}`).join('\n\n')
      : '(未检索到相关段落)'
    const system = `${SYSTEM_BASE}\n\n--- 文档参考 ---\n${docSection}`
    const messages: LLMMessage[] = [{ role: 'user', content: message }]

    try {
      const stream = streamChat({ messages, system, tag: 'chat/stream' })
      for await (const text of stream) send({ text })
      send({ done: true })
    } catch (err) {
      app.log.error(err)
      send({ error: err instanceof Error ? err.message : 'Unknown error' })
    } finally {
      try { reply.raw.end() } catch { /* already ended */ }
    }
  })
}
```

- [ ] **Step 2: 端到端手测**

放开 index.ts 中 chat 两行。确保 Task 6 已上传碳酸锂报告,取其 `docId`。
Run:
```bash
pnpm dev:server &
sleep 4
DOC=$(curl -s localhost:3001/api/documents | python3 -c "import sys,json;print(json.load(sys.stdin)['documents'][0]['id'])")
curl -sN -X POST localhost:3001/api/chat/stream \
  -H 'Content-Type: application/json' \
  -d "{\"docId\":\"$DOC\",\"message\":\"碳酸锂的三条铁律是什么?\"}"
```
Expected: 首行 `data: {"sources":[...]}` 含 §1.3 相关章节;随后多行 `data: {"text":...}` 流式输出三条铁律;末尾 `data: {"done":true}`。停掉后台 server。

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: chat 路由 单篇 RAG 流式问答 + 来源 SSE 事件"
```

---

### Task 8: 前端脚手架 —— 路由 + API 客户端 + 类型

**Files:**
- Create: `packages/client/src/types.ts`, `packages/client/src/api.ts`
- Rewrite: `packages/client/src/App.tsx`
- Create: `packages/client/src/pages/HomePage.tsx`, `packages/client/src/pages/ReaderPage.tsx`(本任务占位)

**Interfaces:**
- Produces:
  - types:`Document`、`Source`(`{section_title,section_slug,chunk_index}`)、`ChatMessage`(`{role,content,sources?}`)
  - `api.listDocuments()`、`api.getDocument(id)`、`api.uploadDocument(file)`、`api.deleteDocument(id)`
  - 路由:`/`→HomePage,`/reports/:id`→ReaderPage

- [ ] **Step 1: 类型**

`packages/client/src/types.ts`:
```ts
export interface Document {
  id: string
  filename: string
  size_bytes: number
  chunk_count: number
  created_at: string
}
export interface Source {
  section_title: string
  section_slug: string
  chunk_index: number
}
export interface ChatMessage {
  role: 'user' | 'assistant'
  content: string
  sources?: Source[]
}
```

- [ ] **Step 2: API 客户端**

`packages/client/src/api.ts`:
```ts
import type { Document } from './types'

export const api = {
  async listDocuments(): Promise<Document[]> {
    const r = await fetch('/api/documents')
    if (!r.ok) throw new Error('列表加载失败')
    return (await r.json()).documents
  },
  async getDocument(id: string): Promise<{ document: Document; markdown: string }> {
    const r = await fetch(`/api/documents/${id}`)
    if (!r.ok) throw new Error('报告加载失败')
    return r.json()
  },
  async uploadDocument(file: File): Promise<Document> {
    const fd = new FormData()
    fd.append('file', file)
    const r = await fetch('/api/documents', { method: 'POST', body: fd })
    if (!r.ok) throw new Error((await r.json().catch(() => ({}))).error ?? '上传失败')
    return (await r.json()).document
  },
  async deleteDocument(id: string): Promise<void> {
    const r = await fetch(`/api/documents/${id}`, { method: 'DELETE' })
    if (!r.ok) throw new Error('删除失败')
  },
}
```

- [ ] **Step 3: 路由骨架**

`packages/client/src/App.tsx`:
```tsx
import { Routes, Route } from 'react-router-dom'
import HomePage from './pages/HomePage'
import ReaderPage from './pages/ReaderPage'

export default function App() {
  return (
    <Routes>
      <Route path="/" element={<HomePage />} />
      <Route path="/reports/:id" element={<ReaderPage />} />
    </Routes>
  )
}
```

`packages/client/src/pages/HomePage.tsx`(占位):
```tsx
export default function HomePage() {
  return <div style={{ padding: 24 }}>首页(待实现)</div>
}
```

`packages/client/src/pages/ReaderPage.tsx`(占位):
```tsx
import { useParams } from 'react-router-dom'
export default function ReaderPage() {
  const { id } = useParams()
  return <div style={{ padding: 24 }}>阅读页 {id}(待实现)</div>
}
```

- [ ] **Step 4: 类型检查 + 启动验证**

Run: `pnpm --filter @arh/client exec tsc --noEmit`
Expected: 通过。`pnpm dev:client` 访问 `/` 与 `/reports/x` 分别显示两个占位页。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat: 前端脚手架 路由 + API 客户端 + 类型"
```

---

### Task 9: 首页 —— 报告卡片流 + 上传

**Files:**
- Rewrite: `packages/client/src/pages/HomePage.tsx`
- Create: `packages/client/src/pages/HomePage.module.css`

**Interfaces:**
- Consumes: `api.listDocuments`/`uploadDocument`/`deleteDocument`。
- Produces: 首页组件。上传成功后刷新列表;卡片点击导航 `/reports/:id`。

- [ ] **Step 1: 实现首页**

`packages/client/src/pages/HomePage.tsx`:
```tsx
import { useEffect, useRef, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { api } from '../api'
import type { Document } from '../types'
import styles from './HomePage.module.css'

export default function HomePage() {
  const [docs, setDocs] = useState<Document[]>([])
  const [uploading, setUploading] = useState(false)
  const [error, setError] = useState('')
  const fileRef = useRef<HTMLInputElement>(null)
  const navigate = useNavigate()

  const refresh = () => api.listDocuments().then(setDocs).catch(e => setError(String(e.message)))
  useEffect(() => { refresh() }, [])

  async function onFile(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0]
    if (!file) return
    setUploading(true); setError('')
    try { await api.uploadDocument(file); await refresh() }
    catch (err) { setError(err instanceof Error ? err.message : '上传失败') }
    finally { setUploading(false); if (fileRef.current) fileRef.current.value = '' }
  }

  async function onDelete(e: React.MouseEvent, id: string) {
    e.stopPropagation()
    if (!confirm('删除这篇报告?')) return
    await api.deleteDocument(id); await refresh()
  }

  return (
    <div className={styles.page}>
      <header className={styles.header}>
        <h1>研报站</h1>
        <label className={styles.upload}>
          {uploading ? '上传中…' : '+ 上传研报 (.md)'}
          <input ref={fileRef} type="file" accept=".md,.markdown,.txt" onChange={onFile} hidden />
        </label>
      </header>
      {error && <div className={styles.error}>{error}</div>}
      <div className={styles.grid}>
        {docs.map(d => (
          <article key={d.id} className={styles.card} onClick={() => navigate(`/reports/${d.id}`)}>
            <h2 className={styles.title}>{d.filename}</h2>
            <div className={styles.meta}>
              <span>{new Date(d.created_at).toLocaleDateString('zh-CN')}</span>
              <span>{d.chunk_count} 段</span>
            </div>
            <button className={styles.del} onClick={e => onDelete(e, d.id)}>删除</button>
          </article>
        ))}
        {docs.length === 0 && <p className={styles.empty}>还没有报告,点右上角上传一篇 markdown 研报。</p>}
      </div>
    </div>
  )
}
```

`packages/client/src/pages/HomePage.module.css`:
```css
.page { max-width: 1100px; margin: 0 auto; padding: 32px 24px; }
.header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 28px; }
.header h1 { font-size: 24px; margin: 0; letter-spacing: 2px; }
.upload { cursor: pointer; background: #1a1a1a; color: #fff; padding: 9px 16px; border-radius: 8px; font-size: 14px; }
.error { color: #b00020; margin-bottom: 16px; }
.grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 16px; }
.card { position: relative; background: #fff; border: 1px solid #ececec; border-radius: 12px; padding: 20px; cursor: pointer; transition: box-shadow .15s, transform .15s; }
.card:hover { box-shadow: 0 6px 24px rgba(0,0,0,.08); transform: translateY(-2px); }
.title { font-size: 17px; margin: 0 0 12px; line-height: 1.4; }
.meta { display: flex; gap: 12px; color: #888; font-size: 13px; }
.del { position: absolute; top: 14px; right: 14px; opacity: 0; border: none; background: none; color: #b00020; cursor: pointer; font-size: 12px; }
.card:hover .del { opacity: 1; }
.empty { color: #999; grid-column: 1 / -1; text-align: center; padding: 60px 0; }
```

- [ ] **Step 2: 手测**

Run: `pnpm dev:server &` + `pnpm dev:client`。访问 `/`,点上传选碳酸锂报告 → 卡片出现,显示"碳酸锂产业链投资研究报告"+日期+段数。刷新页面仍在。
Expected: 上传/列表/删除均正常。

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: 首页 报告卡片流 + 上传/删除"
```

---

### Task 10: 阅读页 —— markdown 渲染 + 章节目录(TOC)+ 标题锚点

**Files:**
- Rewrite: `packages/client/src/pages/ReaderPage.tsx`
- Create: `packages/client/src/pages/ReaderPage.module.css`
- Create: `packages/client/src/components/ReportMarkdown.tsx`
- Create: `packages/client/src/lib/toc.ts`
- Test: `packages/client/src/lib/toc.test.ts`

**Interfaces:**
- Consumes: `api.getDocument`;`react-markdown` + `remark-gfm` + `rehype-slug`;`github-slugger`(与后端一致,用于 TOC slug)。
- Produces:
  - `extractToc(md: string): Array<{ level: number; title: string; slug: string }>`(from `toc.ts`)
  - `ReportMarkdown`:渲染 markdown,标题带 rehype-slug 生成的 `id`。

- [ ] **Step 1: TOC 提取 + 测试(TDD)**

`packages/client/src/lib/toc.test.ts`:
```ts
import { test } from 'vitest'
import assert from 'node:assert/strict'
import { extractToc } from './toc'

test('提取标题与 slug,与后端 slugger 同款', () => {
  const md = '# 标题A\n\n正文\n\n## 1.3 大事件的规律与启示\n\n内容'
  const toc = extractToc(md)
  assert.equal(toc.length, 2)
  assert.equal(toc[1].title, '1.3 大事件的规律与启示')
  assert.equal(toc[1].slug, '13-大事件的规律与启示')
  assert.equal(toc[1].level, 2)
})
```

`packages/client/src/lib/toc.ts`:
```ts
import GithubSlugger from 'github-slugger'

const HEADING_RE = /^(#{1,6})\s+(.+?)\s*#*\s*$/

export function extractToc(md: string): Array<{ level: number; title: string; slug: string }> {
  const slugger = new GithubSlugger()
  const out: Array<{ level: number; title: string; slug: string }> = []
  for (const line of md.split('\n')) {
    const m = HEADING_RE.exec(line)
    if (m) out.push({ level: m[1].length, title: m[2].trim(), slug: slugger.slug(m[2].trim()) })
  }
  return out
}
```
> 需把 `github-slugger` 加入 client 依赖:`pnpm --filter @arh/client add github-slugger`。

Run: `pnpm --filter @arh/client test`
Expected: PASS。若 slug 不符,以库输出为准修正断言(与后端 Task 3 同款处理)。

- [ ] **Step 2: markdown 渲染组件**

`packages/client/src/components/ReportMarkdown.tsx`:
```tsx
import Markdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import rehypeSlug from 'rehype-slug'

export default function ReportMarkdown({ markdown }: { markdown: string }) {
  return (
    <Markdown remarkPlugins={[remarkGfm]} rehypePlugins={[rehypeSlug]}>
      {markdown}
    </Markdown>
  )
}
```

- [ ] **Step 3: 阅读页布局**

`packages/client/src/pages/ReaderPage.tsx`:
```tsx
import { useEffect, useState } from 'react'
import { useParams, Link } from 'react-router-dom'
import { api } from '../api'
import { extractToc } from '../lib/toc'
import ReportMarkdown from '../components/ReportMarkdown'
import styles from './ReaderPage.module.css'

export default function ReaderPage() {
  const { id = '' } = useParams()
  const [markdown, setMarkdown] = useState('')
  const [title, setTitle] = useState('')
  const [error, setError] = useState('')

  useEffect(() => {
    api.getDocument(id)
      .then(({ document, markdown }) => { setMarkdown(markdown); setTitle(document.filename) })
      .catch(e => setError(String(e.message)))
  }, [id])

  const toc = extractToc(markdown)

  function jump(slug: string) {
    document.getElementById(slug)?.scrollIntoView({ behavior: 'smooth', block: 'start' })
  }

  if (error) return <div className={styles.page}><Link to="/">← 返回</Link><p>{error}</p></div>

  return (
    <div className={styles.layout}>
      <aside className={styles.toc}>
        <Link to="/" className={styles.back}>← 全部报告</Link>
        <nav>
          {toc.map((t, i) => (
            <a key={i} className={styles.tocItem} style={{ paddingLeft: 8 + (t.level - 1) * 12 }}
               onClick={() => jump(t.slug)}>{t.title}</a>
          ))}
        </nav>
      </aside>
      <main className={styles.content} id="report-content">
        <h1 className={styles.docTitle}>{title}</h1>
        <ReportMarkdown markdown={markdown} />
      </main>
      <aside className={styles.chat} id="chat-slot">{/* Task 11 挂载问答面板 */}</aside>
    </div>
  )
}
```

`packages/client/src/pages/ReaderPage.module.css`:
```css
.layout { display: grid; grid-template-columns: 240px minmax(0, 1fr) 360px; gap: 0; height: 100vh; }
.toc { border-right: 1px solid #eee; padding: 20px 12px; overflow-y: auto; background: #fbfbf9; }
.back { display: block; margin-bottom: 16px; color: #555; text-decoration: none; font-size: 14px; }
.tocItem { display: block; padding: 5px 8px; color: #444; font-size: 13px; cursor: pointer; border-radius: 6px; line-height: 1.4; }
.tocItem:hover { background: #eee; color: #000; }
.content { overflow-y: auto; padding: 40px 56px; max-width: 860px; line-height: 1.75; }
.docTitle { font-size: 26px; margin: 0 0 24px; }
.chat { border-left: 1px solid #eee; display: flex; flex-direction: column; background: #fbfbf9; }
/* 报告排版 */
.content :global(table) { border-collapse: collapse; width: 100%; margin: 16px 0; font-size: 14px; display: block; overflow-x: auto; }
.content :global(th), .content :global(td) { border: 1px solid #ddd; padding: 7px 10px; text-align: left; }
.content :global(th) { background: #f5f5f2; }
.content :global(blockquote) { border-left: 3px solid #c9a227; margin: 16px 0; padding: 4px 16px; color: #555; background: #faf8f0; }
.content :global(pre) { background: #f6f6f4; padding: 14px; border-radius: 8px; overflow-x: auto; font-size: 13px; line-height: 1.5; }
.content :global(h2) { margin-top: 32px; border-bottom: 1px solid #eee; padding-bottom: 6px; }
.content :global(h3) { margin-top: 24px; }
.content :global(:target) { background: #fff6d6; transition: background 1.2s; }
```

- [ ] **Step 4: 手测**

访问某报告 `/reports/:id`:左侧 TOC 列出所有章节,点击平滑滚动到对应标题;表格/引用块/ASCII 走势图(在 ``` 代码块或缩进块中)渲染正常。
Expected: 渲染与跳转正常。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat: 阅读页 markdown 渲染 + TOC + 标题锚点"
```

---

### Task 11: 问答面板 —— SSE 流式对话

**Files:**
- Create: `packages/client/src/components/ChatPanel.tsx`, `packages/client/src/components/ChatPanel.module.css`
- Create: `packages/client/src/hooks/useDocChat.ts`
- Modify: `packages/client/src/pages/ReaderPage.tsx`(挂载 ChatPanel,提供 onCite 回调占位)

**Interfaces:**
- Consumes: `POST /api/chat/stream`。
- Produces:
  - `useDocChat(docId: string)` → `{ messages: ChatMessage[]; send(text: string): Promise<void>; streaming: boolean }`
  - `ChatPanel({ docId, onCite })`:输入框 + 消息列表;`onCite(slug: string)` 供 Task 12 用。

- [ ] **Step 1: SSE hook**

`packages/client/src/hooks/useDocChat.ts`:
```ts
import { useState } from 'react'
import type { ChatMessage, Source } from '../types'

export function useDocChat(docId: string) {
  const [messages, setMessages] = useState<ChatMessage[]>([])
  const [streaming, setStreaming] = useState(false)

  async function send(text: string) {
    if (!text.trim() || streaming) return
    setMessages(m => [...m, { role: 'user', content: text }, { role: 'assistant', content: '' }])
    setStreaming(true)
    try {
      const res = await fetch('/api/chat/stream', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ docId, message: text }),
      })
      if (!res.body) throw new Error('无响应流')
      const reader = res.body.getReader()
      const decoder = new TextDecoder()
      let buf = ''
      for (;;) {
        const { done, value } = await reader.read()
        if (done) break
        buf += decoder.decode(value, { stream: true })
        const parts = buf.split('\n\n')
        buf = parts.pop() ?? ''
        for (const part of parts) {
          const line = part.replace(/^data: /, '').trim()
          if (!line) continue
          const evt = JSON.parse(line) as
            | { sources: Source[] } | { text: string } | { done: true } | { error: string }
          if ('sources' in evt) updateLast(m => ({ ...m, sources: evt.sources }))
          else if ('text' in evt) updateLast(m => ({ ...m, content: m.content + evt.text }))
          else if ('error' in evt) updateLast(m => ({ ...m, content: m.content + `\n\n[出错] ${evt.error}` }))
        }
      }
    } catch (err) {
      updateLast(m => ({ ...m, content: m.content + `\n\n[出错] ${err instanceof Error ? err.message : err}` }))
    } finally {
      setStreaming(false)
    }
  }

  function updateLast(fn: (m: ChatMessage) => ChatMessage) {
    setMessages(prev => {
      const next = [...prev]
      const i = next.length - 1
      if (i >= 0 && next[i].role === 'assistant') next[i] = fn(next[i])
      return next
    })
  }

  return { messages, send, streaming }
}
```

- [ ] **Step 2: 面板组件**

`packages/client/src/components/ChatPanel.tsx`:
```tsx
import { useState } from 'react'
import { useDocChat } from '../hooks/useDocChat'
import Markdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import styles from './ChatPanel.module.css'

export default function ChatPanel({ docId, onCite }: { docId: string; onCite: (slug: string) => void }) {
  const { messages, send, streaming } = useDocChat(docId)
  const [input, setInput] = useState('')

  function submit(e: React.FormEvent) {
    e.preventDefault()
    const text = input.trim()
    if (!text) return
    setInput('')
    void send(text)
  }

  return (
    <div className={styles.panel}>
      <div className={styles.head}>问这篇报告</div>
      <div className={styles.messages}>
        {messages.length === 0 && <p className={styles.hint}>就当前报告提问,答案会标注来源章节。</p>}
        {messages.map((m, i) => (
          <div key={i} className={m.role === 'user' ? styles.user : styles.assistant}>
            {m.role === 'assistant'
              ? <Markdown remarkPlugins={[remarkGfm]}>{m.content || '…'}</Markdown>
              : m.content}
            {m.sources && m.sources.length > 0 && (
              <div className={styles.sources}>
                {m.sources.map((s, j) => (
                  <button key={j} className={styles.chip} onClick={() => onCite(s.section_slug)}>
                    来源 §{s.section_title || '引言'}
                  </button>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
      <form className={styles.inputRow} onSubmit={submit}>
        <input value={input} onChange={e => setInput(e.target.value)}
               placeholder={streaming ? '生成中…' : '输入问题,回车发送'} disabled={streaming} />
        <button type="submit" disabled={streaming || !input.trim()}>发送</button>
      </form>
    </div>
  )
}
```

`packages/client/src/components/ChatPanel.module.css`:
```css
.panel { display: flex; flex-direction: column; height: 100vh; }
.head { padding: 16px; font-weight: 600; border-bottom: 1px solid #eee; }
.messages { flex: 1; overflow-y: auto; padding: 16px; display: flex; flex-direction: column; gap: 14px; }
.hint { color: #999; font-size: 13px; }
.user { align-self: flex-end; background: #1a1a1a; color: #fff; padding: 8px 12px; border-radius: 12px 12px 2px 12px; max-width: 85%; font-size: 14px; }
.assistant { align-self: flex-start; font-size: 14px; line-height: 1.7; max-width: 100%; }
.sources { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 8px; }
.chip { border: 1px solid #c9a227; background: #faf8f0; color: #8a6d1b; border-radius: 999px; padding: 3px 10px; font-size: 12px; cursor: pointer; }
.chip:hover { background: #f3ead0; }
.inputRow { display: flex; gap: 8px; padding: 12px; border-top: 1px solid #eee; }
.inputRow input { flex: 1; padding: 9px 12px; border: 1px solid #ddd; border-radius: 8px; font-size: 14px; }
.inputRow button { padding: 9px 16px; border: none; background: #1a1a1a; color: #fff; border-radius: 8px; cursor: pointer; }
.inputRow button:disabled { opacity: .5; cursor: default; }
```

- [ ] **Step 3: 挂载到阅读页**

修改 `ReaderPage.tsx`:引入 `ChatPanel`,把 `<aside className={styles.chat} id="chat-slot">` 内容替换为 `<ChatPanel docId={id} onCite={jump} />`(`jump` 已在 Task 10 定义为按 slug 滚动)。
```tsx
// 顶部 import
import ChatPanel from '../components/ChatPanel'
// ...
      <aside className={styles.chat}>
        <ChatPanel docId={id} onCite={jump} />
      </aside>
```

- [ ] **Step 4: 手测**

阅读碳酸锂报告,右侧问"三条铁律是什么?" → 流式出现答案,下方出现"来源 §1.3 …"chip。
Expected: 流式对话正常,来源 chip 渲染。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat: 问答面板 SSE 流式对话 + 来源 chip"
```

---

### Task 12: 溯源回链 —— 点击来源滚动到章节并高亮

**Files:**
- Modify: `packages/client/src/pages/ReaderPage.tsx`(强化 `jump`:滚动 + 临时高亮)
- Modify: `packages/client/src/pages/ReaderPage.module.css`(高亮样式)

**Interfaces:**
- Consumes: 章节标题 `id`(rehype-slug 生成)、chip 的 `onCite(slug)`。
- Produces: `jump(slug)` 滚动到 `#slug` 并加 `.flash` 类高亮约 1.6s。

- [ ] **Step 1: 强化 jump**

在 `ReaderPage.tsx` 用下面的实现替换 Task 10 里的 `jump`:
```tsx
function jump(slug: string) {
  const el = document.getElementById(slug)
  if (!el) return
  el.scrollIntoView({ behavior: 'smooth', block: 'start' })
  el.classList.remove('flash')
  // 强制回流以便重复点击能重新触发动画
  void el.offsetWidth
  el.classList.add('flash')
  window.setTimeout(() => el.classList.remove('flash'), 1600)
}
```

- [ ] **Step 2: 高亮样式**

在 `ReaderPage.module.css` 追加(用 `:global` 因为 `flash` 加在 react-markdown 生成的标题上):
```css
.content :global(.flash) { animation: flash 1.6s ease; }
@keyframes flash {
  0% { background: #fff2b8; }
  100% { background: transparent; }
}
```

- [ ] **Step 3: 手测(核心验收)**

阅读碳酸锂报告,问"三条铁律" → 点答案下的"来源 §1.3"chip → 正文平滑滚动到"1.3 大事件的规律与启示"标题并黄色高亮渐隐。再点另一 chip 能重复触发。
Expected: 点击 chip → 滚动 + 高亮,来源与内容确实对应(核对 §1.3 原文正是三条铁律)。

- [ ] **Step 4: 提交**

```bash
git add -A && git commit -m "feat: 溯源回链 点击来源滚动到章节并高亮"
```

---

### Task 13: 排版打磨 + 端到端验收 + README

**Files:**
- Modify: `packages/client/src/pages/ReaderPage.module.css`(★评分/大师语录/ASCII 图细节)
- Create: `README.md`(根,替换 Task 1 的简版)
- Create: `.env`(本地,不提交;确认在 .gitignore)

**Interfaces:** 无新增。

- [ ] **Step 1: 排版细节**

在 `ReaderPage.module.css` 追加:
```css
/* ASCII 走势图/等宽块保持不换行、可横向滚动 */
.content :global(pre) code { white-space: pre; font-family: "SF Mono", Menlo, Consolas, monospace; }
/* 列表与段落间距 */
.content :global(ul), .content :global(ol) { padding-left: 22px; }
.content :global(li) { margin: 4px 0; }
.content :global(p) { margin: 12px 0; }
/* 分隔线 */
.content :global(hr) { border: none; border-top: 1px solid #eee; margin: 28px 0; }
```

- [ ] **Step 2: 全链路端到端验收**

Run:
```bash
docker compose -f docker-compose.dev.yml up chroma -d
pnpm dev:server &   # 确认日志: collection "research_docs"
pnpm dev:client
```
清单(逐项在浏览器核对):
1. 首页上传碳酸锂 + 茅台两篇 → 两张卡片,最新在前。
2. 点茅台 → 阅读页 TOC 含"护城河评估""估值与安全边际"等;表格(五年盈利能力)、★评分、`>` 大师语录块渲染美观。
3. 右侧问"茅台的护城河有哪些?" → 流式答案 + 来源 chip。
4. 点来源 chip → 滚动到对应章节 + 高亮;核对答案内容确实出自该章节。
5. 返回首页删除一篇 → 卡片消失,重进已删报告 404 友好提示。
Expected: 全部通过。发现渲染问题就地微调 CSS。

- [ ] **Step 3: README**

`README.md`:
```markdown
# ai-research-hub — 研报站

本地运行的精致研报阅读站 + 单篇 AI 问答(带溯源回链)。

## 技术栈
- 后端:Fastify + TypeScript(tsx)+ better-sqlite3 + ChromaDB
- 前端:React 19 + Vite + react-markdown + rehype-slug
- AI:智谱 GLM(生成)+ embedding-3(向量)

## 快速开始
```bash
pnpm install
cp packages/server/.env.example packages/server/.env   # 填 ZHIPU_API_KEY
docker compose -f docker-compose.dev.yml up chroma -d
pnpm dev:server   # http://localhost:3001
pnpm dev:client   # http://localhost:5173
```

## 功能
- [x] 上传 markdown 研报,按标题分块索引
- [x] 报告流首页 + 精致阅读页(TOC + 表格/语录/走势图渲染)
- [x] 单篇 RAG 流式问答
- [x] 溯源回链:点击答案来源跳回原文章节并高亮
- [ ] 全库问答 / 上线部署(后续)

设计与计划见 `docs/superpowers/`。
```

- [ ] **Step 4: 最终类型检查 + 测试 + 提交**

Run:
```bash
pnpm typecheck && pnpm test
```
Expected: 全绿。
```bash
git add -A && git commit -m "feat: 排版打磨 + README + 端到端验收通过"
```

---

## Self-Review

**Spec coverage:**
- 定位/本地/无登录 → Task 1、Global Constraints ✅
- 精选上传 md、文件名即展示名 → Task 6 ✅
- 报告流首页 → Task 9 ✅
- 精致阅读页(TOC + markdown) → Task 10 ✅
- 单篇 RAG 问答 → Task 7 + 11 ✅
- 溯源回链(按标题分块 → 章节 slug → chip → 滚动高亮) → Task 3/5/7/11/12 ✅
- 全智谱 provider → Task 2 + Global Constraints ✅
- 复用 docmind 叶子服务 → Task 2/5 ✅
- 非目标(记忆/eval/rerank/登录/全库/公司聚合/Docker 上线)均未纳入 ✅

**Placeholder scan:** 无 TBD/TODO;各步均含完整代码或确切命令。Task 8 的 HomePage/ReaderPage 占位在 Task 9/10 被完整替换,非遗留占位。

**Type consistency:** `MdChunk`(Task 3)字段与 `upsertChunks`(Task 5)消费一致;`DocumentChunk`(Task 2)含 `section_title`/`section_slug`,被 searchChunks(Task 5)产出、chat 路由(Task 7)消费为 `sources`;前端 `Source`(Task 8)字段与后端 SSE `sources` 事件一致;`useDocChat`(Task 11)的事件解析与 chat 路由 SSE 形状一致;`jump(slug)`(Task 10)被 `onCite`(Task 11)与强化版(Task 12)对齐。

**已知集成盲点(手测覆盖,非单测):** ChromaDB 检索质量、SSE 端到端、slug 两侧一致性 —— 分别由 Task 6/7/12 的手测步骤验收。github-slugger 对中文标题的确切输出以库为准,Task 3/10 均注明"以库输出修正断言"。
