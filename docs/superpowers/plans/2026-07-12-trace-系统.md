# 研报站 Trace 系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 docmind 的自研 trace 系统移植进研报站,让每次 `/chat/stream` 问答产生一棵可视化 span 树(检索距离 / prompt / 各阶段耗时 / degraded·error)。

**Architecture:** 后端 `tracing.ts`(AsyncLocalStorage 每请求 tracer)+ `traceStore.ts`(SQLite 两表)+ `/api/traces` 路由,大部分逐字移植自 `~/myCode/docmind`;给现有极简 `chat.ts` 补 `runInTrace` + 3 个 `withSpan` 埋点(唯一新写代码)。前端移植 waterfall 可视化页面(5 组件 + 2 lib + 1 hook),去掉 auth。

**Tech Stack:** Node/Fastify/tsx/better-sqlite3;React19/Vite/react-router;测试 `tsx --test`(后端)、vitest(前端)。

## Global Constraints

- 移植文件除下述明确编辑外**逐字保留**,不重构。
- 无登录:`traces` 路由删除 `requireAdmin`;`runInTrace` 的 `userId` 一律传 `null`。
- 开关:`TRACING`(默认开,`=off` 关闭整套 trace)、`TRACE_CONTENT`(默认开,`=off` 不记 span 的 input/output 正文)。这两个开关在移植的 `tracing.ts` 内已实现,无需新增。
- SQLite 复用现有连接(`services/db.ts` 的 `initDb()`),trace 表随启动 `initTraceTables(db)` 建。
- 前端测试用 vitest `expect`(不用 `node:assert`,client 无 @types/node);测试 import 不带 `.ts` 扩展(client 用 Bundler resolution)。
- 后端测试文件 import 带 `.ts` 扩展(`tsx --test` + NodeNext + allowImportingTsExtensions,已配)。
- docmind 源路径:`~/myCode/docmind/packages`(下称 `$DM`)。

---

### Task 1: 后端 traceStore + tracing(移植)

**Files:**
- Create: `packages/server/src/services/traceStore.ts`(移植 + 改 1 处 import)
- Replace: `packages/server/src/services/tracing.ts`(空壳 → docmind 完整版,逐字)
- Test: `packages/server/src/services/tracing.test.ts`

**Interfaces:**
- Consumes: `DB` from `./db.js`。
- Produces:
  - tracing:`runInTrace(meta:{route:string;userId:string|null}, fn:()=>Promise<T>):Promise<T>`、`withSpan(name:string, fn:()=>Promise<T>):Promise<T>`、`spanInput(text:string)`、`spanOutput(text:string)`、`spanMeta(key:string,value:unknown)`、`markDegraded(reason:string, meta?:Record<string,unknown>)`、`appendSpanLate(...)`、`currentTraceId():string|null`、类型 `SpanRecord`、`TraceRecord`、`SpanStatus`。
  - traceStore:`initTraceTables(db:DB)`、`saveTrace(trace,spans)`、`appendSpan(span)`、`getTrace(id):{trace,spans}|null`、`listTraces(f):TraceRecord[]`、`traceStats(f):{total,degradedPct,byReason}`。

- [ ] **Step 1: 移植两个文件**

Run:
```bash
DM=~/myCode/docmind/packages
S=packages/server/src
cp "$DM/server/src/services/traceStore.ts" "$S/services/traceStore.ts"
cp "$DM/server/src/services/tracing.ts"    "$S/services/tracing.ts"   # 覆盖空壳
```

- [ ] **Step 2: 修 traceStore 的 DB import**

在 `packages/server/src/services/traceStore.ts` 顶部,把
```ts
import type { DB } from './memoryStore.js'
```
改为
```ts
import type { DB } from './db.js'
```
(该文件其余部分不动。)

- [ ] **Step 3: 写 round-trip 测试**

`packages/server/src/services/tracing.test.ts`:
```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import Database from 'better-sqlite3'
import { initTraceTables, listTraces, getTrace } from './traceStore.ts'
import { runInTrace, withSpan, spanMeta, markDegraded } from './tracing.ts'

test('runInTrace + withSpan 落库,可查回带 metadata 与降级状态', async () => {
  const db = new Database(':memory:')
  initTraceTables(db)

  await runInTrace({ route: '/chat/stream', userId: null }, async () => {
    await withSpan('doc_retrieval', async () => {
      spanMeta('kept', 3)
      markDegraded('doc_retrieval_minK', { topDistance: 0.71 })
    })
    await withSpan('llm_generation', async () => {
      spanMeta('provider', 'zhipu')
    })
  })

  const traces = listTraces({})
  assert.equal(traces.length, 1)
  assert.equal(traces[0].route, '/chat/stream')
  assert.equal(traces[0].status, 'degraded')      // markDegraded 上卷
  assert.equal(traces[0].span_count, 2)

  const detail = getTrace(traces[0].id)!
  assert.equal(detail.spans.length, 2)
  const ret = detail.spans.find(s => s.name === 'doc_retrieval')!
  assert.equal(ret.status, 'degraded')
  assert.equal(ret.degraded_reason, 'doc_retrieval_minK')
  assert.ok(ret.metadata.includes('kept'))
})
```

- [ ] **Step 4: 运行测试**

Run: `pnpm --filter @arh/server exec tsx --test src/services/tracing.test.ts`
Expected: PASS(1 test)。若因 `TRACING=off` 环境变量导致不落库,确保运行环境未设 `TRACING=off`。

- [ ] **Step 5: 全量类型检查**

Run: `pnpm --filter @arh/server exec tsc --noEmit`
Expected: 0 errors。注:`llm.ts` / `documentVector.ts` 之前 import 的 `markDegraded` 现在来自真实 `tracing.ts`,签名 `(reason:string, meta?:Record<string,unknown>)` 与调用点兼容。

- [ ] **Step 6: 提交**

```bash
git add packages/server/src/services/traceStore.ts packages/server/src/services/tracing.ts packages/server/src/services/tracing.test.ts
git commit -m "feat(trace): 移植 tracing + traceStore(SQLite span 树落库)"
```

---

### Task 2: traces 路由 + index 装配

**Files:**
- Create: `packages/server/src/routes/traces.ts`(移植,去 auth)
- Modify: `packages/server/src/index.ts`

**Interfaces:**
- Consumes: `listTraces`/`getTrace`/`traceStats`(traceStore)、`initTraceTables`。
- Produces:`GET /api/traces`、`GET /api/traces/stats`、`GET /api/traces/:id`。

- [ ] **Step 1: 写路由(去 requireAdmin)**

`packages/server/src/routes/traces.ts`:
```ts
// packages/server/src/routes/traces.ts
import type { FastifyPluginAsync } from 'fastify'
import { listTraces, getTrace, traceStats } from '../services/traceStore.js'

export const traceRoutes: FastifyPluginAsync = async (app) => {
  app.get<{ Querystring: { status?: string; route?: string; limit?: string } }>(
    '/traces', async (req) => {
      const { status, route, limit } = req.query
      return { traces: listTraces({ status, route, limit: limit ? Number(limit) : undefined }) }
    })

  // 注册在 /traces/:id 之前,避免 "stats" 被当作 :id。
  app.get<{ Querystring: { route?: string } }>('/traces/stats', async (req) => {
    return traceStats({ route: req.query.route })
  })

  app.get<{ Params: { id: string } }>('/traces/:id', async (req, reply) => {
    const found = getTrace(req.params.id)
    if (!found) return reply.code(404).send({ error: 'trace not found' })
    return found
  })
}
```

- [ ] **Step 2: 装配 index.ts**

在 `packages/server/src/index.ts`:
- import 段加:
```ts
import { initTraceTables } from './services/traceStore.js'
import { traceRoutes } from './routes/traces.js'
```
- `initDocumentTable(db)` 之后加:
```ts
initTraceTables(db)
```
- 注册 documentRoutes/chatRoutes 之后加:
```ts
await app.register(traceRoutes, { prefix: '/api' })
```

- [ ] **Step 3: 类型检查 + 空列表验证**

Run:
```bash
pnpm --filter @arh/server exec tsc --noEmit
```
Expected: 0 errors。

启动后端(需 `.env` 里已有 ZHIPU_API_KEY;ChromaDB 可选)并验证空列表:
```bash
(pnpm --filter @arh/server start >/tmp/arh.log 2>&1 &)
for i in $(seq 1 25); do curl -s localhost:3001/health >/dev/null 2>&1 && break; sleep 1; done
curl -s localhost:3001/api/traces
```
Expected: `{"traces":[]}`(尚无问答)。停掉后台进程。

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/routes/traces.ts packages/server/src/index.ts
git commit -m "feat(trace): /api/traces 路由(去登录鉴权)+ index 装配"
```

---

### Task 3: chat.ts 埋点(runInTrace + 3 span,唯一新写)

**Files:**
- Modify: `packages/server/src/routes/chat.ts`

**Interfaces:**
- Consumes: `runInTrace`/`withSpan`/`spanInput`/`spanOutput`/`spanMeta`/`markDegraded`(tracing)。
- Produces: 每次 `/chat/stream` 生成一条 trace,含 `doc_retrieval`(kept/distances/sections)、`prompt_assembly`(finalTokens/chunkCount)、`llm_generation`(provider/ttfbMs/outputTokens)三个 span。

- [ ] **Step 1: 用带埋点的版本替换整个 chat.ts**

`packages/server/src/routes/chat.ts`:
```ts
// packages/server/src/routes/chat.ts
import type { FastifyPluginAsync } from 'fastify'
import { streamChat, PROVIDER } from '../llm.js'
import { searchChunks, isDocVectorAvailable } from '../services/documentVector.js'
import { getDocument } from '../services/documentStore.js'
import { runInTrace, withSpan, spanInput, spanOutput, spanMeta, markDegraded } from '../services/tracing.js'
import type { LLMMessage } from '../types.js'
import type { DocumentChunk } from '../types.js'

const SYSTEM_BASE = `你是投研报告阅读助手。只依据"文档参考"中的内容回答用户关于当前这篇研报的问题。
规则:
- 严格基于文档参考,不要编造数字或结论;文档中没有的,明确说"报告中未提及"。
- 回答用中文,简洁、分点。
- 在引用具体结论时,用【来源:§章节名】标注它出自哪一节(章节名取文档参考中给出的)。`

interface StreamBody {
  docId: string
  message: string
}

type SSE =
  | { sources: Array<{ section_title: string; section_slug: string; chunk_index: number }> }
  | { text: string }
  | { done: true }
  | { error: string }

function estimateTokens(text: string): number {
  return Math.ceil((text || '').length / 3)
}

export const chatRoutes: FastifyPluginAsync = async (app) => {
  app.get('/chat/health', async () => ({ status: 'ok', provider: PROVIDER }))

  app.post<{ Body: StreamBody }>('/chat/stream', async (request, reply) => {
    const { docId, message } = request.body ?? ({} as StreamBody)
    if (!docId) return reply.status(400).send({ error: 'docId is required' })
    if (!message?.trim()) return reply.status(400).send({ error: 'message is required' })
    if (!getDocument(docId)) return reply.status(404).send({ error: 'document not found' })

    await runInTrace({ route: '/chat/stream', userId: null }, async () => {
      // 1) 检索
      const chunks: DocumentChunk[] = await withSpan('doc_retrieval', async () => {
        spanInput(message)
        const found = isDocVectorAvailable() ? await searchChunks(message, docId) : []
        if (!found.length) markDegraded('doc_retrieval_empty')
        spanMeta('kept', found.length)
        spanMeta('distances', found.map(c => Number(c.distance.toFixed(4))))
        spanMeta('sections', found.map(c => c.section_title))
        spanOutput(found.map(c => `[§${c.section_title}] ${c.content}`).join('\n\n'))
        return found
      })

      reply.raw.writeHead(200, {
        'Content-Type': 'text/event-stream',
        'Cache-Control': 'no-cache',
        'Connection': 'keep-alive',
        'X-Accel-Buffering': 'no',
      })
      const send = (p: SSE) => { try { reply.raw.write(`data: ${JSON.stringify(p)}\n\n`) } catch { /* client gone */ } }

      // 来源(去重章节)
      const seen = new Set<string>()
      const sources = chunks
        .filter(c => (seen.has(c.section_slug) ? false : (seen.add(c.section_slug), true)))
        .map(c => ({ section_title: c.section_title, section_slug: c.section_slug, chunk_index: c.chunk_index }))
      send({ sources })

      // 2) 组装 prompt
      const system = await withSpan('prompt_assembly', async () => {
        const docSection = chunks.length
          ? chunks.map(c => `[§${c.section_title || '引言'}] ${c.content}`).join('\n\n')
          : '(未检索到相关段落)'
        const finalSystem = `${SYSTEM_BASE}\n\n--- 文档参考 ---\n${docSection}`
        spanMeta('chunkCount', chunks.length)
        spanMeta('finalTokens', estimateTokens(finalSystem))
        return finalSystem
      })

      const messages: LLMMessage[] = [{ role: 'user', content: message }]

      // 3) 生成
      await withSpan('llm_generation', async () => {
        spanMeta('provider', PROVIDER)
        const t0 = performance.now()
        let firstAt = 0
        let out = ''
        try {
          const stream = streamChat({ messages, system, tag: 'chat/stream' })
          for await (const text of stream) {
            if (!firstAt) { firstAt = performance.now(); spanMeta('ttfbMs', Math.round(firstAt - t0)) }
            out += text
            send({ text })
          }
          spanMeta('outputTokens', estimateTokens(out))
          spanOutput(out)
          send({ done: true })
        } catch (err) {
          app.log.error(err)
          send({ error: err instanceof Error ? err.message : 'Unknown error' })
          throw err   // 让 withSpan 记为 error 状态
        } finally {
          try { reply.raw.end() } catch { /* already ended */ }
        }
      })
    }).catch(() => { /* 错误已通过 SSE 告知 + 记入 trace */ })
  })
}
```

- [ ] **Step 2: 类型检查**

Run: `pnpm --filter @arh/server exec tsc --noEmit`
Expected: 0 errors。

- [ ] **Step 3: 端到端手测(需 ZHIPU_API_KEY + ChromaDB + 已上传报告)**

```bash
docker compose -f docker-compose.dev.yml up chroma -d   # 若 8000 已有 chroma 可跳过
(pnpm --filter @arh/server start >/tmp/arh.log 2>&1 &)
for i in $(seq 1 25); do curl -s localhost:3001/health >/dev/null 2>&1 && break; sleep 1; done
DOC=$(curl -s localhost:3001/api/documents | python3 -c "import sys,json;print(json.load(sys.stdin)['documents'][0]['id'])")
curl -sN -X POST localhost:3001/api/chat/stream -H 'Content-Type: application/json' \
  -d "{\"docId\":\"$DOC\",\"message\":\"三条铁律是什么?\"}" >/dev/null
echo "--- trace 列表 ---" && curl -s localhost:3001/api/traces
TID=$(curl -s localhost:3001/api/traces | python3 -c "import sys,json;print(json.load(sys.stdin)['traces'][0]['id'])")
echo "--- trace 详情 span 名与 metadata ---"
curl -s localhost:3001/api/traces/$TID | python3 -c "import sys,json;d=json.load(sys.stdin);[print(s['name'],s['metadata']) for s in d['spans']]"
```
Expected: 列表有 1 条 `/chat/stream` trace;详情 3 个 span:`doc_retrieval`(metadata 含 `kept`/`distances`/`sections`)、`prompt_assembly`(含 `finalTokens`/`chunkCount`)、`llm_generation`(含 `provider`/`ttfbMs`/`outputTokens`)。停掉后台进程。

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/routes/chat.ts
git commit -m "feat(trace): chat/stream 埋点(doc_retrieval/prompt_assembly/llm_generation + 检索距离)"
```

---

### Task 4: 前端 trace 类型 + waterfall + statBars(移植 + 单测)

**Files:**
- Modify: `packages/client/src/types.ts`(追加 trace 类型)
- Create: `packages/client/src/lib/waterfall.ts`(移植)
- Create: `packages/client/src/lib/statBars.ts`(移植)
- Test: `packages/client/src/lib/waterfall.test.ts`、`packages/client/src/lib/statBars.test.ts`(改写为 vitest expect)

**Interfaces:**
- Produces:
  - types:`TraceStatus`、`TraceRecord`、`SpanRecord`、`WaterfallRow`。
  - `buildWaterfall(spans:SpanRecord[], totalMs:number):WaterfallRow[]`
  - `statBars(byReason:Record<string,number>):StatBar[]`(`StatBar={reason:string;count:number;pct:number}`)

- [ ] **Step 1: 追加 trace 类型**

在 `packages/client/src/types.ts` 末尾追加:
```ts
export type TraceStatus = 'ok' | 'degraded' | 'error'

export interface TraceRecord {
  id: string
  route: string
  user_id: string | null
  status: TraceStatus
  duration_ms: number
  span_count: number
  degraded_count: number
  error_count: number
  started_at: string
  created_at: string
}

export interface SpanRecord {
  id: string
  trace_id: string
  parent_span_id: string | null
  name: string
  status: TraceStatus
  start_offset_ms: number
  duration_ms: number
  degraded_reason: string | null
  input: string | null
  output: string | null
  /** JSON 字符串 */
  metadata: string
  error_message: string | null
}

export interface WaterfallRow {
  span: SpanRecord
  depth: number
  leftPct: number
  widthPct: number
}
```

- [ ] **Step 2: 移植 waterfall.ts + statBars.ts**

Run:
```bash
DM=~/myCode/docmind/packages
C=packages/client/src
cp "$DM/client/src/lib/waterfall.ts" "$C/lib/waterfall.ts"
cp "$DM/client/src/lib/statBars.ts"  "$C/lib/statBars.ts"
```
然后把 `packages/client/src/lib/waterfall.ts` 顶部的
```ts
import type { SpanRecord, WaterfallRow } from '../types.ts'
```
改为(去掉 `.ts` 扩展,与本仓库 client 风格一致):
```ts
import type { SpanRecord, WaterfallRow } from '../types'
```

- [ ] **Step 3: 写 vitest 测试**

`packages/client/src/lib/waterfall.test.ts`:
```ts
import { test, expect } from 'vitest'
import { buildWaterfall } from './waterfall'
import type { SpanRecord } from '../types'

function span(p: Partial<SpanRecord>): SpanRecord {
  return {
    id: 'sp', trace_id: 'tr', parent_span_id: null, name: 'x',
    status: 'ok', start_offset_ms: 0, duration_ms: 0,
    degraded_reason: null, input: null, output: null,
    metadata: '{}', error_message: null, ...p,
  }
}

test('单个根 span:depth=0,按 total 计算百分比', () => {
  const rows = buildWaterfall([span({ id: 'a', start_offset_ms: 0, duration_ms: 50 })], 100)
  expect(rows.length).toBe(1)
  expect(rows[0].depth).toBe(0)
  expect(rows[0].leftPct).toBe(0)
  expect(rows[0].widthPct).toBe(50)
})

test('子 span:depth 随 parent 链递增', () => {
  const spans = [
    span({ id: 'a', start_offset_ms: 0, duration_ms: 100 }),
    span({ id: 'b', parent_span_id: 'a', start_offset_ms: 10, duration_ms: 20 }),
    span({ id: 'c', parent_span_id: 'b', start_offset_ms: 12, duration_ms: 5 }),
  ]
  const rows = buildWaterfall(spans, 100)
  expect(rows[1].depth).toBe(1)
  expect(rows[1].leftPct).toBe(10)
  expect(rows[2].depth).toBe(2)
})

test('totalMs<=0 回退到 max(offset+duration)', () => {
  const rows = buildWaterfall([
    span({ id: 'a', start_offset_ms: 0, duration_ms: 40 }),
    span({ id: 'b', start_offset_ms: 60, duration_ms: 40 }),
  ], 0)
  expect(rows[1].leftPct).toBe(60)
  expect(rows[1].widthPct).toBe(40)
})

test('duration_ms=0 给最小可见宽度', () => {
  const rows = buildWaterfall([span({ id: 'a', start_offset_ms: 10, duration_ms: 0 })], 100)
  expect(rows[0].widthPct).toBe(0.5)
})

test('空数组返回空数组', () => {
  expect(buildWaterfall([], 100)).toEqual([])
})
```

`packages/client/src/lib/statBars.test.ts`:
```ts
import { test, expect } from 'vitest'
import { statBars } from './statBars'

test('按次数降序,pct 相对最大值归一化', () => {
  const bars = statBars({ a: 1, b: 4, c: 2 })
  expect(bars.map(x => x.reason)).toEqual(['b', 'c', 'a'])
  expect(bars[0].pct).toBe(100)
  expect(bars[1].pct).toBe(50)
})

test('空对象返回空数组', () => {
  expect(statBars({})).toEqual([])
})
```

- [ ] **Step 4: 运行前端测试**

Run: `pnpm --filter @arh/client test`
Expected: 全部通过(toc 1 + waterfall 5 + statBars 2 = 8 tests)。

- [ ] **Step 5: 提交**

```bash
git add packages/client/src/types.ts packages/client/src/lib/waterfall.ts packages/client/src/lib/statBars.ts packages/client/src/lib/waterfall.test.ts packages/client/src/lib/statBars.test.ts
git commit -m "feat(trace): 前端 trace 类型 + waterfall/statBars(移植 + vitest)"
```

---

### Task 5: 前端 useTraces + 5 组件(移植)

**Files:**
- Create: `packages/client/src/hooks/useTraces.ts`(移植)
- Create: `packages/client/src/components/TracesPage.tsx` + `.module.css`
- Create: `packages/client/src/components/TraceList.tsx` + `.module.css`
- Create: `packages/client/src/components/TraceStats.tsx` + `.module.css`
- Create: `packages/client/src/components/TraceDetailPage.tsx` + `.module.css`
- Create: `packages/client/src/components/SpanWaterfall.tsx` + `.module.css`

**Interfaces:**
- Consumes: types(Task 4)、`buildWaterfall`、`statBars`。
- Produces: `useTraces()`、`TracesPage`、`TraceDetailPage`(+ 内部 TraceList/TraceStats/SpanWaterfall)。

- [ ] **Step 1: 逐字移植 hook + 5 组件 + 各自 CSS**

Run:
```bash
DM=~/myCode/docmind/packages/client/src
C=packages/client/src
cp "$DM/hooks/useTraces.ts" "$C/hooks/useTraces.ts"
for n in TracesPage TraceList TraceStats TraceDetailPage SpanWaterfall; do
  cp "$DM/components/$n.tsx" "$C/components/$n.tsx"
  cp "$DM/components/$n.module.css" "$C/components/$n.module.css"
done
```
这些文件不引用 auth / userId,直接可用(TraceRecord 里的 `user_id` 字段仅展示、研报站恒为 null)。

- [ ] **Step 2: 类型检查 + 构建**

Run:
```bash
pnpm --filter @arh/client exec tsc --noEmit
pnpm --filter @arh/client build
```
Expected: 均通过。若 tsc 报某组件 import 带 `.ts`/`.tsx` 扩展的类型,去掉扩展即可(本仓库 client 用 Bundler resolution,import 一律不带扩展)。

- [ ] **Step 3: 提交**

```bash
git add packages/client/src/hooks/useTraces.ts packages/client/src/components/Trace*.tsx packages/client/src/components/Trace*.module.css packages/client/src/components/SpanWaterfall.tsx packages/client/src/components/SpanWaterfall.module.css
git commit -m "feat(trace): 前端 useTraces + waterfall 可视化组件(移植)"
```

---

### Task 6: 路由 + 首页入口

**Files:**
- Modify: `packages/client/src/App.tsx`
- Modify: `packages/client/src/pages/HomePage.tsx`、`packages/client/src/pages/HomePage.module.css`

**Interfaces:**
- Consumes: `TracesPage`、`TraceDetailPage`。
- Produces: `/traces`、`/traces/:id` 路由 + 首页右上角 trace 入口。

- [ ] **Step 1: 加路由**

`packages/client/src/App.tsx`:
```tsx
import { Routes, Route } from 'react-router-dom'
import HomePage from './pages/HomePage'
import ReaderPage from './pages/ReaderPage'
import { TracesPage } from './components/TracesPage'
import { TraceDetailPage } from './components/TraceDetailPage'

export default function App() {
  return (
    <Routes>
      <Route path="/" element={<HomePage />} />
      <Route path="/reports/:id" element={<ReaderPage />} />
      <Route path="/traces" element={<TracesPage />} />
      <Route path="/traces/:id" element={<TraceDetailPage />} />
    </Routes>
  )
}
```

- [ ] **Step 2: 首页加入口链接**

在 `packages/client/src/pages/HomePage.tsx`:
- 顶部 import 加:`import { Link } from 'react-router-dom'`(若已从 react-router-dom 引入 `useNavigate`,合并为 `import { useNavigate, Link } from 'react-router-dom'`)。
- header 里 `<h1>研报站</h1>` 与上传 label 之间,插入 trace 链接。把 header 改为:
```tsx
      <header className={styles.header}>
        <h1>研报站</h1>
        <div className={styles.headerRight}>
          <Link to="/traces" className={styles.traceLink}>🔍 trace</Link>
          <label className={styles.upload}>
            {uploading ? '上传中…' : '+ 上传研报 (.md)'}
            <input ref={fileRef} type="file" accept=".md,.markdown,.txt" onChange={onFile} hidden />
          </label>
        </div>
      </header>
```

- [ ] **Step 3: 入口样式**

在 `packages/client/src/pages/HomePage.module.css` 追加:
```css
.headerRight { display: flex; align-items: center; gap: 14px; }
.traceLink { color: #555; text-decoration: none; font-size: 14px; padding: 8px 12px; border: 1px solid #e0e0e0; border-radius: 8px; }
.traceLink:hover { color: #000; background: #f3f3f0; }
```

- [ ] **Step 4: 类型检查 + 构建**

Run:
```bash
pnpm --filter @arh/client exec tsc --noEmit && pnpm --filter @arh/client build
```
Expected: 均通过。

- [ ] **Step 5: 提交**

```bash
git add packages/client/src/App.tsx packages/client/src/pages/HomePage.tsx packages/client/src/pages/HomePage.module.css
git commit -m "feat(trace): /traces 路由 + 首页入口"
```

---

### Task 7: 端到端验收 + 开关验证

**Files:** 无(纯验证)。

- [ ] **Step 1: 全链路手测**

```bash
docker compose -f docker-compose.dev.yml up chroma -d   # 8000 已有则跳过
pnpm dev:server   # 终端1
pnpm dev:client   # 终端2
```
浏览器核对:
1. 打开某报告,右侧问一个问题(如"三条铁律")。
2. 回首页,点右上角 "🔍 trace" → `/traces` 列表出现刚才这条 `/chat/stream`,状态/耗时/span 数正确。
3. 点进去 → waterfall 三条 span(doc_retrieval / prompt_assembly / llm_generation),点 `doc_retrieval` 展开 → metadata 里能看到 `distances`(各命中块 cosine 距离)与 `sections`;点 `llm_generation` → 看到 `ttfbMs`、`outputTokens`。
Expected: 全部符合。

- [ ] **Step 2: 开关验证**

```bash
# TRACING=off:不落库
TRACING=off pnpm --filter @arh/server start &   # 或临时写入 .env
# 问一次后:
curl -s localhost:3001/api/traces   # 期望 {"traces":[]}(该次不记)
```
再测 `TRACE_CONTENT=off`:重启(不带 TRACING=off,带 TRACE_CONTENT=off)→ 问一次 → trace 详情里 span 的 `input`/`output` 为 null,但 metadata(距离等)仍在。
Expected: 两个开关行为符合。清理:从 .env 移除临时开关。

- [ ] **Step 3: 后端全测 + 提交(若前面有零散未提交的验证脚本产物,确认工作树干净)**

Run:
```bash
pnpm test && pnpm typecheck
```
Expected: 全绿(后端 tracing round-trip + markdownParser + documentStore;前端 toc + waterfall + statBars)。
```bash
git status --porcelain   # 应为空
```

---

## Self-Review

**Spec coverage:**
- tracing.ts 全量(runInTrace/withSpan/spanMeta/markDegraded 等)→ Task 1 ✅
- traceStore SQLite 两表 → Task 1 ✅
- traces 路由去 auth → Task 2 ✅
- index 装配 → Task 2 ✅
- chat.ts 三 span + 检索距离埋点 → Task 3 ✅
- 前端 waterfall.ts + statBars + 类型 → Task 4 ✅
- useTraces + 5 组件移植 → Task 5 ✅
- 路由 + 首页入口 → Task 6 ✅
- 开关 TRACING/TRACE_CONTENT 验证 → Task 7 ✅
- 非目标(第三方服务、告警、采样)均未纳入 ✅

**Placeholder scan:** 无 TBD/TODO;移植文件用 cp + 明确 edit,新写的 chat.ts / 测试 / 类型均为完整代码。

**Type consistency:** 后端 `SpanRecord`/`TraceRecord`(tracing.ts 导出)与前端 `types.ts`(Task 4)字段一致;`markDegraded(reason:string, meta?)` 签名与既有 llm.ts/documentVector.ts 调用兼容(Task 1 Step 5 校验);`runInTrace({route,userId})`、`withSpan(name, fn)`、`spanMeta(key,value)` 在 Task 3 的调用与 Task 1 的定义一致;`buildWaterfall`/`statBars` 在 Task 5 组件的用法与 Task 4 定义一致。

**已知集成盲点(手测覆盖):** trace 落库依赖真实 SQLite(Task 1 用 :memory: 单测覆盖);SSE + trace flush 时序、检索距离 metadata、开关行为由 Task 3/7 手测覆盖。
