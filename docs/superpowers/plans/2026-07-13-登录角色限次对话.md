# 登录 + 角色 + 限次 + 对话保存(子系统 A)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给研报站加 GitHub 登录与三类身份(匿名可读、登录可问、管理员可管),问答限次(每人 10 次 + 全局总上限),并按 (用户,报告) 保存对话。

**Architecture:** 后端移植 docmind 的 `userStore`/`auth`(GitHub OAuth + 签名 cookie,`AUTH_DISABLED` 本地逃生口),新增 `usageStore`(全局计数)与精简 `chatStore`(按 user+doc 存一条线程);现有路由加 `requireUser`/`requireAdmin` 守卫 + 限次。前端加 `useAuth` + 头部登录态 + 按角色隐藏管理 UI + 问答区登录引导 + 历史恢复。

**Tech Stack:** Fastify + @fastify/cookie + better-sqlite3;React19 + react-router;测试 `tsx --test`(后端)、vitest(前端)。

## Global Constraints

- 移植文件除明确编辑外逐字保留;docmind 源在 `~/myCode/docmind/packages`(下称 `$DM`)。
- 访问模型:匿名(读)/ 登录普通用户(读+问,每人 `MESSAGE_LIMIT` 默认 10 次)/ 管理员(全部,无限)。全局 `GLOBAL_LIMIT` 默认 1000,普通用户超个人或全局即拒,管理员豁免。
- 管理员认定:手动改 DB(`UPDATE users SET is_admin=1 WHERE username='...'`),无写 API。
- 门禁:`POST/DELETE /api/documents` 与 `/api/traces*` → `requireAdmin`;`GET /api/documents*` → 公开;`POST /api/chat/stream`、`GET /api/chat/messages` → `requireUser`。
- 本地开发/测试用 `AUTH_DISABLED=true`(所有请求 = 合成管理员 DEV_USER,免 OAuth)。
- 对话按 `(user_id, doc_id)` 一条线程;匿名不落库不加载。
- env(server `.env`):`GITHUB_CLIENT_ID`、`GITHUB_CLIENT_SECRET`、`APP_URL`、`COOKIE_SECRET`、`AUTH_DISABLED`、`MESSAGE_LIMIT`、`GLOBAL_LIMIT`。
- 后端测试 import 带 `.ts` 扩展;前端测试用 vitest `expect`、import 不带扩展。

---

### Task 1: 后端 userStore + auth 路由 + cookie 装配

**Files:**
- Create: `packages/server/src/services/userStore.ts`(移植 + 改 DB import)
- Create: `packages/server/src/routes/auth.ts`(移植)
- Modify: `packages/server/src/index.ts`(注册 cookie + authRoutes + initUserTables)
- Modify: `packages/server/package.json`(加 `@fastify/cookie`)
- Modify: `packages/server/.env.example`(加 auth env)

**Interfaces:**
- Produces:
  - userStore:`initUserTables(db:DB)`、`upsertGithubUser({githubId,username,avatarUrl}):User`、`getUserById(id):User|null`、`incrementMessageCount(id)`、`canSend(user):boolean`、`remaining(user):number|null`、`MESSAGE_LIMIT:number`、类型 `User`。
  - auth:`currentUser(req):User|null`、`requireUser(req,reply):User|null`、`requireAdmin(req,reply):User|null`、`authRoutes` 插件(`/api/auth/github`、`/callback`、`/me`、`/logout`)。

- [ ] **Step 1: 移植两个文件 + 改 DB import**

Run:
```bash
DM=~/myCode/docmind/packages; S=packages/server/src
cp "$DM/server/src/services/userStore.ts" "$S/services/userStore.ts"
cp "$DM/server/src/routes/auth.ts"        "$S/routes/auth.ts"
perl -0pi -e "s{import type \{ DB \} from './memoryStore.js'}{import type { DB } from './db.js'}" "$S/services/userStore.ts"
grep -n "import type { DB }" "$S/services/userStore.ts"
```
Expected: 打印 `./db.js`。

- [ ] **Step 2: 加 @fastify/cookie 依赖**

在 `packages/server/package.json` 的 dependencies 里加(与 docmind 同版本):
```json
    "@fastify/cookie": "^11.0.2",
```
然后 `pnpm install`。
Expected: 安装成功。

- [ ] **Step 3: 装配 index.ts**

在 `packages/server/src/index.ts`:
- import 段加:
```ts
import cookie from '@fastify/cookie'
import { randomBytes } from 'node:crypto'
import { initUserTables } from './services/userStore.js'
import { authRoutes } from './routes/auth.js'
```
- 在 `await app.register(multipart, ...)` 之后加(cookie 插件):
```ts
const cookieSecret = process.env.COOKIE_SECRET
if (!cookieSecret) app.log.warn('COOKIE_SECRET 未设 — 用随机密钥,重启后登录态失效')
await app.register(cookie, { secret: cookieSecret || randomBytes(32).toString('hex') })
```
- 在 `initTraceTables(db)` 之后加:`initUserTables(db)`
- 在注册 documentRoutes 之前加:`await app.register(authRoutes, { prefix: '/api' })`

- [ ] **Step 4: 补 .env.example**

在 `packages/server/.env.example` 末尾追加:
```
# 认证(GitHub OAuth);本地开发可设 AUTH_DISABLED=true 跳过登录(所有人=管理员)
AUTH_DISABLED=true
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
APP_URL=http://localhost:5173
COOKIE_SECRET=
# 限次
MESSAGE_LIMIT=10
GLOBAL_LIMIT=1000
```

- [ ] **Step 5: 类型检查 + AUTH_DISABLED 冒烟**

Run:
```bash
pnpm --filter @arh/server exec tsc --noEmit
lsof -tiTCP:3001 -sTCP:LISTEN | xargs -r kill -9; sleep 1
(AUTH_DISABLED=true pnpm --filter @arh/server start >/tmp/a.log 2>&1 &)
for i in $(seq 1 25); do curl -s localhost:3001/health >/dev/null 2>&1 && break; sleep 1; done
curl -s localhost:3001/api/auth/me
```
Expected: typecheck 0 错;`/api/auth/me` 返回合成管理员 JSON(`"username":"dev","isAdmin":true`)。停掉后台。

- [ ] **Step 6: 提交**

```bash
git add packages/server/src/services/userStore.ts packages/server/src/routes/auth.ts packages/server/src/index.ts packages/server/package.json packages/server/.env.example pnpm-lock.yaml
git commit -m "feat(auth): 移植 userStore + GitHub OAuth auth 路由 + cookie 装配"
```

---

### Task 2: usageStore(全局计数)

**Files:**
- Create: `packages/server/src/services/usageStore.ts`
- Test: `packages/server/src/services/usageStore.test.ts`
- Modify: `packages/server/src/index.ts`(initUsageTable)

**Interfaces:**
- Consumes: `DB` from `./db.js`。
- Produces:`initUsageTable(db:DB)`、`getGlobalCount():number`、`incrementGlobalCount():void`、`globalRemaining():number`、`GLOBAL_LIMIT:number`。

- [ ] **Step 1: 写失败测试**

`packages/server/src/services/usageStore.test.ts`:
```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import Database from 'better-sqlite3'
import { initUsageTable, getGlobalCount, incrementGlobalCount, globalRemaining, GLOBAL_LIMIT } from './usageStore.ts'

test('全局计数:初始 0,自增,剩余递减', () => {
  const db = new Database(':memory:')
  initUsageTable(db)
  assert.equal(getGlobalCount(), 0)
  assert.equal(globalRemaining(), GLOBAL_LIMIT)
  incrementGlobalCount()
  incrementGlobalCount()
  assert.equal(getGlobalCount(), 2)
  assert.equal(globalRemaining(), GLOBAL_LIMIT - 2)
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @arh/server exec tsx --test src/services/usageStore.test.ts`
Expected: FAIL(模块不存在)。

- [ ] **Step 3: 实现**

`packages/server/src/services/usageStore.ts`:
```ts
// packages/server/src/services/usageStore.ts
import type { DB } from './db.js'

/** 全站累计成功问答上限。达到后普通用户被拒(管理员豁免)。 */
export const GLOBAL_LIMIT = Number(process.env.GLOBAL_LIMIT) || 1000

let _db: DB | null = null

export function initUsageTable(db: DB): void {
  _db = db
  db.exec(`
    CREATE TABLE IF NOT EXISTS usage (
      id          INTEGER PRIMARY KEY CHECK (id = 1),
      total_count INTEGER NOT NULL DEFAULT 0
    );
  `)
  db.prepare('INSERT OR IGNORE INTO usage (id, total_count) VALUES (1, 0)').run()
}

function db(): DB {
  if (!_db) throw new Error('usageStore not initialized — call initUsageTable() first')
  return _db
}

export function getGlobalCount(): number {
  return (db().prepare('SELECT total_count FROM usage WHERE id = 1').get() as { total_count: number }).total_count
}

export function incrementGlobalCount(): void {
  db().prepare('UPDATE usage SET total_count = total_count + 1 WHERE id = 1').run()
}

export function globalRemaining(): number {
  return Math.max(0, GLOBAL_LIMIT - getGlobalCount())
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @arh/server exec tsx --test src/services/usageStore.test.ts`
Expected: PASS。

- [ ] **Step 5: 装配 index.ts**

`packages/server/src/index.ts`:import 加 `import { initUsageTable } from './services/usageStore.js'`;在 `initUserTables(db)` 后加 `initUsageTable(db)`。

- [ ] **Step 6: 提交**

```bash
git add packages/server/src/services/usageStore.ts packages/server/src/services/usageStore.test.ts packages/server/src/index.ts
git commit -m "feat(auth): usageStore 全局问答计数 + GLOBAL_LIMIT"
```

---

### Task 3: 门禁守卫 + 限次(documents / traces / chat)

**Files:**
- Modify: `packages/server/src/routes/documents.ts`
- Modify: `packages/server/src/routes/traces.ts`
- Modify: `packages/server/src/routes/chat.ts`

**Interfaces:**
- Consumes: `requireUser`/`requireAdmin`/`currentUser`(auth)、`canSend`/`incrementMessageCount`/`remaining`/`MESSAGE_LIMIT`(userStore)、`getGlobalCount`/`incrementGlobalCount`/`GLOBAL_LIMIT`(usageStore)。
- Produces: documents 写操作限管理员;traces 限管理员;chat/stream 限登录 + 双重限次。

- [ ] **Step 1: documents 写操作加 requireAdmin**

在 `packages/server/src/routes/documents.ts`:
- 顶部 import 加:`import { requireAdmin } from './auth.js'`
- `POST /documents` handler 首行加:
```ts
    if (!requireAdmin(request, reply)) return
```
- `DELETE /documents/:id` handler 首行加同样一行。
(`GET /documents`、`GET /documents/:id` 不加,保持公开。)

- [ ] **Step 2: traces 全部加 requireAdmin**

在 `packages/server/src/routes/traces.ts`:
- 顶部 import 加:`import { requireAdmin } from './auth.js'`
- 三个 handler(`/traces`、`/traces/stats`、`/traces/:id`)各自首行加:
```ts
    if (!requireAdmin(req, reply)) return
```

- [ ] **Step 3: chat/stream 加登录 + 限次**

在 `packages/server/src/routes/chat.ts`:
- 顶部 import 加:
```ts
import { requireUser } from './auth.js'
import { canSend, incrementMessageCount, remaining, MESSAGE_LIMIT } from '../services/userStore.js'
import { getGlobalCount, incrementGlobalCount, GLOBAL_LIMIT } from '../services/usageStore.js'
```
- `POST /chat/stream` handler 在参数校验(docId/message/getDocument)**之后、`runInTrace` 之前**插入:
```ts
    const user = requireUser(request, reply)
    if (!user) return
    if (user.is_admin !== 1) {
      if (!canSend(user)) {
        return reply.status(403).send({ error: 'message_limit_reached', scope: 'user', remaining: remaining(user), limit: MESSAGE_LIMIT })
      }
      if (getGlobalCount() >= GLOBAL_LIMIT) {
        return reply.status(403).send({ error: 'message_limit_reached', scope: 'global', limit: GLOBAL_LIMIT })
      }
    }
```
- 在 `llm_generation` span 内、`send({ done: true })` **成功那一行之后**(即生成成功后)加计数(仅普通用户计,管理员不计):
```ts
          if (user.is_admin !== 1) { incrementMessageCount(user.id); incrementGlobalCount() }
```
放在 `spanOutput(out)` 之后、`send({ done: true })` 之前或之后均可,但必须在 for-await 正常结束后、catch 之外。

- [ ] **Step 4: 类型检查 + 冒烟(AUTH_DISABLED=admin 豁免路径)**

Run:
```bash
pnpm --filter @arh/server exec tsc --noEmit
lsof -tiTCP:3001 -sTCP:LISTEN | xargs -r kill -9; sleep 1
(AUTH_DISABLED=true pnpm --filter @arh/server start >/tmp/a.log 2>&1 &)
for i in $(seq 1 25); do curl -s localhost:3001/health >/dev/null 2>&1 && break; sleep 1; done
echo "traces(admin 可见):" && curl -s -o /dev/null -w "%{http_code}\n" localhost:3001/api/traces
echo "documents 列表(公开):" && curl -s -o /dev/null -w "%{http_code}\n" localhost:3001/api/documents
```
Expected: `/api/traces` 200(AUTH_DISABLED 下是合成管理员);`/api/documents` 200。停掉后台。
> 注:非管理员被拒(403)与个人/全局限次的完整验证靠 Task 与单测覆盖(AUTH_DISABLED 恒为管理员,无法在本地 E2E 走普通用户路径);限次逻辑正确性由 usageStore/userStore 的 `canSend`/计数单测保证。

- [ ] **Step 5: 提交**

```bash
git add packages/server/src/routes/documents.ts packages/server/src/routes/traces.ts packages/server/src/routes/chat.ts
git commit -m "feat(auth): 门禁守卫(documents 写/traces 限管理员,chat 限登录+双重限次)"
```

---

### Task 4: chatStore 对话保存 + 恢复接口

**Files:**
- Create: `packages/server/src/services/chatStore.ts`
- Test: `packages/server/src/services/chatStore.test.ts`
- Modify: `packages/server/src/index.ts`(initChatTable)
- Modify: `packages/server/src/routes/chat.ts`(落库 + GET messages)

**Interfaces:**
- Consumes: `DB` from `./db.js`。
- Produces:
  - `initChatTable(db:DB)`、`appendMessage(userId:string, docId:string, m:{role:'user'|'assistant'; content:string; sources?:unknown}):void`、`getMessages(userId:string, docId:string):ChatMessageRow[]`、类型 `ChatMessageRow`(`{id,user_id,doc_id,role,content,sources_json,seq,created_at}`)。
  - `GET /api/chat/messages?docId=` → `{ messages: Array<{role,content,sources}> }`。

- [ ] **Step 1: 写失败测试**

`packages/server/src/services/chatStore.test.ts`:
```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import Database from 'better-sqlite3'
import { initChatTable, appendMessage, getMessages } from './chatStore.ts'

test('按 user+doc 存取,seq 递增,互相隔离', () => {
  const db = new Database(':memory:')
  initChatTable(db)
  appendMessage('u1', 'docA', { role: 'user', content: '问题1' })
  appendMessage('u1', 'docA', { role: 'assistant', content: '回答1', sources: [{ s: 1 }] })
  appendMessage('u1', 'docB', { role: 'user', content: '别的报告' })
  appendMessage('u2', 'docA', { role: 'user', content: '别人的' })

  const a = getMessages('u1', 'docA')
  assert.equal(a.length, 2)
  assert.equal(a[0].seq, 0)
  assert.equal(a[1].seq, 1)
  assert.equal(a[1].role, 'assistant')
  assert.equal(a[1].sources_json, JSON.stringify([{ s: 1 }]))
  assert.equal(getMessages('u1', 'docB').length, 1)  // 按 doc 隔离
  assert.equal(getMessages('u2', 'docA').length, 1)  // 按 user 隔离
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @arh/server exec tsx --test src/services/chatStore.test.ts`
Expected: FAIL(模块不存在)。

- [ ] **Step 3: 实现**

`packages/server/src/services/chatStore.ts`:
```ts
// packages/server/src/services/chatStore.ts
import type { DB } from './db.js'

export interface ChatMessageRow {
  id: string
  user_id: string
  doc_id: string
  role: 'user' | 'assistant'
  content: string
  sources_json: string | null
  seq: number
  created_at: string
}

let _db: DB | null = null

export function initChatTable(db: DB): void {
  _db = db
  db.exec(`
    CREATE TABLE IF NOT EXISTS chat_messages (
      id           TEXT PRIMARY KEY,
      user_id      TEXT NOT NULL,
      doc_id       TEXT NOT NULL,
      role         TEXT NOT NULL,
      content      TEXT NOT NULL,
      sources_json TEXT,
      seq          INTEGER NOT NULL,
      created_at   TEXT NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_chat_user_doc ON chat_messages(user_id, doc_id, seq);
  `)
}

function db(): DB {
  if (!_db) throw new Error('chatStore not initialized — call initChatTable() first')
  return _db
}

function genId(): string {
  return `msg_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`
}

export function appendMessage(
  userId: string,
  docId: string,
  m: { role: 'user' | 'assistant'; content: string; sources?: unknown },
): void {
  const seqRow = db()
    .prepare('SELECT COALESCE(MAX(seq) + 1, 0) AS next FROM chat_messages WHERE user_id = ? AND doc_id = ?')
    .get(userId, docId) as { next: number }
  db().prepare(
    `INSERT INTO chat_messages (id, user_id, doc_id, role, content, sources_json, seq, created_at)
     VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
  ).run(
    genId(), userId, docId, m.role, m.content,
    m.sources === undefined ? null : JSON.stringify(m.sources),
    seqRow.next, new Date().toISOString(),
  )
}

export function getMessages(userId: string, docId: string): ChatMessageRow[] {
  return db()
    .prepare('SELECT * FROM chat_messages WHERE user_id = ? AND doc_id = ? ORDER BY seq ASC')
    .all(userId, docId) as ChatMessageRow[]
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @arh/server exec tsx --test src/services/chatStore.test.ts`
Expected: PASS。

- [ ] **Step 5: 装配 index.ts + chat 路由落库与恢复**

`index.ts`:import 加 `import { initChatTable } from './services/chatStore.js'`;`initUsageTable(db)` 后加 `initChatTable(db)`。

`packages/server/src/routes/chat.ts`:
- 顶部 import 加:`import { initChatTable as _unused } from '../services/chatStore.js'` —— 不,直接:`import { appendMessage, getMessages } from '../services/chatStore.js'`
- `/chat/stream`:在参数校验通过、拿到 `user` 后,`runInTrace` 内 `send({ sources })` 之后落库 user 消息:
```ts
      appendMessage(user.id, docId, { role: 'user', content: message })
```
- `llm_generation` 成功后(`spanOutput(out)` 之后)落库 assistant 消息(带 sources):
```ts
          appendMessage(user.id, docId, { role: 'assistant', content: out, sources })
```
  (`sources` 变量在此作用域可见——它在 `send({ sources })` 处已计算。)
- 新增恢复接口(放在 `chatRoutes` 内):
```ts
  app.get<{ Querystring: { docId?: string } }>('/chat/messages', async (request, reply) => {
    const user = requireUser(request, reply)
    if (!user) return
    const docId = request.query.docId
    if (!docId) return reply.status(400).send({ error: 'docId is required' })
    const messages = getMessages(user.id, docId).map(r => ({
      role: r.role,
      content: r.content,
      sources: r.sources_json ? JSON.parse(r.sources_json) : undefined,
    }))
    return { messages }
  })
```

- [ ] **Step 6: 类型检查 + 提交**

Run: `pnpm --filter @arh/server exec tsc --noEmit`
Expected: 0 错。
```bash
git add packages/server/src/services/chatStore.ts packages/server/src/services/chatStore.test.ts packages/server/src/index.ts packages/server/src/routes/chat.ts
git commit -m "feat(auth): chatStore 按用户+报告保存对话 + GET /chat/messages 恢复"
```

---

### Task 5: 前端 useAuth + 头部登录态 + 按角色隐藏管理 UI

**Files:**
- Create: `packages/client/src/hooks/useAuth.ts`(移植)
- Modify: `packages/client/src/pages/HomePage.tsx`、`packages/client/src/pages/HomePage.module.css`

**Interfaces:**
- Consumes: `GET /api/auth/me`。
- Produces: `useAuth()` → `{ user, loading, login, logout, refresh }`(`user` 为 `AuthUser|null`,含 `username/avatarUrl/isAdmin/remaining`)。首页头部:未登录显示登录按钮;登录显示头像+用户名+剩余次数+登出;非管理员隐藏上传/删除与 trace 入口。

- [ ] **Step 1: 移植 useAuth**

Run:
```bash
cp ~/myCode/docmind/packages/client/src/hooks/useAuth.ts packages/client/src/hooks/useAuth.ts
```
该文件无需改动(纯 fetch `/api/auth/*`,无外部 import)。

- [ ] **Step 2: 首页接入登录态 + 角色隐藏**

`packages/client/src/pages/HomePage.tsx` 改为(在现有基础上引入 useAuth):
```tsx
import { useEffect, useRef, useState } from 'react'
import { useNavigate, Link } from 'react-router-dom'
import { api } from '../api'
import { useAuth } from '../hooks/useAuth'
import type { Document } from '../types'
import styles from './HomePage.module.css'

export default function HomePage() {
  const [docs, setDocs] = useState<Document[]>([])
  const [uploading, setUploading] = useState(false)
  const [error, setError] = useState('')
  const fileRef = useRef<HTMLInputElement>(null)
  const navigate = useNavigate()
  const { user, login, logout } = useAuth()
  const isAdmin = user?.isAdmin === true

  const refresh = () => api.listDocuments().then(setDocs).catch(e => setError(String(e.message)))
  useEffect(() => { refresh() }, [])

  async function onFile(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0]
    if (!file) return
    setUploading(true); setError('')
    try { await api.uploadDocument(file); await refresh() }
    catch (err) { setError(err instanceof Error ? err.message : '上传失败') }
    finally { setUploading(false); if (fileRef.current) fileRef.current.value = '' }
  }

  async function onDelete(e: React.MouseEvent, id: string) {
    e.stopPropagation()
    if (!confirm('删除这篇报告?')) return
    await api.deleteDocument(id); await refresh()
  }

  return (
    <div className={styles.page}>
      <header className={styles.header}>
        <h1>研报站</h1>
        <div className={styles.headerRight}>
          {isAdmin && <Link to="/traces" className={styles.traceLink}>🔍 trace</Link>}
          {isAdmin && (
            <label className={styles.upload}>
              {uploading ? '上传中…' : '+ 上传研报 (.md)'}
              <input ref={fileRef} type="file" accept=".md,.markdown,.txt" onChange={onFile} hidden />
            </label>
          )}
          {user ? (
            <span className={styles.userBox}>
              {user.avatarUrl && <img className={styles.avatar} src={user.avatarUrl} alt="" />}
              <span className={styles.uname}>{user.username}</span>
              {!user.unlimited && <span className={styles.quota}>剩余 {user.remaining}</span>}
              <button className={styles.logout} onClick={() => void logout()}>登出</button>
            </span>
          ) : (
            <button className={styles.loginBtn} onClick={login}>GitHub 登录</button>
          )}
        </div>
      </header>
      {error && <div className={styles.error}>{error}</div>}
      <div className={styles.grid}>
        {docs.map(d => (
          <article key={d.id} className={styles.card} onClick={() => navigate(`/reports/${d.id}`)}>
            <h2 className={styles.title}>{d.filename}</h2>
            <div className={styles.meta}>
              <span>{new Date(d.created_at).toLocaleDateString('zh-CN')}</span>
              <span>{d.chunk_count} 段</span>
            </div>
            {isAdmin && <button className={styles.del} onClick={e => onDelete(e, d.id)}>删除</button>}
          </article>
        ))}
        {docs.length === 0 && <p className={styles.empty}>还没有报告。</p>}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 头部样式**

在 `packages/client/src/pages/HomePage.module.css` 追加:
```css
.userBox { display: flex; align-items: center; gap: 8px; font-size: 14px; }
.avatar { width: 26px; height: 26px; border-radius: 50%; }
.uname { color: #333; }
.quota { color: #8a6d1b; background: #faf8f0; border: 1px solid #e7d9a8; border-radius: 999px; padding: 2px 8px; font-size: 12px; }
.logout, .loginBtn { border: 1px solid #ddd; background: #fff; border-radius: 8px; padding: 7px 12px; font-size: 14px; cursor: pointer; }
.loginBtn { background: #1a1a1a; color: #fff; border-color: #1a1a1a; }
.logout:hover { background: #f3f3f0; }
```

- [ ] **Step 4: 类型检查 + 构建**

Run: `pnpm --filter @arh/client exec tsc --noEmit && pnpm --filter @arh/client build`
Expected: 均通过。

- [ ] **Step 5: 提交**

```bash
git add packages/client/src/hooks/useAuth.ts packages/client/src/pages/HomePage.tsx packages/client/src/pages/HomePage.module.css
git commit -m "feat(auth): 前端 useAuth + 头部登录态 + 按角色隐藏上传/删除/trace"
```

---

### Task 6: 阅读页问答区 登录引导 + 对话历史恢复

**Files:**
- Modify: `packages/client/src/api.ts`(加 getMessages)
- Modify: `packages/client/src/hooks/useDocChat.ts`(初始加载历史)
- Modify: `packages/client/src/components/ChatPanel.tsx`(未登录引导 + 剩余次数)
- Modify: `packages/client/src/components/ChatPanel.module.css`

**Interfaces:**
- Consumes: `GET /api/chat/messages?docId=`、`useAuth`。
- Produces: 已登录进报告时恢复该用户历史;未登录时问答区显示"登录后提问"、输入禁用。

- [ ] **Step 1: api 加 getMessages**

在 `packages/client/src/api.ts` 的 `api` 对象里加:
```ts
  async getMessages(docId: string): Promise<import('./types').ChatMessage[]> {
    const r = await fetch(`/api/chat/messages?docId=${encodeURIComponent(docId)}`)
    if (!r.ok) return []
    return (await r.json()).messages
  },
```

- [ ] **Step 2: useDocChat 初始加载历史**

在 `packages/client/src/hooks/useDocChat.ts`,给 hook 加一个初始加载(仅登录时有数据,匿名返回空):
- 顶部加 `import { useEffect } from 'react'`(与现有 `useState` 合并 import)。
- 在 `const [messages, setMessages] = useState<ChatMessage[]>([])` 之后加:
```ts
  useEffect(() => {
    let alive = true
    import('../api').then(({ api }) => api.getMessages(docId)).then(m => { if (alive && m.length) setMessages(m) })
    return () => { alive = false }
  }, [docId])
```
（send 成功后服务端已落库,刷新/重进由此恢复;当前会话内 messages 本地已累积,无需额外处理。)

- [ ] **Step 3: ChatPanel 未登录引导 + 剩余次数**

`packages/client/src/components/ChatPanel.tsx` 改为:
```tsx
import { useState } from 'react'
import { useDocChat } from '../hooks/useDocChat'
import { useAuth } from '../hooks/useAuth'
import Markdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import styles from './ChatPanel.module.css'

export default function ChatPanel({ docId, onCite }: { docId: string; onCite: (slug: string) => void }) {
  const { messages, send, streaming } = useDocChat(docId)
  const { user, login } = useAuth()
  const [input, setInput] = useState('')

  function submit(e: React.FormEvent) {
    e.preventDefault()
    const text = input.trim()
    if (!text) return
    setInput('')
    void send(text).then(() => window.dispatchEvent(new Event('auth:refresh')))
  }

  const outOfQuota = user && !user.unlimited && (user.remaining ?? 0) <= 0

  return (
    <div className={styles.panel}>
      <div className={styles.head}>
        问这篇报告
        {user && !user.unlimited && <span className={styles.quota}>剩余 {user.remaining}</span>}
      </div>
      <div className={styles.messages}>
        {messages.length === 0 && <p className={styles.hint}>就当前报告提问,答案会标注来源章节。</p>}
        {messages.map((m, i) => (
          <div key={i} className={m.role === 'user' ? styles.user : styles.assistant}>
            {m.role === 'assistant'
              ? <Markdown remarkPlugins={[remarkGfm]}>{m.content || '…'}</Markdown>
              : m.content}
            {m.sources && m.sources.length > 0 && (
              <div className={styles.sources}>
                {m.sources.map((s, j) => (
                  <button key={j} className={styles.chip} onClick={() => onCite(s.section_slug)}>
                    来源 §{s.section_title || '引言'}
                  </button>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
      {user ? (
        <form className={styles.inputRow} onSubmit={submit}>
          <input value={input} onChange={e => setInput(e.target.value)}
                 placeholder={outOfQuota ? '次数已用完' : streaming ? '生成中…' : '输入问题,回车发送'}
                 disabled={streaming || !!outOfQuota} />
          <button type="submit" disabled={streaming || !!outOfQuota || !input.trim()}>发送</button>
        </form>
      ) : (
        <div className={styles.loginPrompt}>
          <span>登录后即可就本篇报告提问</span>
          <button className={styles.loginBtn} onClick={login}>GitHub 登录</button>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 4: 样式**

在 `packages/client/src/components/ChatPanel.module.css` 追加:
```css
.head { display: flex; align-items: center; justify-content: space-between; }
.quota { color: #8a6d1b; background: #faf8f0; border: 1px solid #e7d9a8; border-radius: 999px; padding: 2px 8px; font-size: 12px; }
.loginPrompt { display: flex; flex-direction: column; gap: 10px; align-items: center; padding: 20px 16px; border-top: 1px solid #eee; color: #666; font-size: 14px; }
.loginBtn { background: #1a1a1a; color: #fff; border: none; border-radius: 8px; padding: 9px 16px; cursor: pointer; }
```

- [ ] **Step 5: 类型检查 + 构建**

Run: `pnpm --filter @arh/client exec tsc --noEmit && pnpm --filter @arh/client build`
Expected: 均通过。

- [ ] **Step 6: 提交**

```bash
git add packages/client/src/api.ts packages/client/src/hooks/useDocChat.ts packages/client/src/components/ChatPanel.tsx packages/client/src/components/ChatPanel.module.css
git commit -m "feat(auth): 阅读页问答区登录引导 + 对话历史恢复 + 剩余次数"
```

---

### Task 7: 端到端手测(AUTH_DISABLED)+ 全后端测试

**Files:** 无。

- [ ] **Step 1: 起栈(AUTH_DISABLED=true,合成管理员)**

```bash
docker compose -f docker-compose.dev.yml up chroma -d   # 8000 已有则跳过
lsof -tiTCP:3001 -sTCP:LISTEN | xargs -r kill -9; sleep 1
(AUTH_DISABLED=true pnpm --filter @arh/server start >/tmp/a.log 2>&1 &)
pnpm dev:client
```
浏览器核对(合成管理员身份):
1. 首页 header 显示用户名 `dev` + 登出;上传按钮与 "🔍 trace" 可见(管理员)。
2. 进报告问一个问题 → 有答案 + 来源;刷新页面 → 该报告问答历史恢复。
3. `/traces` 可见(管理员)。
Expected: 均正常。

- [ ] **Step 2: 匿名/普通用户路径说明**

`AUTH_DISABLED=true` 恒为管理员,无法本地走匿名/普通用户。这两条路径由以下保证:
- 后端守卫返回码:临时改 `AUTH_DISABLED=false`(不配 OAuth)重启,`curl /api/auth/me` 应 401;`curl -X POST /api/chat/stream` 应 401;`curl /api/traces` 应 401/403。
```bash
lsof -tiTCP:3001 -sTCP:LISTEN | xargs -r kill -9; sleep 1
(pnpm --filter @arh/server start >/tmp/a.log 2>&1 &)   # 不设 AUTH_DISABLED
sleep 4
echo "me:" && curl -s -o /dev/null -w "%{http_code}\n" localhost:3001/api/auth/me
echo "traces:" && curl -s -o /dev/null -w "%{http_code}\n" localhost:3001/api/traces
echo "chat:" && curl -s -o /dev/null -w "%{http_code}\n" -X POST localhost:3001/api/chat/stream -H 'Content-Type: application/json' -d '{"docId":"x","message":"y"}'
echo "documents 列表(公开):" && curl -s -o /dev/null -w "%{http_code}\n" localhost:3001/api/documents
```
Expected: me=401,traces=401,chat=401,documents=200。停掉后台。

- [ ] **Step 3: 全后端测试**

Run: `pnpm --filter @arh/server test && pnpm --filter @arh/client test`
Expected: 全绿(后端 + usageStore + chatStore;前端 3 文件)。

- [ ] **Step 4: 工作树确认干净**

Run: `git status --porcelain`
Expected: 空。

---

## Self-Review

**Spec coverage:**
- 三类身份 + 门禁(A1/A4)→ Task 1(auth)+ Task 3(守卫)✅
- 每人 10 次(A3)→ Task 3 用 canSend/incrementMessageCount ✅
- 全局上限(A3)→ Task 2(usageStore)+ Task 3(检查/自增)✅
- 管理员改 DB(A1)→ 无代码,Global Constraints 说明 ✅
- 对话保存(A5)→ Task 4(chatStore + 落库 + 恢复接口)+ Task 6(前端恢复)✅
- 前端登录态/角色隐藏(A6)→ Task 5 ✅
- 问答区登录引导/剩余次数(A6)→ Task 6 ✅
- AUTH_DISABLED 本地路径(约束)→ Task 1/7 ✅

**Placeholder scan:** 无 TBD/TODO;移植用 cp+edit,新代码(usageStore/chatStore/前端)均完整。Task 4 Step 5 修正了一处笔误(直接 import appendMessage/getMessages)。

**Type consistency:** `User.is_admin`(number,1=admin)在 Task 1/3 一致判断 `!== 1`;`remaining/canSend/MESSAGE_LIMIT`(userStore)与 Task 3 调用一致;`getGlobalCount/incrementGlobalCount/GLOBAL_LIMIT`(usageStore)与 Task 3 一致;`appendMessage(userId,docId,{role,content,sources?})`/`getMessages`(Task 4)与 chat 路由调用及前端 `ChatMessage{role,content,sources?}` 一致;`AuthUser{isAdmin,unlimited,remaining,avatarUrl,username}`(useAuth)与 Task 5/6 用法一致。

**已知盲点(手测/单测覆盖):** 真实 GitHub OAuth 往返无法本地 E2E(需 OAuth app),由守卫返回码(Task 7 Step 2)+ 限次单测覆盖;`unlimited` 字段前端来自 `/auth/me` 的 `publicUser`(docmind 已含),Task 5/6 直接用。
