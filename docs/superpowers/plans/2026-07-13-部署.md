# 部署(子系统 B)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把研报站打包成"本地一条命令 `./deploy.sh` 部署到远程服务器"的全套部署件,并本地构建 + 冒烟验证;真正上线所需的服务器/域名/GitHub OAuth app/密钥由用户补(占位符版)。

**Architecture:** 移植适配 docmind 的部署栈:三容器(client nginx 仅回环 → server tsx → chroma),`docker-compose.prod.yml` 编排,`deploy.sh` 经 SSH docker context 在远程构建运行,密钥只读自本地 `.env.prod`。

**Tech Stack:** Docker + docker compose;server 镜像 node:20-alpine + tsx(better-sqlite3 alpine 源码编译);client 镜像 多阶段 vite build → nginx。docmind 源在 `~/myCode/docmind`(下称 `$DM`)。

## Global Constraints

- 本次只交付**备齐 + 本地验证**的部署件;不替用户上线公网(无其服务器/域名;注册 OAuth app 是其账号动作)。
- 用**占位符**:`APP_URL=https://your-domain.example`、OAuth/密钥留空,DEPLOY.md 写清用户如何填。
- ChromaDB 集合名 `research_docs`;检索 `RAG_MAX_K=10`。
- 数据全在 `/app/data`(SQLite `data/research.db` + 原文 `data/raw/`)——**只需一个 `server_data:/app/data` 卷**(不要 docmind 的 uploads 卷)。
- **关键:包内 tsconfig 必须自包含**(不能 `extends ../../tsconfig.base.json`),否则 Docker 以包目录为 context 构建时找不到根 base。Task 1 处理。
- client 容器只绑 `127.0.0.1:${CLIENT_PORT:-8080}`(公网经 host nginx+Cloudflare,不直连)。
- `.env.prod`(真实密钥)必须 `.gitignore`;只提交 `.env.prod.example`。
- 部署脚本、context 名统一用 `ai-research-hub`(不留 docmind)。

---

### Task 1: tsconfig 自包含(去 extends)+ 验证 dev 不回归

**Files:**
- Modify: `packages/server/tsconfig.json`、`packages/client/tsconfig.json`
- Delete: `tsconfig.base.json`

**Interfaces:** 无运行时接口;确保 `pnpm typecheck` + `pnpm --filter @arh/client build` 仍通过。

- [ ] **Step 1: server tsconfig 内联 base**

把 `packages/server/tsconfig.json` 整体替换为(内联原 base 的 compilerOptions,去掉 extends):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src"]
}
```

- [ ] **Step 2: client tsconfig 内联 base**

把 `packages/client/tsconfig.json` 整体替换为:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true
  },
  "include": ["src"]
}
```

- [ ] **Step 3: 删除根 base**

Run: `rm tsconfig.base.json`

- [ ] **Step 4: 验证 dev 无回归**

Run:
```bash
pnpm --filter @arh/server exec tsc --noEmit
pnpm --filter @arh/client exec tsc --noEmit
pnpm --filter @arh/server test
pnpm --filter @arh/client test
pnpm --filter @arh/client build
```
Expected: 全部通过(server 14/14,client 8/8,build 成功)。若 client tsc 因 `noUnusedLocals` 之类新报错——不要加那些选项(base 里本就没有);保持上面给出的精确选项集。

- [ ] **Step 5: 提交**

```bash
git add packages/server/tsconfig.json packages/client/tsconfig.json
git rm tsconfig.base.json
git commit -m "chore(deploy): 包内 tsconfig 自包含(去 extends 根 base),便于包目录为 context 的 Docker 构建"
```

---

### Task 2: server 镜像(Dockerfile + .dockerignore)

**Files:**
- Create: `packages/server/Dockerfile`
- Create: `packages/server/.dockerignore`

**Interfaces:** 产出可构建的 server 镜像,`docker build` 成功。

- [ ] **Step 1: Dockerfile**

`packages/server/Dockerfile`:
```dockerfile
FROM node:20-alpine

WORKDIR /app

# better-sqlite3 是原生模块 — alpine 需要编译工具链(g++ 支持 c++20,可源码构建)
RUN apk add --no-cache python3 make g++

# 装依赖。--legacy-peer-deps 容忍宽松 peer;devDependencies 保留(tsx/typescript 运行时需要)
COPY package.json ./
RUN npm install --legacy-peer-deps

# 源码 + tsconfig(tsx 运行时读取)
COPY tsconfig.json ./
COPY src ./src

ENV NODE_ENV=production
EXPOSE 3001

# tsx 运行时转译 TypeScript,入口 src/index.ts
CMD ["npx", "tsx", "src/index.ts"]
```

- [ ] **Step 2: .dockerignore**

`packages/server/.dockerignore`:
```
node_modules
data
.env
.env.prod
*.log
Dockerfile
.dockerignore
```

- [ ] **Step 3: 构建验证**

Run: `docker build -t arh-server-test packages/server`
Expected: 构建成功(含 better-sqlite3 从源码编译通过);末尾 `naming to ...arh-server-test`。构建后可删镜像:`docker rmi arh-server-test`。

- [ ] **Step 4: 提交**

```bash
git add packages/server/Dockerfile packages/server/.dockerignore
git commit -m "feat(deploy): server 镜像(node:20-alpine + tsx + better-sqlite3 源码编译)"
```

---

### Task 3: client 镜像(Dockerfile + nginx.conf + .dockerignore)

**Files:**
- Create: `packages/client/Dockerfile`
- Create: `packages/client/nginx.conf`
- Create: `packages/client/.dockerignore`

**Interfaces:** 产出可构建的 client 镜像(nginx 提供 SPA + 反代 /api)。

- [ ] **Step 1: nginx.conf**

`packages/client/nginx.conf`:
```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — 所有路由返回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 代理 API 到 server 容器
    location /api {
        proxy_pass http://server:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        # SSE 关键:禁用缓冲 + 长超时(流式问答)
        proxy_buffering off;
        proxy_read_timeout 300s;
    }
}
```

- [ ] **Step 2: Dockerfile**

`packages/client/Dockerfile`:
```dockerfile
FROM node:20-alpine AS builder

WORKDIR /app
COPY package.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
```

- [ ] **Step 3: .dockerignore**

`packages/client/.dockerignore`:
```
node_modules
dist
.env
*.log
Dockerfile
.dockerignore
```

- [ ] **Step 4: 构建验证**

Run: `docker build -t arh-client-test packages/client`
Expected: 构建成功(vite build 产出 dist,可能有一条 dynamic import chunk 警告,无害)。构建后可删:`docker rmi arh-client-test`。

- [ ] **Step 5: 提交**

```bash
git add packages/client/Dockerfile packages/client/nginx.conf packages/client/.dockerignore
git commit -m "feat(deploy): client 镜像(多阶段 vite build → nginx,SSE 反代)"
```

---

### Task 4: compose.prod + .env.prod.example + gitignore

**Files:**
- Create: `docker-compose.prod.yml`
- Create: `packages/server/.env.prod.example`
- Modify: `.gitignore`(加 `.env.prod`)

**Interfaces:** `docker compose -f docker-compose.prod.yml config` 通过。

- [ ] **Step 1: compose.prod**

`docker-compose.prod.yml`:
```yaml
services:
  chroma:
    image: chromadb/chroma:latest
    restart: unless-stopped
    expose:
      - "8000"
    volumes:
      - chroma_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE

  server:
    build:
      context: ./packages/server
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3001"
    env_file:
      - ./packages/server/.env.prod
    environment:
      - CHROMA_URL=http://chroma:8000
      - PORT=3001
      - RAG_MAX_K=10
    depends_on:
      - chroma
    volumes:
      - server_data:/app/data

  client:
    build:
      context: ./packages/client
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      # 只绑回环:公网经 host nginx + Cloudflare,不直连 :8080。CLIENT_BIND=0.0.0.0 可暴露。
      - "${CLIENT_BIND:-127.0.0.1}:${CLIENT_PORT:-8080}:80"
    depends_on:
      - server

volumes:
  chroma_data:
  server_data:
```

- [ ] **Step 2: .env.prod.example(占位符)**

`packages/server/.env.prod.example`:
```
# 生产环境配置(与本地 .env 分开)。拷成 .env.prod 后填真实值;.env.prod 已 gitignore。
# --- 智谱 ---
ZHIPU_API_KEY=
ZHIPU_MODEL=glm-4.7
ZHIPU_EMBEDDING_MODEL=embedding-3
# --- 认证:必须 false(生产严禁开 AUTH_DISABLED)---
AUTH_DISABLED=false
# 在 GitHub 注册 OAuth App 后填(见 docs/DEPLOY.md);回调填 https://your-domain.example/api/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# 你的公网域名(无尾斜杠)
APP_URL=https://your-domain.example
# 会话签名密钥:openssl rand -hex 32 生成后填
COOKIE_SECRET=
# --- 限次 ---
MESSAGE_LIMIT=10
GLOBAL_LIMIT=1000
```

- [ ] **Step 3: gitignore .env.prod**

在 `.gitignore` 追加一行 `packages/server/.env.prod`(若已被 `packages/*/.env` 之类覆盖则确认;当前根 .gitignore 有 `packages/*/.env` — 但 `.env.prod` 不被 `*/.env` 匹配,需显式加)。
Run 验证:`git check-ignore packages/server/.env.prod && echo IGNORED`

- [ ] **Step 4: 校验 compose**

Run: `docker compose -f docker-compose.prod.yml config >/dev/null && echo "compose OK"`
Expected: `compose OK`(占位符 env_file 不存在时 config 可能提示;若报 .env.prod 缺失,临时 `cp packages/server/.env.prod.example packages/server/.env.prod` 后再 config,验证完删除该临时文件——勿提交)。

- [ ] **Step 5: 提交**

```bash
git add docker-compose.prod.yml packages/server/.env.prod.example .gitignore
git commit -m "feat(deploy): compose.prod(chroma+server+client,单 data 卷)+ .env.prod 模板"
```

---

### Task 5: deploy.sh + setup-remote.sh

**Files:**
- Create: `deploy.sh`(可执行)
- Create: `scripts/setup-remote.sh`(可执行)

**Interfaces:** `bash -n` 语法通过;脚本用 context 名 `ai-research-hub`。

- [ ] **Step 1: setup-remote.sh**

`scripts/setup-remote.sh`(移植 docmind,改 context 名):
```bash
#!/usr/bin/env bash
# 一次性设置,让 ./deploy.sh 能在远程服务器构建运行。
# 唯一 SSH 触碰服务器的步骤:1) 装 Docker(如缺) 2) 建本地指向服务器的 docker context。
# 之后你只在本地跑 ./deploy.sh。
# 用法: ./scripts/setup-remote.sh user@SERVER_IP [context-name]
set -euo pipefail

TARGET="${1:-}"
CONTEXT="${2:-ai-research-hub}"

if [[ -z "$TARGET" ]]; then
  echo "用法: ./scripts/setup-remote.sh user@SERVER_IP [context-name]"
  exit 1
fi

echo "→ 检查到 $TARGET 的 SSH(需已配好免密 key)..."
if ! ssh -o BatchMode=yes -o ConnectTimeout=8 "$TARGET" true 2>/dev/null; then
  echo "✗ 无法免密 SSH 到 $TARGET。先: ssh-copy-id $TARGET"
  exit 1
fi

echo "→ 确保服务器已装 Docker ..."
ssh "$TARGET" 'command -v docker >/dev/null 2>&1 || (curl -fsSL https://get.docker.com | sh)'

echo "→ 确保当前用户可免 sudo 运行 docker ..."
ssh "$TARGET" 'id -nG "$USER" | grep -qw docker || sudo usermod -aG docker "$USER" || true'

echo "→ 建本地 docker context '$CONTEXT' → $TARGET ..."
docker context rm "$CONTEXT" >/dev/null 2>&1 || true
docker context create "$CONTEXT" --docker "host=ssh://$TARGET"

echo "→ 验证远程 daemon 可达 ..."
docker --context "$CONTEXT" version --format '{{.Server.Version}}' >/dev/null

echo
echo "✓ 完成。随时部署: ./deploy.sh"
echo "  (若 docker 需要新组权限,重连一次 SSH: ssh $TARGET)"
```

- [ ] **Step 2: deploy.sh**

`deploy.sh`(移植 docmind,改 context 名 + 文案):
```bash
#!/usr/bin/env bash
# 本地一条命令部署到远程服务器,全程不需手动 SSH 登录。
# 原理:通过 SSH docker context 在远程 daemon 上构建运行;镜像/数据卷都在远程;
# 密钥只读自本地 packages/server/.env.prod,不写入服务器磁盘、不进镜像、不进 git。
# 用法:
#   ./deploy.sh                      # 用 context "ai-research-hub"
#   DOCKER_CONTEXT=foo ./deploy.sh
# 一次性前置: ./scripts/setup-remote.sh user@SERVER_IP
set -euo pipefail
cd "$(dirname "$0")"

CONTEXT="${DOCKER_CONTEXT:-ai-research-hub}"
COMPOSE_FILE="docker-compose.prod.yml"
ENV_FILE="packages/server/.env.prod"

if ! docker context inspect "$CONTEXT" >/dev/null 2>&1; then
  echo "✗ Docker context '$CONTEXT' 不存在。"
  echo "  先跑一次性设置: ./scripts/setup-remote.sh user@SERVER_IP"
  exit 1
fi

if [[ ! -f "$ENV_FILE" ]]; then
  echo "✗ 缺少 $ENV_FILE(生产配置:智谱 key、GitHub OAuth、APP_URL、COOKIE_SECRET)。"
  echo "  创建: cp packages/server/.env.prod.example $ENV_FILE"
  echo "  然后填 APP_URL=https://你的域名 + GitHub OAuth + COOKIE_SECRET。(见 docs/DEPLOY.md)"
  exit 1
fi

echo "→ 部署到远程 context '$CONTEXT'(端口 ${CLIENT_PORT:-8080})..."
docker --context "$CONTEXT" compose -f "$COMPOSE_FILE" up -d --build --remove-orphans

echo
echo "→ 运行中的容器:"
docker --context "$CONTEXT" compose -f "$COMPOSE_FILE" ps

APP_URL="$(grep -E '^APP_URL=' "$ENV_FILE" | cut -d= -f2-)"
echo
echo "✓ 完成。站点应在: ${APP_URL:-你配置的 APP_URL}"
echo "  首次登录后,把自己设为管理员: 见 docs/DEPLOY.md 的「设管理员」一节。"
```

- [ ] **Step 3: 可执行 + 语法检查**

Run:
```bash
chmod +x deploy.sh scripts/setup-remote.sh
bash -n deploy.sh && bash -n scripts/setup-remote.sh && echo "语法 OK"
```
Expected: `语法 OK`。

- [ ] **Step 4: 提交**

```bash
git add deploy.sh scripts/setup-remote.sh
git commit -m "feat(deploy): deploy.sh + setup-remote.sh(SSH docker context 远程部署)"
```

---

### Task 6: DEPLOY.md(占位符 + OAuth 注册 + 设管理员)

**Files:**
- Create: `docs/DEPLOY.md`

**Interfaces:** 完整部署文档。

- [ ] **Step 1: 写 DEPLOY.md**

`docs/DEPLOY.md`:
````markdown
# 研报站 部署指南

本地一条命令 `./deploy.sh` 部署到远程服务器,**全程不需手动 SSH 登录服务器**。

原理:本地建一个指向服务器的 Docker context,`deploy.sh` 通过 SSH 把构建和运行都交给
服务器上的 Docker daemon。镜像和数据卷都在远程;密钥只读自本地 `packages/server/.env.prod`,
不写入服务器磁盘、不进镜像、不进 git。

## 架构(生产)

```
浏览器  https://your-domain.example
  └─ Cloudflare(证书 / 强制 HTTPS / CDN / 隐藏源站 IP)
       └─ 服务器 host nginx :443(源站证书,Cloudflare SSL=Full)
            └─ 127.0.0.1:8080  client 容器(nginx,仅回环,公网不可直连)
                 ├─ /     → SPA
                 └─ /api  → server 容器 :3001 → ChromaDB 容器 :8000
```

三容器由 `docker-compose.prod.yml` 编排:`client`(8080→80,绑回环)、`server`(3001)、`chroma`(8000)。

## 一、一次性准备

### 1. 注册 GitHub OAuth App
GitHub → Settings → Developer settings → OAuth Apps → New OAuth App:
- Application name: 任意(如 研报站)
- Homepage URL: `https://your-domain.example`
- Authorization callback URL: `https://your-domain.example/api/auth/github/callback`

创建后拿到 **Client ID**,再 Generate 一个 **Client Secret**。

### 2. 生产环境变量 `packages/server/.env.prod`
与本地 `.env` 分开,已被 gitignore。
```bash
cp packages/server/.env.prod.example packages/server/.env.prod
```
填:
- `ZHIPU_API_KEY` = 你的智谱 key
- `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` = 上一步拿到的
- `APP_URL` = `https://your-domain.example`(无尾斜杠)
- `COOKIE_SECRET` = `openssl rand -hex 32` 生成
- `AUTH_DISABLED` 保持 `false`(生产严禁开启)

### 3. 服务器 + 域名 + Cloudflare
- 一台可 SSH 的 Linux 服务器(VPS),已配好 SSH 免密 key(`ssh-copy-id user@IP`)。
- 域名解析到服务器(经 Cloudflare 代理,橙色云)。
- 服务器 host nginx 监听 443(源站证书),反代到 `127.0.0.1:8080`。Cloudflare SSL 模式 Full。
  (host nginx / 证书配置属通用运维,不在本仓库范围。)

### 4. 建 docker context
```bash
./scripts/setup-remote.sh user@SERVER_IP
```

## 二、部署
```bash
./deploy.sh
```
构建 + 启动三容器于远程。完成后访问 `https://your-domain.example`。

## 三、设管理员
管理员靠手动改 DB(无写 API)。首次用你的 GitHub 账号登录一次(在站点点"GitHub 登录"),
让 users 表里有你这行,然后在服务器上:
```bash
# 进 server 容器的 SQLite(数据在 server_data 卷 → /app/data/research.db)
docker --context ai-research-hub compose -f docker-compose.prod.yml exec server \
  sh -c "apk add --no-cache sqlite >/dev/null 2>&1; sqlite3 /app/data/research.db \
  \"UPDATE users SET is_admin=1, unlimited=1 WHERE username='你的GitHub用户名';\""
```
刷新页面即成管理员(可传删研报、看 trace、无限次)。

## 四、更新
改完代码 `git pull` 后再 `./deploy.sh` 即可(增量重建)。数据在卷里不丢。

## 密钥怎么到服务器
`deploy.sh` 用本地 docker CLI 通过 SSH 操作远程 daemon;compose 读本地 `.env.prod` 把值作为
环境变量注入远程容器。`.env.prod` 文件本身不离开你的机器、不进镜像、不进 git。
````

- [ ] **Step 2: 提交**

```bash
git add docs/DEPLOY.md
git commit -m "docs(deploy): DEPLOY.md(占位符 + OAuth 注册 + 设管理员 + 密钥流转)"
```

---

### Task 7: 本地构建 + 冒烟验证(整栈)

**Files:** 无(验证)。

- [ ] **Step 1: 三镜像构建**

Run: `docker compose -f docker-compose.prod.yml build 2>&1 | tail -20`
Expected: chroma(拉取)、server、client 三镜像构建成功,无 error。

- [ ] **Step 2: 临时 .env.prod 起栈冒烟**

用真实智谱 key + AUTH_DISABLED=true(冒烟免 OAuth;仅本地临时,勿提交):
```bash
cp packages/server/.env.prod.example packages/server/.env.prod
# 填入真实 ZHIPU_API_KEY,并把 AUTH_DISABLED 改为 true,COOKIE_SECRET 随便填
# (可从本地 packages/server/.env 抓 ZHIPU_API_KEY)
KEY=$(grep -E '^ZHIPU_API_KEY=' packages/server/.env | cut -d= -f2-)
sed -i '' -e "s|^ZHIPU_API_KEY=.*|ZHIPU_API_KEY=$KEY|" -e "s|^AUTH_DISABLED=.*|AUTH_DISABLED=true|" -e "s|^COOKIE_SECRET=.*|COOKIE_SECRET=smoketest|" packages/server/.env.prod
docker compose -f docker-compose.prod.yml up -d --build
sleep 8
echo "client(SPA):" && curl -s -o /dev/null -w "%{http_code}\n" localhost:8080
echo "api/health(经 nginx 反代):" && curl -s localhost:8080/api/health 2>/dev/null || curl -s localhost:8080/health
echo "auth/me(AUTH_DISABLED→合成管理员):" && curl -s localhost:8080/api/auth/me
```
Expected: SPA 200;`/api/auth/me` 返回合成管理员 JSON(证明 client→server→(cookie/auth) 链路通)。

- [ ] **Step 2b: 清理**

```bash
docker compose -f docker-compose.prod.yml down
rm packages/server/.env.prod   # 删除临时冒烟配置,勿提交
docker image prune -f >/dev/null 2>&1 || true
```

- [ ] **Step 3: 工作树确认干净**

Run: `git status --porcelain | grep -v '.superpowers/'`
Expected: 空(尤其确认 `.env.prod` 未被跟踪/未残留)。

---

## Self-Review

**Spec coverage(子系统 B):**
- Dockerfile×2 + nginx.conf → Task 2/3 ✅
- compose.prod(单 data 卷、research_docs、RAG_MAX_K=10、client 绑回环)→ Task 4 ✅
- deploy.sh + setup-remote.sh → Task 5 ✅
- .env.prod.example 占位符 + gitignore → Task 4 ✅
- DEPLOY.md(OAuth 注册 + 填密钥 + setup + deploy + 设管理员)→ Task 6 ✅
- 本地 build + 冒烟 → Task 7 ✅
- 边界(不替上线)→ Global Constraints + DEPLOY.md 用户步骤 ✅
- tsconfig 自包含(Docker context 前置修复)→ Task 1 ✅

**Placeholder scan:** 占位符是**有意的部署配置**(APP_URL/OAuth 留空),非计划缺口;DEPLOY.md 明确用户如何填。无 TODO/TBD。

**Type/一致性:** 集合名 `research_docs`、数据路径 `/app/data`、context 名 `ai-research-hub`、端口 8080/3001/8000 在 compose/脚本/DEPLOY.md 间一致;server 卷 `server_data:/app/data` 与 documentStore 的 `data/research.db`+`data/raw` 落点一致;RAG_MAX_K=10 与子系统一致。

**已知盲点:** 真实公网上线(host nginx/Cloudflare/DNS/OAuth 往返)本地无法验证,由 DEPLOY.md 用户步骤覆盖;本地冒烟用 AUTH_DISABLED=true 验证容器链路,不覆盖真实 OAuth。
